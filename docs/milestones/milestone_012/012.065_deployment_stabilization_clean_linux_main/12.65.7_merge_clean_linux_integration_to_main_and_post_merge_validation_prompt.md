# Milestone 12.65.7 — Merge Clean Linux Integration to Main and Post-Merge Validation

## Required Files

Prompt:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.7_merge_clean_linux_integration_to_main_and_post_merge_validation_prompt.md`

Closeout:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.7_merge_clean_linux_integration_to_main_and_post_merge_validation_closeout.md`

Create exactly one closeout.

Do not create separate merge reports, transcripts, temporary documentation, or coder-response files.

---

# 1. Purpose

This is the final milestone of the:

`12.65.x — Deployment Stabilization / Clean Linux Main`

arc.

12.65.6 concluded:

`READY FOR 12.65.7`

This milestone must:

1. prove the accepted integration branch can be safely fast-forwarded to `main`;

2. preserve the frozen historical deployment branch;

3. have the Product Owner perform the actual Git branch transition, merge, and push;

4. prove local `main`, remote `main`, and the accepted integration tip are identical after merge;

5. rebuild only the Development backend/frontend from `main`;

6. activate those rebuilt application containers without recreating PostgreSQL or Redis;

7. verify the live Mounted NAS provider and accepted 12.65.5 durable evidence still work from `main`;

8. preserve Test and Production;

9. close the stabilization arc;

10. declare whether the repository is ready to begin the 12.66 Windows Helper / Source Provider ingestion-completion arc.

No new architecture work belongs here.

---

# 2. Reasoning Level

High for Git safety and final acceptance.

Implementation reasoning should be minimal because no implementation change is expected.

---

# 3. Authoritative Branches

Repository:

`/home/chuck/projects/photo-organizer-dev`

Accepted integration branch:

`integration/linux-runtime-clean`

Target branch:

`main`

Frozen historical deployment branch:

`feature/deployment-linux-runtime`

Frozen reference:

`453032b3eddaa677094ef0eaadfa3711ff536989`

The frozen deployment branch is historical evidence only.

Do NOT merge it.

Do NOT cherry-pick from it.

Do NOT reset `main` to it.

Do NOT delete it.

---

# 4. Accepted Stabilization Evidence

Treat the completed 12.65 closeouts as authoritative.

### 12.65.1

Clean reconstruction of the Linux Mounted Source Provider while preserving:

- Windows provider;

- iCloud;

- fixture boundaries;

- Source Intake;

- Vault;

- provenance;

- schema;

- Test.

### 12.65.3

Live host provider activation:

- clean namespace service;

- clean non-root broker;

- canonical Mounted NAS topology;

- safe namespace behavior;

- services enabled.

### 12.65.4

Development activation:

- frontend pinned-builder validation;

- backend/frontend activation;

- PostgreSQL/Redis preservation;

- modern NAS Endpoint `1`;

- modern NAS Profile `2`;

- live readiness and Source Selection.

### 12.65.5

Real Mounted NAS intake:

```text
Profile 2
→ Source Intake Run 3
→ Ingestion Run 3
→ 16 new Assets
→ 16 canonical Vault objects
→ 16 provenance rows
```

Repeat:

```text
Source Intake Run 4
→ Ingestion Run 4
→ scanned 16
→ known 16
→ selected 0
→ staged 0
→ new 0
→ failed 0
```

Source remained unchanged and repeat was idempotent.

### 12.65.6

Final stabilization regression:

- 12.64 protection: 8/8 PASS;

- Mounted/provider regression: 235/235 PASS;

- full backend: 642/643 with only the exact accepted pre-existing iCloud failure;

- frontend pinned builder: PASS;

- Development/Test isolation: PASS;

- current documentation reconciled;

- controlled-reset workflow documented;

- `READY FOR 12.65.7`.

---

# 5. Required Reading

Read:

1. `docs/context/coding_agent_rules_v7.md`

2. this prompt

3. 12.65.5 closeout

4. 12.65.6 closeout

5. `docs/context/Photo_Organizer_Ingestion_Completion_Arc_Deep_Context_Handoff_v1.md`

6. `docs/context/project_context_v7.md`

7. `docs/context/project_architecture_v7.md`

8. `docs/context/project_workflow_v7.md`

Do not repeat architectural reconnaissance.

---

# 6. Milestone Mutation Boundary

This milestone has two specifically authorized mutation classes.

## A. Product Owner Git transition

The Product Owner may perform the explicitly approved:

```text
fetch
switch main
fast-forward-only main update
fast-forward-only integration merge
push main
```

sequence after coder preflight.

The coder must not perform the merge/push unless separately authorized by the Product Owner.

## B. Development backend/frontend rebuild and activation

After the successful main merge:

- backend/frontend image builds are authorized;

- bounded rollback image tags are authorized;

- backend recreation is authorized;

- frontend recreation is authorized.

PostgreSQL and Redis must NOT be rebuilt or recreated.

No other runtime mutation is authorized.

---

# 7. Explicit Prohibitions

Do NOT:

- use `git reset --hard` to establish main;

- force-push;

- rebase main;

- rebase the integration branch;

- create a merge commit if fast-forward is unavailable;

- resolve divergence automatically;

- cherry-pick the stabilization commits individually;

- merge the frozen deployment branch;

- delete any branch;

- delete historical evidence;

- dispatch ingestion;

- start Source Intake;

- mutate Source files;

- create/update/delete Source Profiles or Endpoints;

- alter Assets, provenance, Vault, Events, or schema;

- recreate PostgreSQL;

- recreate Redis;

- delete Docker volumes;

- run `docker compose down`;

- perform broad Docker prune;

- change namespace/broker/systemd/mount state;

- mutate Test;

- mutate Production;

- implement Windows Helper;

- retry Mounted Local.

---

# 8. Gate A — Integration Preflight

Before any Git branch transition, confirm:

```bash
cd /home/chuck/projects/photo-organizer-dev

git branch --show-current
git status --short
git rev-parse HEAD
git rev-parse '@{upstream}'
git rev-parse integration/linux-runtime-clean
git rev-parse origin/integration/linux-runtime-clean
git rev-parse feature/deployment-linux-runtime
git rev-parse origin/feature/deployment-linux-runtime
```

Required:

```text
current branch = integration/linux-runtime-clean
working tree clean
integration HEAD = integration upstream
frozen local ref = 453032b3eddaa677094ef0eaadfa3711ff536989
frozen remote ref = same
```

Record:

`ACCEPTED_INTEGRATION_SHA`

from the current integration tip.

This SHA must remain the authoritative merge target for the remainder of the milestone.

If integration HEAD/upstream differ, stop.

---

# 9. Gate B — Capture Pre-Merge Runtime Baseline

Before changing branches, record bounded runtime identity for:

Development:

```text
backend
frontend
PostgreSQL
Redis
```

Record:

- container ID;

- image ID;

- start timestamp;

- health;

- relevant named volumes.

Record Test container IDs/health if Test is currently running.

Do not start Test if it is stopped.

Record Source service state:

```text
photo-organizer-source-namespace.service
photo-organizer-source-identity-broker.service
```

Record only required mount identity:

```text
/mnt/photo-organizer-sources
/mnt/photo-organizer-sources/nas/photo-organizer
```

No mutation.

---

# 10. Gate C — Determine Whether Fast-Forward Merge Is Safe

The Product Owner controls Git writes.

Before giving merge commands, determine from current Git evidence whether:

```text
origin/main
```

exists and whether its current fetched state can safely become an ancestor of:

`ACCEPTED_INTEGRATION_SHA`.

Because remote state must be current, prepare a bounded Product Owner command to run:

```bash
git fetch origin
```

After the Product Owner runs it, re-evaluate:

```bash
git rev-parse origin/main
git rev-parse origin/integration/linux-runtime-clean
git merge-base --is-ancestor origin/main "$ACCEPTED_INTEGRATION_SHA"
```

The accepted integration SHA must still match:

```text
origin/integration/linux-runtime-clean
```

### Required merge topology

The merge is authorized only if:

```text
origin/main
is an ancestor of
ACCEPTED_INTEGRATION_SHA
```

This means `main` can be advanced to the exact accepted integration commit using fast-forward only.

If this ancestry condition is false:

```text
STATUS: ESCALATION REQUIRED
```

Do NOT:

- create a merge commit;

- rebase;

- cherry-pick;

- reset;

- force-push;

- attempt automatic conflict resolution.

The divergence must be reviewed separately.

---

# 11. Product Owner Git Merge Gate

If Gate C passes, provide the Product Owner the exact bounded commands necessary to perform the branch transition.

Expected shape, adjusted only for verified local branch state:

```bash
cd /home/chuck/projects/photo-organizer-dev

git status --short

git switch main

git pull --ff-only origin main

git merge --ff-only integration/linux-runtime-clean

git status --short
git rev-parse HEAD
git rev-parse integration/linux-runtime-clean

git push origin main
```

If local `main` does not exist but `origin/main` does, it is acceptable for the Product Owner to establish the local tracking branch using the exact bounded equivalent:

```bash
git switch --track -c main origin/main
```

Do not invent another branch topology.

### Stop before push if

after the local fast-forward:

```text
HEAD != ACCEPTED_INTEGRATION_SHA
```

or:

```text
working tree is not clean
```

The Product Owner must not push until the exact accepted SHA is confirmed.

The coder does not request or handle Git credentials.

---

# 12. Gate D — Post-Merge Git Identity

After the Product Owner confirms merge and push, the coder resumes validation.

Required:

```text
current branch = main

HEAD
= origin/main
= ACCEPTED_INTEGRATION_SHA
= integration/linux-runtime-clean
= origin/integration/linux-runtime-clean
```

The working tree must be clean.

Also confirm the frozen deployment refs still equal:

`453032b3eddaa677094ef0eaadfa3711ff536989`

The integration branch remains present.

Do not delete it in this milestone.

### Critical acceptance condition

The target `main` must contain the **exact accepted stabilization tree**.

A new merge commit is not acceptable for this milestone.

---

# 13. Gate E — Main Repository Smoke

Because the successful merge is fast-forward-only to the exact already-tested integration commit, do not blindly repeat every 12.65.6 regression.

Instead run a bounded repository smoke proving that execution is now from `main`.

At minimum:

```bash
git branch --show-current
git rev-parse HEAD
git status --short
```

Then run focused high-value tests for:

```text
12.64 provenance/Vault protections
Mounted provider/readiness/selection
dispatch identity
```

Use the accepted test files identified in 12.65.6.

If these smoke tests fail from the exact same commit that passed on integration:

```text
STATUS: ESCALATION REQUIRED
```

Do not modify code.

---

# 14. Gate F — Preserve Current Development Images for Rollback

Before rebuilding from `main`, obtain the exact currently running backend/frontend image IDs.

Create unique rollback tags containing:

```text
12.65.7
+
short ACCEPTED_INTEGRATION_SHA
```

for:

```text
current Development backend image
current Development frontend image
```

Do not retag:

- PostgreSQL;

- Redis;

- Test;

- Production.

Verify both rollback tags resolve to the exact pre-rebuild image IDs.

Do not delete prior rollback tags.

---

# 15. Gate G — Build Development Application Images From Main

From the repository checked out on:

`main`

build only:

```text
backend
frontend
```

using the accepted Development Compose configuration and GPU overlay.

Do not build:

```text
PostgreSQL
Redis
Test
Production
```

The build must use the exact main working tree at:

`ACCEPTED_INTEGRATION_SHA`.

Record resulting image IDs.

If either build fails:

- do not recreate any service;

- preserve current runtime;

- stop and escalate.

---

# 16. Gate H — Activate Backend From Main

Only after both application images build successfully:

recreate the Development backend only using:

```text
--no-deps
```

and the established Development Compose configuration.

Do not recreate PostgreSQL or Redis.

Wait for backend health.

Required runtime boundaries remain:

```text
non-privileged application user
approved supplemental Source-access groups
read-only /app/sources
read-only broker runtime bind
rslave Source propagation
application storage named volume
no Docker socket
no broad host-root bind
no unexpected device/capability
```

Confirm the backend reaches the existing Mounted provider.

If backend does not become healthy or violates its runtime boundary:

1. do not recreate frontend;

2. restore the previous backend image using the preserved rollback tag;

3. recreate backend only with `--no-deps`;

4. confirm rollback health;

5. stop and escalate.

Do not involve PostgreSQL/Redis in rollback.

---

# 17. Gate I — Activate Frontend From Main

Only after backend Gate H passes:

recreate Development frontend only using:

```text
--no-deps
```

Wait for frontend health.

Confirm:

- frontend root responds;

- proxied backend capability endpoint responds;

- no data service was recreated.

If frontend activation fails:

- restore only frontend from its rollback image;

- leave accepted backend running;

- confirm frontend rollback health;

- stop and escalate.

---

# 18. Gate J — Data-Service Preservation

After backend/frontend activation confirm PostgreSQL and Redis retain:

```text
same container IDs
same start timestamps
same named volumes
healthy state
```

as the pre-merge baseline.

No Development data volume may be deleted or replaced.

Unexpected PostgreSQL/Redis recreation is a blocker.

---

# 19. Gate K — Mounted NAS Post-Merge Application Smoke

Do NOT dispatch ingestion.

Using existing Profile `2` only:

1. fetch Profile `2`;

2. run readiness;

3. run Source Selection.

Required:

```text
Profile ID = 2
Endpoint ID = 1
endpoint type = NAS
relative root = development/sample-media

readiness = ready
durable identity = verified
identity match = matched

selection = selected
```

Expected persisted host root:

`/mnt/photo-organizer-sources/nas/photo-organizer/development/sample-media`

Expected Runtime Root:

`/app/sources/nas/photo-organizer/development/sample-media`

The persisted root must not be rewritten to Runtime Root.

---

# 20. Gate L — Preserve 12.65.5 Durable Evidence

Use bounded read-only application/database inspection.

Confirm:

```text
Source Intake Run 3 exists
Ingestion Run 3 exists
Source Intake Run 4 exists
Ingestion Run 4 exists
```

and both run pairs retain Profile `2` and the expected Runtime Root.

Confirm accepted content state remains:

```text
16 candidate Assets
16 Profile-2 provenance rows from first run
no duplicate candidate Asset state
no additional Profile-2 ingestion run created by 12.65.7
```

Do not dispatch ingestion merely to prove main.

Do not rehash all Source/Vault contents unless investigating an unexpected discrepancy.

12.65.5 remains authoritative for byte-level Source/Vault immutability.

---

# 21. Gate M — Source Selector / Main UI Smoke

Using rebuilt Development frontend/backend from `main`, confirm:

- active NAS Profile `2` appears once;

- NAS maps to the NAS selector category;

- selector choices derive from saved Profiles;

- Mounted capability remains bounded to accepted Linux Local/NAS access;

- External/Removable/Optical are not falsely routed through Mounted Linux access;

- iCloud remains independent.

Literal browser automation is not required.

Do not add Playwright/Cypress.

---

# 22. Gate N — Development/Test Isolation Preservation

If Test is running, use read-only inspection.

Confirm its:

```text
container IDs
images
start timestamps
volumes
networks
ports
```

remain unchanged from Gate B.

Development rebuild/activation must not recreate or alter Test.

Production must remain untouched.

If Test was inactive at baseline, do not start it.

---

# 23. Source Infrastructure Preservation

Confirm after Development activation:

```text
namespace service remains enabled/healthy
broker remains enabled/healthy
namespace root remains exact
NAS slot remains exact
```

No systemd or mount mutation is authorized.

No privileged command should normally be necessary.

If an unexpectedly required read needs sudo:

- coder provides the exact bounded read-only command;

- Product Owner executes it;

- coder never requests or handles the password.

No privileged mutation is authorized.

---

# 24. Reboot Persistence

Do NOT reboot the server in this milestone.

12.65.6 accurately recorded:

```text
services/configuration enabled
actual post-reboot proof not yet performed
```

Preserve that classification.

The lack of a reboot test is not a blocker to this exact Git/main stabilization merge because current runtime functionality and service enablement have already been established.

Do not claim post-reboot proof.

If desired, actual reboot persistence can be performed later as a separately authorized operational validation.

---

# 25. No Additional Documentation Reconciliation

The current v7 documentation set was reconciled in 12.65.6.

Do not reopen broad documentation reconciliation here.

Expected repository write after the merge is only:

`12.65.7_merge_clean_linux_integration_to_main_and_post_merge_validation_closeout.md`

If another documentation or implementation file appears to require editing:

```text
STATUS: ESCALATION REQUIRED
```

---

# 26. Rollback Philosophy

## Git

Because only fast-forward merge is authorized, do not use destructive Git rollback automatically after `main` has been pushed.

If post-merge runtime validation finds an application defect:

- preserve Git evidence;

- preserve `main`;

- restore Development backend/frontend runtime images if needed;

- stop and report.

Do not force-push `main` backward.

Any Git reversal would require a separate Product Owner decision.

## Runtime

Runtime rollback is bounded to backend/frontend image restoration only.

Do not:

```text
restore PostgreSQL
restore Redis
delete volumes
reset database
delete Source Profiles
delete ingestion evidence
delete Vault
```

---

# 27. Final Git Verification

At the end of runtime validation confirm:

```bash
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git rev-parse integration/linux-runtime-clean
git rev-parse origin/integration/linux-runtime-clean
git rev-parse feature/deployment-linux-runtime
git rev-parse origin/feature/deployment-linux-runtime
git status --short
```

Expected before closeout creation:

```text
branch = main

HEAD
= origin/main
= integration/linux-runtime-clean
= origin/integration/linux-runtime-clean
= ACCEPTED_INTEGRATION_SHA

frozen deployment refs
= 453032b3eddaa677094ef0eaadfa3711ff536989
```

After closeout creation, the only expected worktree change is the untracked closeout.

Do not stage it merely for inspection.

Validate its whitespace with bounded no-index checking.

---

# 28. Final Acceptance Matrix

The closeout must include:

| Gate                                  | Result    |
| ------------------------------------- | --------- |
| Integration preflight                 | PASS/FAIL |
| Fast-forward ancestry                 | PASS/FAIL |
| Product Owner merge                   | PASS/FAIL |
| `main` exact SHA identity             | PASS/FAIL |
| Frozen deployment branch preservation | PASS/FAIL |
| Main repository smoke                 | PASS/FAIL |
| Backend build from main               | PASS/FAIL |
| Frontend build from main              | PASS/FAIL |
| Backend activation                    | PASS/FAIL |
| Frontend activation                   | PASS/FAIL |
| PostgreSQL preservation               | PASS/FAIL |
| Redis preservation                    | PASS/FAIL |
| Mounted NAS readiness                 | PASS/FAIL |
| Mounted NAS Source Selection          | PASS/FAIL |
| 12.65.5 durable evidence preservation | PASS/FAIL |
| Source Selector/UI smoke              | PASS/FAIL |
| Development/Test isolation            | PASS/FAIL |
| Source infrastructure preservation    | PASS/FAIL |
| Reboot proof                          | DEFERRED  |

---

# 29. Escalation Conditions

Stop and report:

```text
STATUS: ESCALATION REQUIRED
```

if:

- integration branch is not clean/synchronized;

- remote integration differs from accepted integration SHA;

- `origin/main` is not an ancestor of accepted integration;

- fast-forward-only merge is unavailable;

- merge would require conflict resolution;

- Product Owner merge produces a different SHA;

- push changes remote main to a different SHA;

- frozen deployment reference changes;

- repository smoke fails on main;

- backend or frontend build fails;

- backend runtime boundary is wrong;

- backend cannot reach Mounted provider;

- backend rollback fails;

- frontend activation/rollback fails;

- PostgreSQL/Redis are recreated;

- Profile `2` no longer resolves correctly;

- persisted root or Runtime Root differs;

- 12.65.5 durable evidence changes unexpectedly;

- another ingestion run appears;

- Test is mutated;

- Production is mutated;

- Source infrastructure changes;

- schema/data repair appears necessary;

- code modification appears necessary;

- a non-fast-forward Git solution appears necessary.

Use:

```text
STATUS: ESCALATION REQUIRED

Gate:
Observed state:
Expected state:
Git state:
Runtime state:
Data state:
Rollback state:
Why continuation is unsafe:
Smallest next decision required:
Recommended Product Owner action:
```

Do not patch-and-retry or alter Git history without explicit approval.

---

# 30. Required Closeout

Create exactly:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.7_merge_clean_linux_integration_to_main_and_post_merge_validation_closeout.md`

Use:

## 1. Integration Preflight

## 2. Pre-Merge Runtime Baseline

## 3. Main / Integration Ancestry

## 4. Product Owner Merge Commands and Result

## 5. Post-Merge Git Identity

## 6. Main Repository Smoke

## 7. Rollback Image References

## 8. Main Backend/Frontend Build

## 9. Backend Activation

## 10. Frontend Activation

## 11. PostgreSQL / Redis Preservation

## 12. Mounted NAS Post-Merge Smoke

## 13. 12.65.5 Durable Evidence Preservation

## 14. Source Selector / UI Smoke

## 15. Development / Test Isolation

## 16. Source Infrastructure Preservation

## 17. Reboot Persistence Status

## 18. Frozen Historical Branch Preservation

## 19. Final Acceptance Matrix

## 20. Deviations

## 21. Known Deferred Items

## 22. Git Status

## 23. 12.65 Stabilization Arc Final Decision

End with exactly one:

```text
12.65 STABILIZATION COMPLETE — READY FOR 12.66
```

or:

```text
12.65 STABILIZATION NOT COMPLETE
```

---

# 31. Definition of Done

12.65.7 is complete only when:

- the accepted integration branch is clean and synchronized;

- remote main ancestry permits fast-forward-only merge;

- Product Owner performs the approved merge;

- no merge commit, rebase, reset, force-push, or cherry-pick is used;

- `main`, `origin/main`, local integration, and remote integration all resolve to the exact accepted stabilization SHA;

- frozen deployment refs remain unchanged;

- Development backend/frontend build successfully from `main`;

- only backend/frontend are recreated;

- PostgreSQL and Redis remain unchanged;

- backend/frontend are healthy;

- Mounted NAS Profile `2` remains ready/selectable;

- persisted host root and Runtime Root remain correct/distinct;

- 12.65.5 durable evidence remains intact;

- no ingestion occurs;

- Source Selector behavior remains correct;

- Test remains isolated and unchanged;

- Production remains untouched;

- namespace/broker/mount topology remains healthy;

- no implementation/schema/data repair occurs;

- actual reboot proof remains accurately classified as deferred;

- exactly one closeout is created;

- coder does not commit or push the closeout;

- final decision is:

```text
12.65 STABILIZATION COMPLETE — READY FOR 12.66
```

If complete, the next arc is:

```text
12.66.x — Source Provider / Windows Helper Ingestion Completion
```

No 12.66 implementation begins within this milestone.
