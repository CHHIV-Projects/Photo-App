# Milestone 12.65.5 — Controlled Mounted NAS End-to-End Intake Validation

## Required Files

Prompt:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.5_controlled_mounted_nas_end_to_end_intake_validation_prompt.md`

Closeout:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.5_controlled_mounted_nas_end_to_end_intake_validation_closeout.md`

Create exactly one closeout.

Do not create separate reports, transcripts, implementation notes, or coder-response files.

---

## Reasoning Level

High.

This is a controlled live ingestion milestone with Vault and provenance consequences.

Do not broaden beyond the specifically approved NAS Source.

---

## Milestone Mode

Controlled live end-to-end validation.

This milestone intentionally crosses the real:

```text
Run Ingestion
→ dispatch
→ Source Intake
→ IngestionRun
→ Vault / Asset
→ provenance
```

boundary for the first time on the clean Linux Mounted Source architecture.

Repository implementation changes are NOT expected.

---

# 1. Goal

Prove that the accepted Mounted NAS architecture feeds the existing ingestion system correctly in the live Development environment.

Use exactly:

```text
Profile ID: 2
Endpoint ID: 1
Source label: 12.65.4 NAS Sample Media
endpoint-relative root: development/sample-media
```

Expected persisted host-side Source root:

```text
/mnt/photo-organizer-sources/nas/photo-organizer/development/sample-media
```

Expected runtime container root:

```text
/app/sources/nas/photo-organizer/development/sample-media
```

The desired evidence chain is:

```text
Profile 2
→ readiness
→ Source Selection
→ dispatch revalidation
→ Source Intake
→ IngestionRun
→ Asset/Vault
→ provenance
→ repeat/idempotency
```

Do not validate Mounted Local in this milestone.

---

# 2. Authoritative State

Repository:

`/home/chuck/projects/photo-organizer-dev`

Required branch:

`integration/linux-runtime-clean`

12.65.3 established the clean live host provider.

12.65.4 established the reconstructed Development backend/frontend and modern NAS Profile `2`.

Frozen historical deployment branch remains reference-only at:

`453032b3eddaa677094ef0eaadfa3711ff536989`

Do not modify or switch to the frozen branch.

---

# 3. Required Reading

Read:

1. `docs/context/coding_agent_rules_v7.md`

2. this prompt

3. `12.64.1_source_location_provenance_vault_hardening_closeout.md`

4. `12.65.1_mounted_source_provider_clean_reconstruction_closeout.md`

5. `12.65.3_host_reconciliation_and_provider_activation_closeout.md`

6. `12.65.4_frontend_validation_and_development_activation_closeout.md`

7. `docs/server_deployment/Photo_Organizer_Mounted_Source_Access_Guide.md`

Do not repeat architecture reconnaissance.

---

# 4. Git Preflight

Run:

```bash
cd /home/chuck/projects/photo-organizer-dev

git branch --show-current
git status --short
git rev-parse HEAD
git rev-parse '@{upstream}'
git rev-parse feature/deployment-linux-runtime
git rev-parse origin/feature/deployment-linux-runtime
```

Required:

```text
branch = integration/linux-runtime-clean
working tree clean except committed prompt/addenda
HEAD = upstream

frozen refs =
453032b3eddaa677094ef0eaadfa3711ff536989
```

Stop if preflight fails.

Do not commit or push.

---

# 5. Locked Source

Use only Profile `2`.

Before any intake confirm through the normal application/API boundary:

```text
Profile ID = 2
Endpoint ID = 1
Profile active
Endpoint type = NAS
provider = linux_stable_mount_v1
endpoint-relative root = development/sample-media
```

Confirm the Profile still passes readiness and Source Selection.

Expected Source Selection:

```text
selected Profile ID = 2
durable identity = verified
identity match = matched
Runtime Root =
/app/sources/nas/photo-organizer/development/sample-media
```

Persisted Profile root must remain the host-side root.

If any identity/root differs from the accepted 12.65.4 evidence, stop.

Do not repair or rewrite the Profile.

---

# 6. Locked 12.64 Contract

This milestone must prove, not redefine:

```text
SourceIntakeRun.ingestion_source_id
= Profile 2

IngestionRun.ingestion_source_id
= Profile 2

Provenance.ingestion_source_id
= Profile 2
```

and:

```text
IngestionRun.from_path
= /app/sources/nas/photo-organizer/development/sample-media

Provenance.source_root_path
= /app/sources/nas/photo-organizer/development/sample-media

Provenance.source_relative_path
= relative to that Runtime Root
```

Also preserve:

- persisted Profile root remains host-side;

- Asset identity remains SHA-256 content identity;

- one content object maps to one canonical Asset/Vault object;

- exact-known content does not overwrite or touch the canonical Vault object;

- exact-known content under another extension does not create an orphan Vault object;

- damaged/missing/conflicting canonical Vault fails closed;

- same-Source repeat is idempotent;

- `Asset.original_source_path` remains first-creation history;

- Source filesystem remains read-only.

No schema/migration/backfill.

---

# 7. No Repository Implementation Changes by Default

This is a validation milestone.

Do not modify:

```text
ingestion
Vault
provenance
Source Intake
dispatch
Source identity
Mounted provider
frontend
Compose
systemd
schema
tests
```

merely to make the live run pass.

If live evidence demonstrates a genuine implementation defect, stop and escalate.

Do not patch and rerun ingestion in the same milestone without explicit Product Owner approval.

---

# 8. Pre-Intake Runtime Gate

Confirm:

```text
backend healthy
frontend healthy
PostgreSQL healthy
Redis healthy

Source namespace service healthy/enabled
broker service healthy/enabled
broker socket healthy
one namespace root
one NAS slot
```

Confirm backend still has:

```text
read-only /app/sources
read-only broker bind
approved supplemental GIDs
non-privileged runtime
```

Do not modify runtime to satisfy this gate.

---

# 9. Exact-Root Read-Only Inventory

Read-only inventory of exactly:

```text
development/sample-media
```

is authorized for this milestone.

Do not inspect sibling NAS directories.

Determine only what is necessary to bound intake:

- regular-file count;

- relative paths;

- file sizes;

- total bytes;

- extensions/media types;

- symlink presence;

- SHA-256 for candidate regular files;

- safe timestamp/size evidence needed to prove Source immutability.

Do not print or reproduce private media contents.

Do not open media for visual review unless needed to classify format.

Do not write metadata or files.

### Safety ceiling

Proceed only if the approved root contains:

```text
25 or fewer regular candidate files
AND
1 GiB or less total candidate data
```

If either ceiling is exceeded:

```text
STATUS: ESCALATION REQUIRED
```

Do not choose a subset ad hoc and do not ingest.

If the root contains zero ingestible media candidates, stop and report.

If symlinks or identity ambiguity create unsafe containment, stop.

---

# 10. Pre-Run Source Immutability Baseline

For every bounded candidate record safe evidence sufficient to prove the Source was not modified:

```text
relative path
size
SHA-256
mtime where useful
```

Use no Source writes.

The final gate must compare the same evidence after intake and repeat.

---

# 11. Pre-Run Database/Vault Baseline

Bounded read-only application/database inspection is authorized for only:

- Profile `2`;

- Endpoint `1`;

- candidate SHA-256 values;

- existing Assets matching those SHA values;

- matching existing provenance;

- matching canonical Vault paths;

- Profile 2 Source Intake/IngestionRun counts.

Do not perform manual SQL mutation.

For each candidate classify its pre-run state as:

```text
NEW CONTENT
EXACT-KNOWN CONTENT
```

where supported by existing Asset SHA evidence.

Record the canonical Vault path/hash/size/timestamps for any exact-known Asset before intake.

Do not modify the Vault.

If a known Asset's canonical Vault object is already missing/damaged/inconsistent before the run, stop and report rather than using it as a normal validation candidate.

---

# 12. Gate A — Final Pre-Dispatch Readiness

Immediately before Run Ingestion:

1. re-run readiness for Profile `2`;

2. re-run Source Selection;

3. confirm selected Profile ID `2`;

4. confirm identity verified/matched;

5. confirm exact expected Runtime Root;

6. confirm persisted host root unchanged;

7. confirm no Source Intake/IngestionRun has yet been created for this milestone.

If any result changes, stop before dispatch.

---

# 13. First Controlled Run Ingestion

Authorize exactly **one initial real Run Ingestion dispatch** for Profile `2`.

Use the normal production Development API/UI path.

Do not call internal ingestion functions manually merely to bypass normal dispatch.

Do not change candidate selection semantics.

Do not modify the Source.

Record:

- request/result;

- dispatch identity;

- Source Intake run ID;

- IngestionRun ID;

- start/end status;

- candidate counts;

- imported/new counts;

- exact-known/reused counts if reported;

- skipped counts;

- failure counts.

Wait for the run to reach its normal terminal state before continuing.

If the run fails unexpectedly:

- do not immediately rerun;

- preserve evidence;

- do not delete partial DB/Vault state;

- stop and escalate.

---

# 14. Gate B — Profile and Run Identity

After successful intake prove:

```text
SourceIntakeRun.ingestion_source_id = 2
IngestionRun.ingestion_source_id = 2
```

and:

```text
IngestionRun.from_path
= /app/sources/nas/photo-organizer/development/sample-media
```

Confirm:

- no detached path-based Source/Profile was created;

- Endpoint `1` remains authoritative;

- Profile `2` root remains the same host-side persisted root;

- no Source root rewrite occurred.

Failure is a blocker.

Do not repair identity records manually.

---

# 15. Gate C — Provenance

For each successfully represented candidate verify the applicable provenance result.

Required:

```text
Provenance.ingestion_source_id = 2

Provenance.source_root_path
= /app/sources/nas/photo-organizer/development/sample-media

Provenance.source_relative_path
= exact candidate path relative to the Runtime Root
```

Confirm provenance does not use:

```text
/mnt/photo-organizer-sources/...
/mnt/nas/photo-organizer/...
temporary receiving paths
```

as its runtime Source root.

For exact-known cross-Source content, one canonical Asset may have legitimate additional Profile 2 provenance.

For same-Source already-known location behavior, do not manufacture duplicate provenance.

---

# 16. Gate D — Asset and Vault Results

For every NEW candidate:

confirm:

```text
one canonical Asset
one canonical Vault object
Vault hash = Source candidate SHA-256
expected size
```

For every pre-run EXACT-KNOWN candidate:

confirm:

```text
existing Asset reused
existing canonical Vault path unchanged
Vault hash unchanged
Vault size unchanged
Vault timestamps unchanged where the 12.64 contract protects them
no ordinary replacement copy occurred
no alternate-extension orphan hash pathname appeared
```

If there were no exact-known candidates before the run:

- do not manufacture one;

- state that live cross-Source exact-known reuse was not exercised;

- retain the existing 12.64 regression evidence as authority for that case.

Do not copy a file into the Source merely to create an exact-known scenario.

---

# 17. Gate E — Source Filesystem Immutability

Repeat the exact bounded Source inventory after the first run.

Compare:

```text
relative paths
file count
sizes
SHA-256
timestamps used in baseline
```

Required:

```text
no Source file created
no Source file removed
no Source file renamed
no Source content changed
no Source metadata intentionally changed
```

If the Source changed, stop immediately.

---

# 18. Controlled Same-Source Repeat

Only if Gates A-E all pass:

authorize exactly **one second Run Ingestion dispatch** using the same Profile `2` and unchanged Source root.

Immediately before repeat:

- readiness must still be ready;

- selection must still return Profile `2`;

- Runtime Root must remain exact.

The purpose is same-Source repeat/idempotency validation.

Do not change Source contents between runs.

---

# 19. Gate F — Repeat / Idempotency

After the second run confirm:

- no duplicate Asset for the same SHA;

- no duplicate canonical Vault object;

- no alternate Vault pathname;

- no duplicate successful provenance for an unchanged same-Source observation;

- Profile identity remains `2`;

- Runtime Root remains exact;

- repeat reports/skips known content according to existing semantics;

- no Source mutation.

Compare Asset/provenance/Vault counts before and after repeat.

A new IngestionRun/SourceIntakeRun record for the repeat itself is acceptable; duplicate content state is not.

---

# 20. Final Source Immutability Gate

Run the bounded Source comparison one final time.

Required:

```text
pre-first-run Source evidence
=
post-first-run Source evidence
=
post-repeat Source evidence
```

for content/path/size/hash.

Do not modify Source metadata to "restore" evidence.

---

# 21. Final 12.64 Evidence Matrix

The closeout must provide a candidate-level matrix:

```text
relative path
SHA-256 safe prefix
pre-run state: NEW / EXACT-KNOWN
Asset before
Asset after first run
Asset after repeat
Vault path before
Vault path after
Profile 2 provenance after first run
Profile 2 provenance after repeat
Source unchanged
```

Do not expose unnecessary private filenames if they contain sensitive information; safe redaction is allowed while retaining distinguishable evidence.

Also provide run-level matrix:

```text
first run
repeat run

SourceIntakeRun ID
IngestionRun ID
Profile ID
Runtime Root
new count
known/skip count
failure count
terminal status
```

---

# 22. No Cleanup of Successful Intake Evidence

Successful validation state should remain as real Development history.

Do not delete:

- Profile `2`;

- Endpoint `1`;

- IngestionRuns;

- SourceIntakeRuns;

- Assets;

- provenance;

- Vault files.

Do not "roll back" a successful intake by manually deleting database/Vault evidence.

The result is intended Development validation history.

---

# 23. Failure Handling

Because intake creates durable application evidence, rollback is primarily **stop and preserve**, not destructive reversal.

If first intake fails:

```text
do not run second intake
do not delete partial state
do not manually repair Vault/DB
preserve evidence
escalate
```

If repeat fails:

```text
preserve successful first-run evidence
do not run a third attempt
do not delete duplicate/conflict evidence
escalate
```

Do not enter an iterative patch/retry loop.

---

# 24. Mounted Local Remains Deferred

Do not attempt Local:

```text
family
```

again.

Do not search for another Local root.

Do not create a Local Source.

Do not infer Local live correctness from NAS success.

Record:

```text
Mounted Local live validation remains deferred
```

until separately reconciled.

---

# 25. Development / Infrastructure Preservation

Do not:

- rebuild/recreate containers;

- restart PostgreSQL/Redis;

- stop Source services;

- change namespace topology;

- modify protected Source config;

- modify GIDs;

- remount NAS;

- change Compose;

- change systemd;

- remove rollback images;

- remove historical validation image.

If current runtime unexpectedly needs infrastructure mutation to run intake, stop and escalate.

---

# 26. Repository Validation

Expected repository change is only the closeout.

No code/test/doc change is expected.

At end:

```bash
git status --short
git diff --stat
git diff --check
```

The closeout will be untracked.

Do not stage it merely for validation.

Use a non-staging whitespace check for the closeout.

---

# 27. Privileged Command Model

The coder should execute all authorized non-privileged application/Docker/read-only database gates directly.

If an unexpectedly necessary check requires `sudo`:

1. stop before the privileged action;

2. provide the exact bounded read-only command;

3. Product Owner executes it;

4. coder never requests or handles the sudo password.

No privileged mutation is authorized.

---

# 28. Escalation Conditions

Stop and report:

```text
STATUS: ESCALATION REQUIRED
```

if:

- Profile `2` identity/root no longer matches;

- candidate set exceeds the safety ceiling;

- source contains unsafe symlink/containment ambiguity;

- no ingestible candidate exists;

- preexisting canonical Vault state is damaged/conflicting;

- dispatch selects another Profile;

- Runtime Root differs;

- Source Intake/IngestionRun uses another Source ID;

- provenance uses host/NAS/temp path instead of Runtime Root;

- Vault object is unexpectedly overwritten/touched;

- duplicate Asset/Vault/provenance state appears;

- Source changes;

- first run fails;

- repeat fails;

- infrastructure mutation becomes necessary;

- schema/migration/backfill appears necessary;

- code changes appear necessary;

- Test/Production would be affected.

Use:

```text
STATUS: ESCALATION REQUIRED

Gate:
Observed state:
Expected state:
Run IDs:
Candidate(s) affected:
Durable state created:
Source state preserved:
Vault/provenance implication:
Why continuation is unsafe:
Smallest next decision required:
Recommended Product Owner action:
```

---

# 29. Required Closeout

Create exactly:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.5_controlled_mounted_nas_end_to_end_intake_validation_closeout.md`

Required structure:

## 1. Repository State

## 2. Live Runtime Preconditions

## 3. Profile 2 Preflight

## 4. Bounded Candidate Inventory

## 5. Pre-Run Source Immutability Baseline

## 6. Pre-Run Asset/Vault/Provenance State

## 7. Gate A — Final Pre-Dispatch Identity

## 8. First Run Ingestion

## 9. Gate B — Run/Profile Identity

## 10. Gate C — Provenance

## 11. Gate D — Asset/Vault

## 12. Gate E — Source Immutability

## 13. Repeat Run

## 14. Gate F — Repeat / Idempotency

## 15. Final Source Immutability

## 16. Candidate Evidence Matrix

## 17. Run Evidence Matrix

## 18. 12.64 Contract Result

## 19. Exact-Known Live Coverage

State explicitly whether pre-existing exact-known content was present.

## 20. Development/Infrastructure Preservation

## 21. Mounted Local Deferred Status

## 22. Exact Commands and Results

## 23. Deviations

## 24. Known Limitations

## 25. Recommended 12.65.6 Scope

## 26. Git Status

Clearly label evidence:

```text
confirmed live
confirmed application/API
confirmed database
confirmed Vault
confirmed Source filesystem
confirmed repository
requires later stabilization
not exercised
```

---

# 30. Definition of Done

12.65.5 is complete when:

- only Profile `2` is used;

- only `development/sample-media` is inventoried;

- candidate set is within the bounded ceiling;

- Source pre-run hashes/state are recorded;

- pre-run Asset/Vault/provenance state is classified;

- readiness and selection pass immediately before dispatch;

- one real Run Ingestion is executed through the normal Development path;

- SourceIntakeRun uses Profile `2`;

- IngestionRun uses Profile `2`;

- `IngestionRun.from_path` equals the verified container Runtime Root;

- provenance uses Profile `2`;

- provenance root equals that Runtime Root;

- provenance relative paths are correct;

- NEW content produces only canonical Asset/Vault state;

- any pre-existing EXACT-KNOWN content reuses canonical Vault state safely;

- Source remains unchanged;

- exactly one same-Source repeat is performed only after first-run gates pass;

- repeat creates no duplicate content/provenance state;

- Source remains unchanged after repeat;

- no repository implementation change occurs;

- no infrastructure mutation occurs;

- Mounted Local remains deferred;

- Test and Production remain untouched;

- exactly one closeout is created;

- coder does not commit or push.
