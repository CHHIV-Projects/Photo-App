# Milestone 12.65.3 — Host Reconciliation and Non-Root Provider Activation

## Required Files

Prompt:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.3_host_reconciliation_and_provider_activation_prompt.md`

Closeout:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.3_host_reconciliation_and_provider_activation_closeout.md`

Create exactly one closeout.

Do not create separate implementation notes, validation reports, operator transcripts, or coder-response files.

---

## Reasoning Level

High.

This milestone performs the first controlled host mutation after the clean repository reconstruction.

Use the exact 12.65.2 evidence and classifications. Do not repeat host reconnaissance broadly.

---

## Milestone Mode

Controlled implementation and host activation.

Authorized:

- one minimal tracked Compose guidance correction;

- replacement of exactly two installed Source-access scripts;

- bounded stop/start of exactly two Source-access systemd services;

- validation of the existing Source namespace;

- validation of the non-root identity broker and socket;

- enablement of those two Source-access services only after all required gates pass;

- read-only validation of preserved durable state.

Not authorized:

- Development backend/frontend rebuild or recreation;

- frontend builder validation;

- Source Creation/readiness/selection;

- Run Ingestion;

- Source Intake;

- database mutation;

- NAS writes;

- Test mutation;

- Production mutation.

---

# 1. Goal

Reconcile the live Linux host with the accepted 12.65.1 Mounted Source Provider implementation while preserving already-correct durable state.

The required outcome is:

```text
accepted clean tracked scripts
+ preserved durable Access Node/config/groups
+ already-correct namespace topology
+ working non-root broker/socket
+ reboot-persistent Source services
```

without touching the currently healthy Development application runtime.

This milestone is intentionally narrower than Development activation.

---

# 2. Authoritative State

Repository:

`/home/chuck/projects/photo-organizer-dev`

Required branch:

`integration/linux-runtime-clean`

Accepted repository implementation:

Milestone 12.65.1.

Authoritative host-state reconnaissance:

Milestone 12.65.2.

Frozen historical reference:

`453032b3eddaa677094ef0eaadfa3711ff536989`

Do not treat the currently installed historical scripts as architectural authority.

Do not switch to or modify the frozen branch.

---

# 3. Required Reading

Read:

1. `docs/context/coding_agent_rules_v7.md`

2. this prompt

3. `docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.1_mounted_source_provider_clean_reconstruction_closeout.md`

4. `docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.2_linux_host_state_reconciliation_recon_closeout.md`

5. `docs/server_deployment/Photo_Organizer_Mounted_Source_Access_Guide.md`

6. `docs/server_deployment/Photo_Organizer_Development_Operator_Controls.md`

7. `docs/server_deployment/Photo_Organizer_Development_Restart_and_Recovery_Guide.md`

Do not repeat 12.65.0/12.65.1 reconnaissance.

12.65.2 is the live-state authority for this milestone unless preflight proves the state has changed.

---

# 4. Git Preflight

Run:

```bash
cd /home/chuck/projects/photo-organizer-dev

git branch --show-current
git status --short
git log --oneline --decorate -5
git rev-parse HEAD
git rev-parse '@{upstream}'
git rev-parse feature/deployment-linux-runtime
git rev-parse origin/feature/deployment-linux-runtime
```

Required:

```text
branch = integration/linux-runtime-clean
working tree clean except for the committed 12.65.3 prompt or approved addenda
HEAD = upstream

frozen local and remote refs =
453032b3eddaa677094ef0eaadfa3711ff536989
```

Stop if preflight fails.

Do not commit or push.

---

# 5. Locked Product Owner Decisions

## 5.1 Compose guidance correction is approved

Milestone 12.65.2 confirmed that:

- the approved Source/NAS data-read group on this host is `chuck`;

- the protected `SOURCE_ACCESS_DATA_GID` correctly resolves to GID 1000;

- no fixed group named `photo-organizer-source-data` exists;

- the current Compose missing-variable/help text incorrectly names that nonexistent fixed group.

Authorize only the smallest correction to:

`docker/compose.development.yml`

Change only the misleading operator/help/error wording.

The corrected wording must describe an:

```text
approved Source/NAS data-read group
```

or equivalent configurable concept.

Do NOT hard-code `chuck` as an architectural group name.

Do NOT change:

- `SOURCE_ACCESS_DATA_GID`;

- interpolation behavior;

- Compose mounts;

- supplemental groups;

- protected env values;

- permissions architecture.

No broader Compose modification is authorized.

---

## 5.2 No no-intake dispatch seam

Do not add any diagnostic, dry-run, alternate dispatch, or production bypass merely to exercise Run Ingestion without Source Intake.

Production filesystem dispatch intentionally launches Source Intake.

For later pre-intake application validation, accepted evidence will be:

```text
existing isolated dispatch tests
+
live Mounted Create/readiness/selection/Runtime Root validation
```

Real dispatch will be validated in the later controlled Source Intake milestone.

Do not modify dispatch behavior in 12.65.3.

---

# 6. Durable State That Must Be Preserved

12.65.2 classified the following as accepted durable/live state.

Do not regenerate, rewrite, recreate, or normalize them merely because this milestone activates clean scripts.

## Stable Access Node

`/var/lib/photo-organizer-source-access/access-node-id`

Preserve exactly.

Required safe pre/post evidence:

- regular file;

- not symlink;

- correct ownership;

- mode `0600`;

- UUID syntax valid;

- same masked/hash digest before and after.

Never print the raw ID.

Never run an installer path that regenerates it.

---

## Protected broker configuration

`/etc/photo-organizer/source-access.json`

Preserve exactly unless preflight proves the file no longer satisfies the accepted validator.

12.65.2 established:

- parse-valid;

- accepted clean schema;

- exact keys;

- approved data group;

- Local identity match;

- exact NAS contract;

- correct owner/mode.

Do not rewrite this file during normal execution.

Run only sanitized structural validation before and after.

Do not print protected hardware identity or unnecessary raw values.

---

## Users and groups

Preserve:

```text
photo-organizer-source-broker
photo-organizer-source-access
approved Source/NAS data-read group: chuck
```

Known live GIDs from 12.65.2:

```text
socket group = 987
data-read group = 1000
```

Do not create, delete, rename, or modify users/groups.

If these no longer match preflight evidence, stop and escalate rather than repairing them.

---

## Development protected GIDs

Preserve current:

```text
SOURCE_ACCESS_SOCKET_GID
SOURCE_ACCESS_DATA_GID
```

in:

`docker/.env.development`

Do not rewrite the protected file.

Only sanitized verification is allowed.

---

## NAS authority

Preserve the existing authoritative systemd automount/CIFS mount at:

`/mnt/nas/photo-organizer`

Canonical identity:

```text
//192.168.1.171/PhotoOrganizer
```

Do not stop, restart, remount, replace, or include the authoritative NAS mount in rollback.

---

## Existing namespace topology

12.65.2 found:

- exactly one namespace root;

- correct shared propagation;

- correct Local slot;

- exactly one NAS slot;

- no duplicate stacked NAS row;

- correct canonical CIFS identity;

- matching major/minor relationship.

Treat this topology as pre-existing accepted state.

The clean namespace script should validate it idempotently.

It should not recreate, stack, or replace already-correct mounts.

---

# 7. Approved Repository Change

Make exactly one repository implementation correction before host activation:

`docker/compose.development.yml`

Correct only the inaccurate failure/help text referring to:

`photo-organizer-source-data`

Replace it with wording describing the configured/approved Source/NAS data-read group.

Run:

```bash
git diff -- docker/compose.development.yml
git diff --check
```

Confirm the diff is message-only.

If any functional Compose behavior changes, stop and revert that broader change.

Do not commit.

---

# 8. Pre-Mutation Host Gate

Before stopping or replacing anything, revalidate the critical 12.65.2 state.

At minimum confirm:

## A. Access Node

Same masked digest and ownership/mode as 12.65.2.

## B. Protected config

Same clean structural validation result.

## C. Installed script hashes

Confirm currently installed scripts are still the historical versions identified in 12.65.2.

Expected installed historical broker SHA-256:

```text
ed407f466f234c936bb6a59e546b9168af73d6374c4807a722eba9c49767f74a
```

Expected accepted clean broker SHA-256:

```text
137c3098430cc7fbeede6609c1fa9b96373503a2add661382ac8d93e980587eb
```

Expected installed historical namespace SHA-256:

```text
d254f8c90ff1596f2f8809c8f19da9f8d02efc893f5f3f76adfdd14d435266c4
```

Expected accepted clean namespace SHA-256:

```text
0a7b363e978521e2d2c1ededa7a01998d2eefe9214bcea4eab35d0318676c626
```

Stop if current state differs unexpectedly.

## D. Unit definitions

Confirm installed broker and namespace unit files still exactly match tracked accepted unit files.

Do not replace them if they match.

## E. Users/groups/GIDs

Confirm the accepted identities still exist and match.

## F. Namespace topology

Confirm without changing anything:

- one namespace root;

- one exact NAS slot;

- no duplicate rows;

- expected propagation;

- expected canonical NAS relationship.

## G. NAS authority

Observe only.

Do not trigger, stop, or alter the automount.

If active, record current mount-table identity safely.

If inactive, do not activate it merely for validation.

---

# 9. Rollback Copies

Before replacing either installed script, create a bounded root-only rollback copy of exactly:

```text
/usr/local/lib/photo-organizer/source-identity-broker.py
/usr/local/lib/photo-organizer/prepare-source-namespace.sh
```

Use a clearly named milestone-specific backup location under a protected host path appropriate for temporary rollback evidence.

Requirements:

- root-owned;

- not in Git;

- contains only the two replaced files;

- preserve modes/content;

- record hashes;

- no secrets copied unnecessarily;

- no broad `/etc`, `/usr/local`, or system snapshot.

Do not back up/rewrite the Access Node ID or broker config because they are not being changed.

The closeout must name the rollback location and hashes safely.

Do not delete the rollback copies during this milestone.

---

# 10. Controlled Service Stop

Stop only:

```text
photo-organizer-source-identity-broker.service
photo-organizer-source-namespace.service
```

Recommended order:

```text
broker first
namespace second
```

Do not stop:

- Docker;

- Development containers;

- NAS mount/automount;

- PostgreSQL;

- Redis;

- unrelated services.

After broker stop:

- confirm old broker process is gone;

- record runtime-directory/socket state;

- do not manually fabricate the socket.

After namespace unit stop:

- because the unit has no destructive `ExecStop`, confirm the pre-existing namespace root and NAS slot remain unchanged;

- if stop unexpectedly alters the accepted pre-existing mount topology, stop immediately and escalate.

---

# 11. Atomic Script Replacement

Replace only:

```text
/usr/local/lib/photo-organizer/source-identity-broker.py
/usr/local/lib/photo-organizer/prepare-source-namespace.sh
```

with the accepted tracked versions:

```text
scripts/operator/linux/source_identity_broker.py
scripts/operator/linux/prepare_source_namespace.sh
```

Requirements:

- atomic replacement;

- `root:root`;

- mode `0755`;

- no symlink;

- exact clean tracked hash after installation.

Do not run the general installer if it would rewrite:

- Access Node;

- protected config;

- users/groups;

- systemd units;

- unrelated host state.

Prefer direct bounded installation of the two approved files.

No daemon reload is required because the unit bytes already match the accepted tracked units.

If unit bytes unexpectedly differ during preflight, stop before replacing them.

---

# 12. Gate A — Installed File and Durable Config Identity

After script replacement and before service activation, prove:

- installed broker hash equals tracked clean broker hash;

- installed namespace hash equals tracked clean namespace hash;

- ownership/mode correct;

- unit definitions unchanged and still match tracked versions;

- Access Node masked digest unchanged;

- protected broker config still passes clean sanitized structural validation;

- protected GIDs unchanged.

Success:

All expected values match.

Failure:

Restore only the replaced script(s) from rollback copies and stop.

Do not modify identity/config to force the gate green.

---

# 13. Namespace Activation

Start only:

`photo-organizer-source-namespace.service`

The clean namespace script must operate idempotently against the already-correct live topology.

It must not stack a second NAS slot.

It must not unmount or replace the authoritative NAS target.

It must not recreate the existing namespace root unnecessarily.

---

# 14. Gate B — Users, Groups, and Permissions

Confirm after namespace activation:

- broker user unchanged;

- broker group unchanged;

- approved data-read group unchanged;

- expected numeric GIDs unchanged;

- protected config ownership/mode unchanged;

- Access Node ownership/mode unchanged;

- namespace directory/slot ownership still compatible with the accepted contract.

No user/group mutation is allowed to repair a mismatch.

If mismatch appears, stop before broker activation and escalate.

---

# 15. Gate C — Namespace Topology

After the clean namespace service completes, inspect exact live topology.

Required success:

```text
exactly one namespace-root row
exactly one NAS-slot row
no duplicate stacked rows
canonical NAS source
expected major/minor relationship
shared namespace propagation
shared NAS-slot propagation where required
Local slot identity unchanged
authoritative NAS mount untouched
```

Compare against the pre-mutation mount-table evidence.

The expected ideal result is:

```text
clean script accepted the existing topology without materially changing it
```

If the clean invocation creates temporary state and rolls it back safely, report it exactly.

Failure conditions include:

- duplicate slot;

- unexpected new namespace layer;

- wrong CIFS source;

- wrong major/minor;

- unexpected propagation;

- authoritative NAS unmounted/remounted;

- pre-existing accepted row removed.

On failure:

- do not start broker;

- allow only the clean script's invocation-owned rollback;

- never remove pre-existing accepted namespace/NAS rows;

- stop and escalate.

Do not begin iterative repair attempts.

---

# 16. Broker Activation

Only after Gates A-C pass:

Start:

`photo-organizer-source-identity-broker.service`

Do not yet enable it.

Confirm:

- service active;

- process runs as `photo-organizer-source-broker`;

- expected primary/supplemental groups;

- runtime directory correct;

- named broker socket exists;

- socket is a Unix socket;

- socket ownership/group correct;

- socket mode `0660`.

---

# 17. Gate D — Broker Protocol and Denial Boundary

Use the tracked bounded protocol checker or equivalent approved read-only requests.

Validate approved Local/NAS identity requests and fail-closed behavior.

Required success includes:

- approved Local location resolves;

- approved NAS location resolves;

- stable Access Node evidence matches;

- broker is non-root;

- arbitrary location fails;

- malformed/unknown fields fail;

- arbitrary path fails;

- backslash-containing POSIX root fails;

- traversal attempt fails;

- no Source files are modified;

- no mount action occurs;

- no Docker action occurs.

Do not print protected raw identity.

If a check requires Source-byte access beyond metadata/identity, stop and report rather than broadening broker authority.

On Gate D failure:

- stop broker;

- restore historical broker script only if the clean script itself caused the failure;

- preserve Access Node/config;

- do not restart repeatedly while changing unrelated state;

- stop and escalate.

---

# 18. Enablement for Reboot Persistence

Only after Gates A-D all pass:

Enable:

```text
photo-organizer-source-namespace.service
photo-organizer-source-identity-broker.service
```

Do not alter their unit files.

Confirm:

```text
namespace = enabled and successful/active as appropriate
broker = enabled and active/running
```

Re-run a bounded post-enable status check.

Do not reboot as part of this milestone.

Reboot persistence will be proven later or by a separately approved maintenance action.

---

# 19. Final Preservation Check

Before closeout, re-confirm:

- Access Node masked digest unchanged;

- protected config unchanged structurally;

- users/groups unchanged;

- protected Development GIDs unchanged;

- authoritative NAS configuration/mount unchanged;

- one namespace root;

- one NAS slot;

- broker named socket healthy;

- Source services enabled;

- Development containers remain the same pre-12.65.1 images/containers;

- PostgreSQL/Redis untouched.

Do not inspect application data unnecessarily.

---

# 20. Current Development Must Remain Untouched

12.65.2 established that current backend/frontend containers are healthy but predate 12.65.1.

That is acceptable.

Do not:

- rebuild backend;

- rebuild frontend;

- recreate backend;

- recreate frontend;

- restart Development;

- run `docker compose up`;

- run `docker compose down`;

- alter images/tags;

- change live Compose configuration;

- change named volumes;

- alter application storage.

12.65.4 will handle frontend validation and Development activation.

---

# 21. Frontend Validation Deferred

Do not perform the builder-target frontend validation in this milestone.

12.65.2 established the approved future Gate F path:

```text
frontend/Dockerfile
Node 22.23.1
npm ci
lint
build
```

via a separate builder-target Docker build.

That belongs to 12.65.4 with separate Docker build/network authorization.

Do not install host-global Node/npm.

---

# 22. Application Source Validation Deferred

Do not run:

- Mounted Source Create;

- readiness;

- Source Selection;

- Run Ingestion dispatch;

- Source Intake;

- provenance checks;

- Vault checks.

Those are intentionally separated from host activation.

12.65.4 will validate Create/readiness/selection without intake.

12.65.5 will perform the controlled real dispatch/intake proof.

---

# 23. Historical M012 Validation Image

Do not remove:

`photo-organizer-m012-backend-validation:10c37d31b48046fcdb93b0354b256b3b5fe223b5`

during 12.65.3.

Its classification is REMOVE, but cleanup remains separately approval-gated.

No Docker cleanup of any kind is authorized.

---

# 24. Validation

Required validation includes:

## Repository

```bash
git diff --check
```

and exact review of the Compose guidance-only diff.

## Installed scripts

Exact hashes/modes/owners versus tracked clean versions.

## Durable state

Sanitized Access Node/config/GID checks.

## Namespace

Exact mount-table topology before and after clean namespace activation.

## Broker

Systemd/process/socket/protocol/denial checks.

## Services

Active/enabled state after gates pass.

Do not substitute broad runtime testing for these bounded gates.

---

# 25. Mutation Stop Rule

This milestone must not become another repair loop.

If any gate fails:

1. stop the next mutation step;

2. preserve evidence;

3. perform only the already-defined bounded rollback applicable to that exact mutation;

4. do not try alternate mount orders, socket fixes, user/group edits, config rewrites, Docker rebuilds, or repeated service surgery;

5. report escalation.

One bounded retry is allowed only when failure is clearly caused by a transient command/read issue and requires no state/design change.

Do not repeatedly patch and retry.

---

# 26. Escalation Conditions

Stop and report:

`STATUS: ESCALATION REQUIRED`

if:

- Access Node digest changes;

- protected config no longer validates;

- users/groups/GIDs differ from accepted evidence;

- installed unit files unexpectedly differ from tracked clean versions;

- script replacement cannot be performed atomically;

- stopping the namespace service removes accepted pre-existing mounts;

- clean namespace activation stacks duplicate rows;

- authoritative NAS is altered;

- namespace topology becomes ambiguous;

- broker must run as root;

- named socket cannot be created with correct ownership/mode;

- broker requires arbitrary path/mount/Docker/Source-byte authority;

- application/DB mutation becomes necessary;

- Docker Development mutation becomes necessary;

- Test/Production would be affected;

- 12.65.1 repository architecture appears wrong.

Use:

```text
STATUS: ESCALATION REQUIRED

Gate:
Observed state:
Expected state:
Mutation already performed:
Rollback performed:
Durable state preserved:
Why safe continuation is not possible:
Smallest next decision required:
Recommended Product Owner action:
```

---

# 27. Required Closeout

Create exactly:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.3_host_reconciliation_and_provider_activation_closeout.md`

Required structure:

## 1. Repository State

Branch, HEAD, upstream, initial/final Git status.

## 2. Approved Scope

Exact repository/host mutations performed.

## 3. Compose Guidance Correction

Exact message-only change and validation.

## 4. Durable State Preflight

Access Node, protected config, users/groups/GIDs, NAS/namespace safe evidence.

## 5. Rollback Copies

Exact paths and safe hashes.

## 6. Service Stop Evidence

Broker and namespace only.

## 7. Installed Script Replacement

Historical vs clean hashes, ownership/modes.

## 8. Gate A — Installed File/Config Identity

Result and evidence.

## 9. Namespace Activation

Exact action.

## 10. Gate B — Users/Groups/Permissions

Result.

## 11. Gate C — Namespace Topology

Before/after rows, uniqueness, identity, propagation.

## 12. Broker Activation

Process/runtime/socket evidence.

## 13. Gate D — Broker Protocol and Denial Boundary

Approved and denied cases.

## 14. Service Enablement

Final active/enabled states.

## 15. Durable State Final Verification

Explicit confirmation of preserved Access Node/config/GIDs/NAS.

## 16. Development Preservation

Confirm backend/frontend/PostgreSQL/Redis were not rebuilt/recreated/restarted.

## 17. Files Changed

Tracked and host-installed.

## 18. Exact Commands and Results

Bounded execution record.

## 19. Rollback Status

What rollback material remains and whether any rollback was exercised.

## 20. Deviations

Any approved or unavoidable deviations.

## 21. Risks / Remaining Limitations

Only remaining issues.

## 22. Recommended 12.65.4 Scope

Frontend validation + Development activation + no-intake Mounted application validation.

## 23. Git Status

Include:

```bash
git status --short
git diff --stat
git diff --check
```

Do not stage the closeout merely to make Git inspect it.

Clearly label evidence as:

```text
confirmed live
confirmed repository
confirmed by gate
preserved from preflight
requires 12.65.4
requires 12.65.5
not tested
```

---

# 28. Definition of Done

12.65.3 is complete when:

- the Compose guidance wording is corrected without changing behavior;

- durable Access Node state is preserved exactly;

- protected broker config is preserved and still validates;

- users/groups/GIDs remain unchanged;

- the two historical installed scripts are replaced with the accepted clean versions;

- systemd unit files remain unchanged because they already match;

- namespace clean script validates the existing topology without duplicate stacking;

- authoritative NAS remains untouched;

- non-root broker starts successfully;

- named broker socket exists with correct ownership/mode;

- approved broker identity requests succeed;

- denial cases fail closed;

- both Source services are enabled only after Gates A-D pass;

- current Development backend/frontend/PostgreSQL/Redis remain untouched;

- no Source Creation/readiness/selection/dispatch/intake occurred;

- no Test/Production mutation occurred;

- rollback copies remain available;

- exactly one closeout is created;

- coder does not commit or push.
