# Milestone 12.65.6 — Stabilization Regression and Documentation Reconciliation

## Required Files

Prompt:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.6_stabilization_regression_and_documentation_reconciliation_prompt.md`

Closeout:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.6_stabilization_regression_and_documentation_reconciliation_closeout.md`

Create exactly one closeout.

Do not create separate reports, transcripts, or coder-response documents.

---

# 1. Milestone Purpose

This is the **final stabilization acceptance milestone before the clean integration branch is merged to `main`**.

Do not introduce a new architecture.

Do not perform another ingestion experiment.

Do not attempt to solve the deferred Mounted Local validation issue.

The goals are to:

1. run the final bounded regression suite against the reconstructed Linux system;

2. confirm Development remains healthy after the successful 12.65.5 live NAS intake;

3. confirm Mounted NAS readiness/selection still works without another dispatch;

4. confirm iCloud and Windows provider boundaries remain preserved;

5. confirm Source Selector behavior remains coherent;

6. verify Development/Test isolation;

7. reconcile the controlling project documentation with the accepted stabilization outcome;

8. document the controlled-reset / escape-from-loop workflow learned during M012;

9. determine whether the branch is ready for the final merge milestone.

This milestone should end with a clear:

```text
READY FOR 12.65.7
```

or:

```text
NOT READY FOR 12.65.7
```

decision.

---

# 2. Reasoning Level

High for acceptance analysis and documentation reconciliation.

Implementation work should remain minimal.

Repository runtime/code changes are not expected.

---

# 3. Authoritative Current State

Repository:

`/home/chuck/projects/photo-organizer-dev`

Required branch:

`integration/linux-runtime-clean`

Frozen historical deployment branch:

`feature/deployment-linux-runtime`

Frozen reference:

`453032b3eddaa677094ef0eaadfa3711ff536989`

The frozen branch remains historical evidence only.

Do not switch to it, modify it, merge it, or resume M012 implementation from it.

---

# 4. Accepted 12.65 State

Treat the completed 12.65 milestones as authoritative evidence.

### 12.65.1

Accepted Mounted Source Provider reconstruction:

- Linux Mounted Local/NAS provider reconstructed selectively;

- Windows provider preserved;

- fixtures preserved;

- iCloud preserved;

- Source Intake/Vault/provenance/schema boundaries preserved;

- host Observed Path and container Runtime Root remain distinct;

- dispatch preserves selected Source Profile identity.

### 12.65.3

Accepted live host provider state:

- namespace service installed and healthy;

- broker installed and healthy as non-root;

- exact NAS topology healthy;

- services enabled;

- corrected namespace implementation installed;

- no duplicate mount stacking;

- existing mounted NAS root is not metadata-mutated.

### 12.65.4

Accepted Development activation:

- repository-pinned frontend builder passed;

- backend/frontend rebuilt and activated;

- PostgreSQL/Redis preserved;

- Mounted capability live;

- modern NAS Endpoint `1` / Profile `2` created;

- NAS readiness and Source Selection passed;

- persisted host root and container Runtime Root remain distinct;

- Mounted Local live validation remained deferred.

### 12.65.5

Accepted controlled Mounted NAS end-to-end proof:

```text
Profile 2
→ readiness
→ Source Selection
→ production dispatch
→ Source Intake Run 3 / Ingestion Run 3
→ 16 Assets
→ 16 canonical Vault objects
→ 16 Profile-2 provenance rows
```

Repeat:

```text
Source Intake Run 4 / Ingestion Run 4
→ scanned 16
→ known 16
→ selected 0
→ staged 0
→ new 0
→ failed 0
```

Confirmed:

- Profile identity remained `2`;

- Runtime Root remained `/app/sources/nas/photo-organizer/development/sample-media`;

- Source remained byte-for-byte unchanged;

- no duplicate Asset;

- no duplicate Vault object;

- no duplicate same-Source provenance;

- Vault objects remained unchanged during repeat;

- no infrastructure mutation occurred.

Pre-existing exact-known cross-Source behavior was not exercised live in 12.65.5; existing 12.64 automated validation remains authoritative for that case.

---

# 5. Required Reading

Read only what is necessary for this acceptance pass:

1. `docs/context/coding_agent_rules_v7.md`

2. this prompt

3. the 12.65.1 closeout

4. the 12.65.3 closeout

5. the 12.65.4 closeout

6. the 12.65.5 closeout

7. `12.64.1_source_location_provenance_vault_hardening_closeout.md`

8. `docs/server_deployment/Photo_Organizer_Mounted_Source_Access_Guide.md`

9. `Photo_Organizer_Ingestion_Completion_Arc_Deep_Context_Handoff_v1.md`

10. the current project workflow document whose basename is `project_workflow_v4.md`

Use `git ls-files` or another bounded exact-basename lookup if the last two files need their repository paths resolved.

Do not conduct broad architecture reconnaissance.

---

# 6. Git Preflight

Run:

```bash
cd /home/chuck/projects/photo-organizer-dev

git branch --show-current
git status --short
git rev-parse HEAD
git rev-parse '@{upstream}'
git rev-parse feature/deployment-linux-runtime
git rev-parse origin/feature/deployment-linux-runtime
```

Required:

```text
branch = integration/linux-runtime-clean
working tree clean except committed 12.65.6 prompt/addenda
HEAD = upstream

frozen refs =
453032b3eddaa677094ef0eaadfa3711ff536989
```

Stop if this fails.

Do not commit or push.

---

# 7. Scope Boundary

This milestone may:

- run non-mutating repository tests;

- run frontend builder/lint/build validation;

- inspect live Development health;

- call readiness and Source Selection for existing Profile `2`;

- inspect existing run/Profile/Asset/Vault/provenance state read-only;

- inspect Test isolation read-only;

- update the specifically authorized documentation;

- create the 12.65.6 closeout.

This milestone must NOT:

- dispatch another ingestion;

- start another Source Intake run;

- modify Source files;

- create another Source Profile or Endpoint;

- retry Mounted Local;

- search for another Local root;

- modify Vault/Asset/provenance state;

- modify Events;

- modify schema/migrations/backfills;

- rebuild/recreate Development containers merely for validation;

- restart PostgreSQL/Redis;

- change namespace/broker/systemd/mount state;

- mutate Test or Production;

- merge to `main`;

- delete historical evidence;

- modify the frozen deployment branch.

If code/runtime implementation changes appear necessary, stop and escalate.

---

# 8. Gate A — Current Development Health

Confirm the current Development stack remains healthy after 12.65.5.

Check:

```text
backend
frontend
PostgreSQL
Redis
```

Confirm the same Development storage boundaries remain present.

Confirm:

- backend remains non-privileged;

- expected supplemental Source-access groups remain;

- `/app/sources` remains read-only;

- broker runtime bind remains read-only;

- no Docker socket or broad host-root bind exists;

- PostgreSQL/Redis named volumes remain intact.

Confirm Source services:

```text
photo-organizer-source-namespace.service
photo-organizer-source-identity-broker.service
```

remain enabled and healthy.

Use bounded read-only inspection.

Do not restart anything to make the gate pass.

---

# 9. Gate B — Mounted NAS Post-Ingestion Smoke

Do NOT dispatch ingestion.

Using existing Profile `2` only:

1. fetch Profile;

2. run readiness;

3. run Source Selection.

Required:

```text
Profile ID = 2
Endpoint ID = 1
active
endpoint type = NAS
location = linux-nas-photo-organizer
relative root = development/sample-media
readiness = ready
durable identity = verified
identity match = matched
selection = selected
```

Expected Runtime Root:

`/app/sources/nas/photo-organizer/development/sample-media`

Expected persisted host root:

`/mnt/photo-organizer-sources/nas/photo-organizer/development/sample-media`

Confirm these remain distinct and unchanged.

Also confirm read-only that:

- Source Intake Runs `3` and `4` still exist;

- Ingestion Runs `3` and `4` still use Profile `2`;

- candidate Asset count remains `16`;

- Profile `2` candidate provenance remains one row per ingested candidate;

- no additional ingestion run appeared from this milestone.

Do not rehash all Source/Vault content unless needed to investigate an unexpected discrepancy.

12.65.5 remains the authoritative byte-level proof.

---

# 10. Gate C — Locked 12.64 Regression

Run the focused automated regression coverage protecting the 12.64 guarantees.

At minimum include tests covering:

```text
selected Source Profile identity
runtime root propagation
provenance source_root_path
provenance source_relative_path
exact-known SHA reuse
canonical Vault immutability
same-Source repeat/idempotency
cross-Source provenance
damaged/conflicting canonical Vault fail-closed behavior
different-extension exact-known handling
Asset.original_source_path preservation
```

Do not rewrite tests merely to make them pass.

If a regression fails:

- determine whether it is a new failure;

- preserve evidence;

- stop if it represents a 12.64 contract regression.

---

# 11. Gate D — Mounted Provider Regression

Run the focused regression coverage added/preserved through 12.65.1 and 12.65.3.

Include coverage for:

```text
strict POSIX relative roots
backslash rejection
path traversal rejection
opaque Linux locations
Mounted Local/NAS mapping
host Observed Path vs Runtime Root
legacy Mounted Profile adoption/relink blocking
selected Profile dispatch identity
namespace existing-exact topology
namespace absent-slot behavior
wrong/duplicate mount fail-closed behavior
no rebind/stack behavior
authoritative NAS target no metadata mutation
broker allow/deny boundaries
```

No live mount mutation is required.

Use automated tests for destructive/error-path behaviors.

---

# 12. Gate E — iCloud Preservation Regression

The stabilization arc must not regress iCloud while reconstructing Mounted Sources.

Run the existing focused iCloud regression coverage relevant to:

```text
provider separation
exact-selection adapter
batch/limit semantics
logical-item behavior
non-repeat semantics
known-item tracking
cleanup/reporting boundaries
```

Do not perform a new live iCloud acquisition unless separately authorized.

Do not modify iCloud implementation in this milestone.

### Previously known full-suite failure

12.65.1 documented one iCloud-related full-backend failure that was reproduced against the exact pre-implementation baseline and classified as pre-existing.

Use the 12.65.1 closeout to identify that exact failure signature if it appears again.

If the same exact known failure is the only full-suite failure:

- classify it consistently as pre-existing;

- do not fix it in 12.65.6 merely to obtain an all-green count.

If the failure differs materially, or additional iCloud failures appear:

```text
STATUS: ESCALATION REQUIRED
```

---

# 13. Gate F — Windows Provider Preservation

Do not attempt Windows ingestion from Linux.

Do not translate Windows paths into Linux paths.

Use existing automated/static regression coverage to confirm that stabilization did not remove or corrupt the preserved Windows provider architecture.

Confirm:

- Windows Local/External/Removable/Optical implementation remains present where expected;

- Linux Mounted provider does not claim those Windows-connected Source types;

- no artificial Windows-to-Linux path translation was introduced;

- External durable identity safeguards remain preserved;

- future Windows Helper work remains deferred to the 12.66 arc.

No Windows Helper implementation belongs in this milestone.

---

# 14. Gate G — Source Selector / UI Sanity

Use the existing live Development API plus compiled/frontend test evidence.

Do not add Playwright/Cypress.

Confirm:

### Create Source capability

Linux server exposes appropriate Mounted capability for:

```text
Local
NAS
```

Windows-connected:

```text
External
Removable
Optical
```

must not be routed through the Mounted provider.

iCloud remains independent.

### Source Selector

Source Selector derives choices from active saved Profiles rather than raw capability choices.

Existing active modern NAS Profile `2` should appear in the appropriate NAS selector category exactly once.

Do not require creation of additional validation Profiles.

Do not mutate the legacy M005 Profile.

No browser-test framework should be added.

---

# 15. Gate H — Full Backend Regression

Run the full backend test suite from the accepted project-local validation environment.

Record:

```text
total
passed
failed
skipped
duration where available
```

Compare any failure with the specifically known 12.65.1 baseline failure.

Acceptable:

```text
all pass
```

or, only if exactly reproduced:

```text
same single previously classified pre-existing iCloud failure
```

Not acceptable:

- new failure;

- additional failure;

- changed failure behavior;

- Mounted/Source/Vault/provenance regression.

Do not broaden into unrelated repair work.

---

# 16. Gate I — Frontend Validation

Use the repository-pinned frontend builder path established in 12.65.4.

Validate using the existing Dockerfile builder target / pinned Node toolchain.

Required:

```text
npm ci
lint
TypeScript/build validation
optimized production build
```

A no-cache rebuild is not required unless needed to establish trustworthy evidence.

Existing warnings/audit findings already classified outside this stabilization scope do not become implementation work unless they now fail a required command.

Do not update dependencies or lockfiles.

---

# 17. Gate J — Development / Test Isolation

Perform bounded inspection of Development and Test configuration/runtime boundaries.

The goal is to prove stabilization did not accidentally couple them.

Confirm where applicable:

```text
distinct Compose/project identity
distinct PostgreSQL storage
distinct Redis storage
distinct application storage
distinct service/container naming
distinct exposed ports where configured
no accidental shared writable application volume
no Production resource use
```

Do not print protected environment secrets.

### If Test is currently running

Read-only health inspection is authorized.

Do not recreate/restart it.

### If Test is currently stopped

Do not start it merely for this milestone.

Use configuration/runtime evidence and record:

```text
Test live runtime health not exercised because Test was inactive
```

Do not mutate Test.

Any unexpected Development/Test writable-state coupling is a blocker.

---

# 18. Reboot Persistence

Do not reboot the server in this milestone without explicit Product Owner authorization.

Instead record current evidence that:

```text
namespace service = enabled
broker service = enabled
authoritative NAS automount configuration remains present
```

A successful actual post-reboot validation has **not yet been proven** unless supported by existing evidence.

Do not claim otherwise.

Record this transparently in Known Limitations.

A reboot proof may be handled after merge or as a separately authorized operational acceptance step.

---

# 19. Documentation Reconciliation

Documentation changes ARE expected in this milestone.

Keep them bounded.

Do not rewrite the entire documentation set.

## 19.1 Controlling ingestion-completion handoff

Locate the exact tracked file named:

`Photo_Organizer_Ingestion_Completion_Arc_Deep_Context_Handoff_v1.md`

Update it only as necessary to reflect the now-accepted stabilization state.

It should clearly record:

- clean stabilization branch reconstruction succeeded;

- Mounted Source Provider is the accepted Linux direct-filesystem provider;

- modern NAS Profile creation/readiness/selection validated live;

- real Mounted NAS intake validated end-to-end;

- Profile identity / Runtime Root / provenance / Vault guarantees survived;

- same-Source repeat/idempotency validated live;

- Mounted Local live validation remains deferred;

- Windows-connected Source types remain for the future Windows Helper provider;

- iCloud remains an independent provider;

- 12.66 is the next architecture/implementation arc after stabilization/merge.

Do not rewrite historical facts merely for narrative consistency.

Preserve the document's role as the controlling transition document.

---

## 19.2 Mounted Source Access Guide

Update:

`docs/server_deployment/Photo_Organizer_Mounted_Source_Access_Guide.md`

only where needed to reflect the accepted operational state.

Document:

- durable Source identity vs runtime path distinction;

- host Observed/Persisted Source root vs backend Runtime Root;

- current Linux Mounted provider boundary;

- NAS access through the approved namespace/broker;

- no artificial Windows path mapping;

- read-only Source expectations;

- readiness/selection before intake;

- operator-safe troubleshooting boundary;

- fail-closed behavior when identity/mapping is ambiguous.

Do not turn this into a historical M012 debugging transcript.

Do not expose secrets.

---

## 19.3 Project workflow — controlled reset / escape from loop

Locate the tracked file whose basename is:

`project_workflow_v4.md`

Add a concise reusable workflow section describing the stabilization lesson from the M012 mount-topology loop.

Suggested concept/name:

```text
Controlled Reset / Escape From Iterative Repair Loop
```

The workflow should capture:

```text
1. Recognize repeated patch/retry behavior or architectural uncertainty.
2. Stop live mutation.
3. Freeze the current branch/state as historical evidence.
4. Identify the last trusted baseline.
5. Preserve closeouts/logs/evidence from the failed or overgrown path.
6. Classify the delta into KEEP / REVISE / RESTORE / DISCARD.
7. Reconstruct only accepted behavior on a clean integration branch.
8. Revalidate from bounded gates rather than continuing the old patch loop.
9. Preserve safety contracts and data rather than “cleaning up” evidence.
10. Merge only after independent stabilization acceptance.
```

Explain that the technique is appropriate when incremental fixes are increasing uncertainty rather than reducing it.

Do not make this Photo-Organizer-M012-specific; document it as a reusable project workflow technique, while optionally noting M012 as the motivating example.

Do not materially rewrite unrelated workflow sections.

---

# 20. Documentation Conflict Check

After the bounded updates above, inspect only those documents and the directly referenced provider architecture statements necessary to determine whether they still contain a **material contradiction** such as:

```text
Windows paths should be mounted/translated into Linux
Linux Mounted provider should own Windows External/Removable/Optical
old M012 namespace architecture remains the target architecture
persisted Profile root should be replaced with Runtime Root
```

If a directly relevant document contains such a contradiction:

- report the exact file and statement;

- correct it only if the document is clearly part of the active controlling/current documentation set.

If a broad archival/historical document contains old direction:

- do not rewrite history;

- leave it intact or mark it historical only if necessary.

Do not sweep the entire repository for stale wording.

---

# 21. Documentation Boundaries

Expected tracked modifications should be limited to:

1. the controlling ingestion-completion handoff;

2. `Photo_Organizer_Mounted_Source_Access_Guide.md`;

3. `project_workflow_v4.md`;

4. the 12.65.6 closeout.

If another existing document appears to require modification for stabilization correctness:

```text
STATUS: ESCALATION REQUIRED
```

before broadening the documentation write set.

---

# 22. No New Runtime/Data Mutation

During the documentation/regression phase, do not:

```text
dispatch ingestion
run Source Intake
add/remove Source media
create/delete Profiles
create/delete Endpoints
alter Asset records
alter provenance
alter Vault content
alter Events
run cleanup
alter schema
restart containers
restart Source services
change mounts
change Docker volumes
```

Read-only checks are sufficient.

---

# 23. Regression Acceptance Matrix

The closeout must provide a concise matrix covering:

| Area                         | Evidence               | Result    |
| ---------------------------- | ---------------------- | --------- |
| Development health           | live runtime           | PASS/FAIL |
| Mounted NAS readiness        | live API               | PASS/FAIL |
| Mounted NAS selection        | live API               | PASS/FAIL |
| 12.64 provenance/Vault suite | automated              | PASS/FAIL |
| Mounted provider suite       | automated              | PASS/FAIL |
| iCloud regression            | automated              | PASS/FAIL |
| Windows preservation         | automated/static       | PASS/FAIL |
| Source Selector/UI           | builder/API/repository | PASS/FAIL |
| Full backend suite           | automated              | PASS/FAIL |
| Frontend builder             | Docker builder         | PASS/FAIL |
| Development/Test isolation   | runtime/config         | PASS/FAIL |
| Documentation reconciliation | repository             | PASS/FAIL |
| Controlled-reset workflow    | repository             | PASS/FAIL |

For any non-PASS result, state whether it is:

```text
BLOCKER
PRE-EXISTING ACCEPTED
DEFERRED LIMITATION
NOT EXERCISED
```

---

# 24. Known Deferred Items

Do not convert these into 12.65.6 work unless new evidence makes them a blocker:

### Mounted Local

Live validation remains deferred.

Do not retry `family`.

Do not search for another Local root.

### Pre-existing exact-known live NAS case

Not required.

12.64 automated validation remains authoritative.

### Windows Helper

Not part of 12.65.

Begins in 12.66 after stabilization merge.

### Reboot persistence

Do not claim actual reboot proof unless one has occurred.

### Existing frontend warnings/audit findings

Remain separate unless they fail required validation.

---

# 25. Escalation Conditions

Stop and report:

```text
STATUS: ESCALATION REQUIRED
```

if:

- current Development runtime is unhealthy;

- Profile `2` readiness/selection no longer resolves correctly;

- Profile/Runtime Root/provenance state from 12.65.5 has changed unexpectedly;

- a 12.64 protection regression appears;

- Mounted provider tests regress;

- iCloud has a new regression beyond the previously classified baseline;

- Windows architecture was accidentally removed or rerouted through Mounted Linux access;

- Source Selector exposes incorrect provider/source behavior;

- backend full-suite failures exceed the accepted baseline;

- frontend required build/lint validation fails;

- Development and Test share unintended writable state;

- schema/migration/backfill appears necessary;

- another ingestion run appears necessary;

- runtime/infrastructure mutation appears necessary;

- documentation requires a new architectural decision;

- documentation reconciliation would require broad repository rewriting;

- any code fix appears necessary.

Use:

```text
STATUS: ESCALATION REQUIRED

Gate:
Observed state:
Expected state:
Regression implication:
Runtime/data implication:
Documentation implication:
Why continuation is unsafe:
Smallest next decision required:
Recommended Product Owner action:
```

Do not patch-and-retry within this milestone without explicit Product Owner approval.

---

# 26. Repository Validation

Before closeout:

```bash
git status --short
git diff --stat
git diff --check
```

Review every tracked changed file.

Expected tracked write set:

```text
Photo_Organizer_Ingestion_Completion_Arc_Deep_Context_Handoff_v1.md
Photo_Organizer_Mounted_Source_Access_Guide.md
project_workflow_v4.md
12.65.6_stabilization_regression_and_documentation_reconciliation_closeout.md
```

The closeout may initially be untracked.

Do not stage files merely for inspection.

Do not commit or push.

No implementation/source-code files should be modified.

---

# 27. Required Closeout

Create exactly:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.6_stabilization_regression_and_documentation_reconciliation_closeout.md`

Use this structure:

## 1. Repository State

## 2. Development Runtime Health

## 3. Mounted NAS Post-Ingestion Smoke

## 4. 12.64 Protection Regression

## 5. Mounted Provider Regression

## 6. iCloud Preservation Regression

## 7. Windows Provider Preservation

## 8. Source Selector / UI Validation

## 9. Full Backend Regression

## 10. Frontend Builder Validation

## 11. Development / Test Isolation

## 12. Reboot Persistence Status

## 13. Documentation Reconciliation

### Controlling Handoff

### Mounted Source Access Guide

### Project Workflow / Controlled Reset

## 14. Documentation Conflict Check

## 15. Regression Acceptance Matrix

## 16. Known Deferred Limitations

## 17. Exact Commands and Results

## 18. Deviations

## 19. Git Status

## 20. 12.65 Stabilization Acceptance Decision

End with exactly one of:

```text
READY FOR 12.65.7
```

or:

```text
NOT READY FOR 12.65.7
```

and state the evidence supporting that decision.

---

# 28. Definition of Done

12.65.6 is complete when:

- repository preflight passes;

- Development remains healthy;

- Source services remain healthy;

- Profile `2` still passes readiness and Source Selection;

- 12.65.5 durable evidence remains intact;

- focused 12.64 protection tests pass;

- focused Mounted provider tests pass;

- iCloud regression remains preserved;

- Windows provider architecture remains preserved;

- Source Selector behavior remains coherent;

- full backend regression is acceptable against the established baseline;

- frontend pinned-builder validation passes;

- Development/Test isolation is confirmed;

- no new ingestion or Source Intake occurs;

- no runtime/infrastructure mutation occurs;

- no code implementation changes occur;

- controlling handoff is reconciled;

- Mounted Source Access Guide is reconciled;

- controlled-reset / escape-from-loop workflow is documented in the project workflow;

- Mounted Local remains explicitly deferred;

- reboot persistence status is accurately documented without overclaiming;

- exactly one closeout is created;

- coder does not commit or push;

- the closeout makes an explicit readiness decision for 12.65.7.

If all acceptance gates pass, the next milestone is:

```text
12.65.7 — Merge Clean Linux Integration to Main and Post-Merge Validation
```
