# Milestone 12.65.2 — Linux Host-State Reconciliation Reconnaissance

## Required Files

Prompt:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.2_linux_host_state_reconciliation_recon_prompt.md`

Closeout:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.2_linux_host_state_reconciliation_recon_closeout.md`

Create exactly one closeout.

Do not create separate reports, implementation notes, validation notes, or coder-response files.

---

## Reasoning Level

High.

This milestone establishes the exact boundary between the accepted repository reconstruction and the current Linux host state.

---

## Milestone Mode

Reconnaissance-only.

Read-only inspection of:

- tracked repository state;

- installed Linux Source-access artifacts;

- systemd unit definitions/status;

- relevant users/groups;

- protected configuration metadata;

- mount topology;

- broker/socket state;

- Development container configuration/state;

- historical M012 validation artifacts;

- available frontend validation tooling.

No host, runtime, repository implementation, database, NAS, Docker, mount, systemd, Test, or Production mutation is authorized.

---

# 1. Goal

Determine exactly what currently exists on the Linux host after the historical M012 work and compare it with the clean Mounted Source Provider implementation accepted in Milestone 12.65.1.

For every relevant host/runtime artifact, classify the current state as:

```text
KEEP
REVISE
DISABLE
REMOVE
ARCHIVE
ABSENT
```

Then produce the exact bounded reconciliation and activation plan for Milestone 12.65.3.

This milestone must answer:

```text
What is installed now?
What does the clean repository now require?
What already matches?
What differs?
What should remain untouched?
What must later be changed?
In what exact order should those changes occur?
How can we validate the frontend before activation?
```

Do not perform those changes now.

---

# 2. Authoritative Repository and Branch

Repository:

`/home/chuck/projects/photo-organizer-dev`

Required branch:

`integration/linux-runtime-clean`

Trusted pre-M012 Linux baseline:

`461653e`

Frozen historical M012 reference:

`453032b3eddaa677094ef0eaadfa3711ff536989`

12.65.1 is now the accepted clean repository implementation of the Mounted Source Provider.

The frozen deployment branch remains historical/reference-only.

Do not switch to it, modify it, merge it, reset it, rebase it, or use it as the active implementation.

Historical files may be inspected read-only using:

```bash
git show 453032b:<path>
```

when needed to identify an installed historical artifact.

---

# 3. Required Reading

Read:

1. `docs/context/coding_agent_rules_v7.md`

2. this prompt

3. `docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.0_bounded_m012_delta_classification_recon_closeout.md`

4. `docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.1_mounted_source_provider_clean_reconstruction_closeout.md`

5. `docs/server_deployment/Photo_Organizer_Mounted_Source_Access_Guide.md`

6. `docs/server_deployment/Photo_Organizer_Development_Operator_Controls.md`

7. `docs/server_deployment/Photo_Organizer_Development_Restart_and_Recovery_Guide.md`

Read project architecture/context/workflow documents only where directly needed to resolve authority or environment boundaries.

Do not repeat broad repository reconnaissance.

---

# 4. Git Preflight

Run:

```bash
cd /home/chuck/projects/photo-organizer-dev

git branch --show-current
git status --short
git log --oneline --decorate -5
git rev-parse HEAD
git rev-parse '@{upstream}'
git rev-parse feature/deployment-linux-runtime
git rev-parse origin/feature/deployment-linux-runtime
```

Required:

```text
branch = integration/linux-runtime-clean
working tree clean except for the committed 12.65.2 prompt or approved addenda
HEAD = upstream

frozen local and remote deployment refs =
453032b3eddaa677094ef0eaadfa3711ff536989
```

Stop and report if these conditions are not met.

Do not commit or push.

---

# 5. Locked Architecture

The accepted Source-provider architecture remains:

```text
Mounted Source Provider
Windows Source Helper Provider
Cloud Provider
```

This milestone concerns only the Linux host/runtime state needed for the Mounted Source Provider.

The accepted runtime chain is:

```text
allowlisted Linux-mounted location
→ non-root host identity broker
→ linux_stable_mount_v1
→ Access Node / Endpoint / Source Profile
→ readiness / selection / dispatch revalidation
→ backend-derived container Runtime Root
→ unchanged Source Intake
```

Do not redesign that architecture during host inspection.

If current installed state reflects older M012 assumptions, classify it against the accepted architecture rather than treating installed state as authoritative.

---

# 6. Locked 12.64 Guarantees

No host reconciliation may weaken:

```text
SourceIntakeRun.ingestion_source_id
= IngestionRun.ingestion_source_id
= Provenance.ingestion_source_id
= selected Source Profile ID
```

and:

```text
IngestionRun.from_path
= current verified container Runtime Root

Provenance.source_root_path
= same Runtime Root

Provenance.source_relative_path
= relative to that Runtime Root
```

Also preserve:

- persisted Profile root as host-side Source root;

- Source identity distinct from path;

- canonical Vault immutability;

- exact-known reuse behavior;

- fail-closed damaged/missing Vault behavior;

- existing Asset/provenance authority;

- same-Source repeat idempotency;

- cross-Source provenance;

- no schema/migration/backfill.

Host-state findings must not be allowed to redefine these contracts.

---

# 7. Read-Only Host Inspection Rules

Read-only host inspection is authorized.

Examples of acceptable commands include:

```text
stat
ls
find
readlink
realpath
id
getent
systemctl cat
systemctl status
systemctl is-enabled
systemctl is-active
systemctl show
findmnt
mountpoint
lsblk
blkid
docker ps
docker inspect
docker image ls
docker compose ... config
cat/read-only inspection of approved non-secret tracked/installed files
cmp
diff
sha256sum
```

Use `sudo` only when required to **read** protected host state that cannot otherwise be inspected.

Do not use `sudo` for mutation.

Do not print protected secrets, passwords, credentials, raw stable identifiers, private keys, or unnecessary raw hardware identity.

Mask or hash sensitive identity evidence in the closeout.

---

# 8. Explicitly Forbidden Operations

Do NOT:

- install or replace files;

- edit `/etc`;

- edit `/var/lib`;

- edit protected configuration;

- modify users/groups;

- change passwords;

- enable/disable/start/stop/restart/reload systemd services;

- run `daemon-reload`;

- create/delete socket directories;

- remove stale sockets;

- mount/unmount/remount anything;

- change propagation;

- run namespace-preparation scripts in mutation mode;

- rebuild Docker images;

- recreate/restart/stop containers;

- change Compose runtime;

- edit `docker/.env.development`;

- run real Source creation/readiness/selection/dispatch if it mutates state;

- run Source Intake;

- write to NAS;

- query/mutate application databases unless explicitly authorized later;

- mutate Test;

- mutate Production;

- delete/archive images;

- clean historical artifacts;

- install Node/npm;

- change repository implementation;

- stage/commit/push.

If a desired check would mutate state, document it as a required 12.65.3 action instead.

---

# 9. Required Host-State Inventory

Inspect the exact artifacts identified by 12.65.1.

## A. Stable Access Node state

Inspect:

`/var/lib/photo-organizer-source-access/access-node-id`

Determine:

- exists/absent;

- ownership/mode;

- whether content is syntactically plausible;

- whether repository/application evidence appears compatible with preserving it.

Do not print the raw ID.

Record only safe masked/hash evidence.

Treat an existing stable Access Node ID as presumptively durable.

Do not recommend regeneration merely because the clean branch was reconstructed.

---

## B. Protected broker configuration

Inspect:

`/etc/photo-organizer/source-access.json`

Determine:

- exists/absent;

- ownership/mode;

- parse validity;

- configured opaque location IDs;

- Local/NAS topology fields;

- whether actual configuration matches the new tracked schema;

- whether prohibited historical or obsolete fields remain.

Do not print raw filesystem UUID/device/inode values unnecessarily.

Report safe structural comparison.

Compare against:

`scripts/operator/linux/source-access.example.json`

Classify:

```text
KEEP
REVISE
REMOVE
ABSENT
```

---

## C. Installed broker implementation

Inspect:

`/usr/local/lib/photo-organizer/source-identity-broker.py`

Compare read-only against:

`scripts/operator/linux/source_identity_broker.py`

Determine:

- exists;

- file mode/owner;

- exact match/diff;

- historical versus clean version;

- whether replacement will be required.

Do not modify it.

---

## D. Installed namespace implementation

Inspect:

`/usr/local/lib/photo-organizer/prepare-source-namespace.sh`

Compare against:

`scripts/operator/linux/prepare_source_namespace.sh`

Determine:

- exists;

- file mode/owner;

- exact match/diff;

- whether installed script reflects one of the historical intermediate M012 implementations;

- whether clean replacement is required.

Do not execute the installed script in mutation mode.

---

## E. Systemd units

Inspect:

```text
/etc/systemd/system/photo-organizer-source-identity-broker.service
/etc/systemd/system/photo-organizer-source-namespace.service
```

Use read-only:

```bash
systemctl cat
systemctl status
systemctl is-enabled
systemctl is-active
systemctl show
```

Compare installed units against tracked clean units.

Record:

- present/absent;

- enabled/disabled;

- active/inactive/failed;

- exact installed/tracked differences;

- configured user/group;

- ExecStart;

- dependencies/order;

- security settings.

Do not start/stop/enable/disable/reload anything.

---

## F. Users and groups

Inspect read-only:

```text
photo-organizer-source-broker
photo-organizer-source-access
approved Source-data group
```

Determine:

- existence;

- numeric UID/GID where operationally relevant;

- primary/supplemental membership;

- compatibility with tracked systemd/Compose contract.

Do not modify users/groups.

Avoid publishing unrelated host memberships.

---

## G. Broker runtime directory/socket

Inspect:

```text
/run/photo-organizer-source-access
/run/photo-organizer-source-access/broker.sock
```

Determine:

- exists/absent;

- owner/group/mode;

- whether socket is actually a Unix socket;

- whether state agrees with systemd active/inactive status;

- whether stale runtime state appears possible.

Do not connect to the broker if doing so could cause mutation.

A protocol-only read request may be proposed for 12.65.3.

Do not delete a stale socket in this milestone.

---

## H. Source namespace

Inspect:

```text
/mnt/photo-organizer-sources
/mnt/photo-organizer-sources/local/server-photos
/mnt/photo-organizer-sources/nas/photo-organizer
```

Use `findmnt`, `mountpoint`, `stat`, and other read-only evidence.

Determine:

- existence;

- mount status;

- number of exact mount rows;

- source;

- target;

- filesystem type;

- major/minor;

- options;

- read-only/read-write;

- propagation state;

- whether duplicate stacked rows remain;

- whether topology matches clean 12.65.1 invariants.

Do not mount/unmount/remount or change propagation.

---

## I. Authoritative NAS mount

Inspect:

`/mnt/nas/photo-organizer`

Confirm read-only evidence for:

```text
canonical source = //192.168.1.171/PhotoOrganizer
filesystem = CIFS
target = /mnt/nas/photo-organizer
```

Determine mount identity and major/minor relationship relevant to the stable NAS slot.

Do not write files to the NAS.

Do not remount it.

---

## J. Development protected GID configuration

Inspect, without exposing unrelated secrets:

`docker/.env.development`

Determine whether the expected Source-access variables exist and whether their numeric values are compatible with current host groups:

```text
SOURCE_ACCESS_SOCKET_GID
SOURCE_ACCESS_DATA_GID
```

Do not print other environment variables.

Do not modify the file.

Compare structurally against:

`docker/.env.development.example`

---

## K. Development backend runtime

Inspect current Development containers read-only.

Determine:

- current backend image/container identity;

- health;

- supplemental groups;

- namespace bind;

- broker-directory/socket bind;

- bind read-only status;

- propagation configuration where inspectable;

- whether current runtime predates 12.65.1 reconstruction.

Do not recreate, restart, stop, or modify containers.

Do not infer repository compatibility solely from container health.

---

## L. Historical M012 validation images/artifacts

Perform bounded read-only inventory for clearly identified M012 validation images or retained diagnostic artifacts.

Do not broadly clean Docker.

Classify only clearly attributable artifacts:

```text
ARCHIVE
REMOVE later
KEEP temporarily
not found
```

Do not remove anything now.

---

# 10. Tracked-vs-Installed Comparison

For every installed tracked artifact, compare current installed content with the clean repository version.

Preferred evidence:

```text
exact match
hash match
bounded diff
installed historical version
unknown/protected
absent
```

Do not copy installed secrets into the repository.

Do not assume the installed version should win.

The clean 12.65.1 repository implementation is the accepted architecture.

Installed state is evidence to reconcile.

---

# 11. Classification Requirements

For every artifact classify:

## KEEP

Installed state already matches the accepted contract and can remain.

## REVISE

Artifact remains required but must be updated to the clean contract.

State exactly what must later change.

## DISABLE

Artifact may remain installed but should not remain enabled/active in the accepted state.

State why.

## REMOVE

Artifact is obsolete or historical and should be removed in 12.65.3.

Do not remove now.

## ARCHIVE

Historical evidence may be useful but should be kept outside active runtime architecture.

## ABSENT

Expected artifact does not currently exist.

State whether 12.65.3 must create/install it.

---

# 12. Special Attention — Namespace History

Historical M012 experienced duplicate stacked NAS slot rows and propagation-order problems.

The clean accepted topology is:

```text
verify exact authority
→ self-bind namespace if needed
→ make namespace private
→ create one exact NAS slot bind
→ validate one exact slot row
→ make namespace shared
→ validate again
```

Read-only inspection must specifically determine:

- how many namespace-root rows exist;

- how many exact NAS-slot rows exist;

- whether any duplicate stacked rows remain;

- source/target identity;

- propagation state;

- whether current topology appears historical/intermediate versus accepted.

Do not try to repair any discrepancy.

If topology is unexpectedly complex or ambiguous, stop that part of inspection and report exact evidence.

---

# 13. Special Attention — Current Development Runtime

Determine whether the currently running Development environment was built before or after the accepted 12.65.1 repository reconstruction.

The expected answer may be that Development is healthy but still running an older image/configuration.

That is acceptable during reconnaissance.

Do not rebuild merely to make it match.

Produce the exact 12.65.3 rebuild/recreate sequence only after all host prerequisites are reconciled.

---

# 14. Frontend Validation Path

12.65.1 could not run frontend lint/type/build because `node`, `npm`, and `npx` were absent from the host.

Determine the smallest approved path for completing frontend validation before live activation.

Inspect only:

- repository frontend manifest/lockfile;

- documented Node/tooling requirements if present;

- whether an existing supported project/container validation path exists;

- whether Node tooling already exists in a non-global usable location;

- whether the current frontend container/image can support non-mutating validation without altering live runtime.

Do not install Node/npm.

Do not build/recreate containers.

Recommend one bounded approach for 12.65.3 or a separate validation step.

Prefer existing repository-pinned tooling over introducing new host-global dependencies.

If current evidence cannot establish the correct Node version, state that explicitly rather than guessing.

---

# 15. Current Application/Data State

Do not query PostgreSQL, Redis, or application APIs merely to inventory host Source-access infrastructure.

12.65.1 already established Source Selector behavior as a non-regression.

Database inspection is outside this milestone unless a specific host/runtime contradiction cannot be resolved otherwise.

If such a need arises, stop and identify the smallest read-only query required before performing it.

---

# 16. Required 12.65.3 Reconciliation Plan

The closeout must provide an exact ordered mutation plan, but must not execute it.

The plan should address, where applicable:

```text
1. preserve durable identity/state that should survive
2. stop/disable only services requiring replacement
3. reconcile users/groups if necessary
4. atomically replace tracked installed files
5. reconcile protected configuration
6. daemon reload
7. prepare/reconcile namespace
8. start/validate namespace service
9. start/validate non-root broker
10. configure protected Development GIDs
11. complete frontend validation
12. rebuild Development images if required
13. recreate Development containers if required
14. verify container groups/binds/read-only visibility
15. run broker denial/protocol checks
16. run Mounted Source Create/readiness/selection/dispatch without intake
```

This order is illustrative.

The coder must derive and recommend the exact safest order from actual host evidence.

Clearly identify every step requiring Product Owner approval.

Do not include real Source Intake in 12.65.3 unless later explicitly approved.

---

# 17. Rollback Planning

For every proposed 12.65.3 mutation, identify the bounded rollback.

Examples:

```text
installed file replacement
→ preserve prior copy/hash and restore atomically

service enable/start
→ stop/disable only that service if validation fails

namespace preparation
→ rollback only invocation-created namespace/slot state

Development recreation
→ preserve known-good image/config evidence and revert to previous image/config if required
```

Do not execute rollback actions now.

The plan must avoid another open-ended repair loop.

If a proposed change lacks a bounded rollback, flag it before recommending activation.

---

# 18. Validation Gates for 12.65.3

Define explicit gates.

At minimum:

## Gate A — Installed file/config identity

Tracked and installed approved assets agree.

## Gate B — Users/groups/permissions

Non-root broker and Development GIDs are correct.

## Gate C — Namespace topology

Exactly one expected namespace and NAS-slot topology with correct identity and propagation.

## Gate D — Broker/socket

Non-root broker works; arbitrary/unapproved access fails closed.

## Gate E — Development container visibility

Backend has only approved read-only Source namespace/socket access.

## Gate F — Frontend validation

Lint/type/build complete using approved pinned tooling.

## Gate G — Mounted Source application flow

Create/readiness/selection/dispatch work for approved Mounted Local/NAS without Source Intake.

Each gate should have:

```text
required evidence
success condition
failure condition
bounded rollback or stop action
```

Do not create historical Gate A–G diagnostic ceremony; keep these concise and execution-oriented.

---

# 19. Scope of Repository Changes

This milestone should create only its closeout.

Do not change application code, tests, scripts, Compose, systemd assets, documentation, or configuration.

If host reconnaissance discovers a repository defect in 12.65.1, report:

```text
STATUS: ESCALATION REQUIRED
```

Do not fix it inside 12.65.2.

---

# 20. Escalation Conditions

Stop and report:

`STATUS: ESCALATION REQUIRED`

if:

- installed state contradicts the accepted 12.65.1 architecture in a way that cannot be safely classified;

- multiple unexpected Source namespace mount layers make identity ambiguous;

- durable Access Node identity appears corrupted or inconsistent;

- protected configuration cannot be safely interpreted without exposing secrets;

- system users/groups conflict with unrelated services;

- authoritative NAS mount identity differs from the established canonical Source unexpectedly;

- current host state suggests 12.65.1 repository assumptions are incorrect;

- safe activation would require broad mount/privileged-container authority;

- safe reconciliation requires schema/database changes;

- Windows-local Source access appears coupled to current Linux mount infrastructure;

- Test or Production would need mutation;

- a host mutation becomes necessary merely to continue reconnaissance.

Use:

```text
STATUS: ESCALATION REQUIRED

Observed state:
Affected artifact:
Tracked expected state:
Why classification is unsafe:
Architecture / 12.64 implications:
Smallest additional evidence needed:
Recommended Product Owner decision:
```

---

# 21. Required Closeout

Create exactly:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.2_linux_host_state_reconciliation_recon_closeout.md`

Required structure:

## 1. Repository State

Branch, HEAD, upstream, working tree, frozen reference.

## 2. Reconnaissance Scope

Exact read-only host/runtime areas inspected.

## 3. Stable Access Node

Safe evidence and classification.

## 4. Protected Broker Configuration

Structural comparison and classification.

## 5. Installed Broker

Installed-vs-tracked comparison.

## 6. Installed Namespace Script

Installed-vs-tracked comparison.

## 7. Systemd Units

Definition/status comparison and classifications.

## 8. Users and Groups

Required identities/memberships and classification.

## 9. Broker Runtime Directory and Socket

Observed state.

## 10. Source Namespace Topology

Exact read-only mount evidence and classification.

## 11. Authoritative NAS Mount

Canonical identity evidence.

## 12. Development Protected GIDs

Safe structural evidence.

## 13. Current Development Container State

Image/config/groups/binds/health and whether runtime predates reconstruction.

## 14. Historical M012 Runtime Artifacts

Images/logs/etc. and ARCHIVE/REMOVE/KEEP recommendations.

## 15. Frontend Validation Path

Exact recommended way to complete lint/type/build.

## 16. Artifact Classification Matrix

For every material artifact:

```text
Artifact
Current state
Tracked expected state
Classification
Reason
Required 12.65.3 action
Rollback
```

## 17. Accepted Durable State to Preserve

Explicitly identify things 12.65.3 must not regenerate or erase casually.

## 18. Exact 12.65.3 Mutation Plan

Ordered, bounded, approval-aware sequence.

## 19. 12.65.3 Validation Gates

Gate A onward with success/failure criteria.

## 20. Rollback Plan

Bounded rollback for each mutation class.

## 21. Risks and Escalations

Only genuine remaining uncertainties.

## 22. Recommended 12.65.3 Scope

Confirm whether host reconciliation + Development activation remains one safe milestone or should be split.

## 23. Git Status

Include:

```bash
git status --short
git diff --stat
git diff --check
```

Explain that the untracked closeout is not inspected by ordinary `git diff`.

Do not stage it merely for validation.

Clearly distinguish:

```text
confirmed from live read-only host evidence
confirmed from repository comparison
confirmed from historical documentation
inferred
requires later mutation/validation
unknown
```

---

# 22. Definition of Done

12.65.2 is complete when:

- current installed Source-access host state has been inventoried read-only;

- installed artifacts have been compared against the accepted 12.65.1 tracked implementation;

- stable Access Node state has been safely identified without exposing/regenerating it;

- protected configuration has been structurally compared without exposing secrets;

- installed broker and namespace scripts have been compared to tracked clean versions;

- systemd definition/status has been recorded without mutation;

- users/groups/GIDs have been mapped;

- broker runtime/socket state has been identified;

- Source namespace and NAS slot topology have been inspected for exact identity, uniqueness, and propagation;

- canonical NAS authority has been confirmed read-only;

- current Development container image/config/groups/binds have been identified;

- historical M012 artifacts have been classified;

- the frontend validation gap has an exact recommended resolution path;

- every host artifact is classified KEEP / REVISE / DISABLE / REMOVE / ARCHIVE / ABSENT;

- durable state to preserve is explicitly identified;

- the exact ordered 12.65.3 mutation/activation plan is documented;

- every planned mutation has a bounded rollback/stop strategy;

- explicit 12.65.3 validation gates are defined;

- no application/database/NAS/Test/Production mutation occurred;

- no service/mount/container/config/user/group mutation occurred;

- no repository implementation changed;

- exactly one closeout was created;

- coder did not commit or push.
