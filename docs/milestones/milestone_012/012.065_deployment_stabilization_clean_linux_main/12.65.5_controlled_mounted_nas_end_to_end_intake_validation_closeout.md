# Milestone 12.65.5 — Controlled Mounted NAS End-to-End Intake Validation Closeout

Status: **PASS**

This milestone exercised one bounded production-path intake and one repeat against the approved Mounted NAS Profile. The Source remained byte-for-byte unchanged, the first run ingested all 16 eligible candidates, and the repeat classified all 16 as known without creating duplicate Asset, Vault, or provenance state.

## 1. Repository State

**Evidence: confirmed repository.**

- Branch: `integration/linux-runtime-clean`.
- HEAD: `d4e26a7b3d55b74c206a72c98ec44a16c94a3129`.
- Upstream: `d4e26a7b3d55b74c206a72c98ec44a16c94a3129`.
- Frozen deployment references remained unchanged at `453032b3eddaa677094ef0eaadfa3711ff536989` locally and remotely.
- The worktree was clean before execution. No implementation, configuration, schema, migration, or test file was changed.
- No commit or push was performed.

## 2. Live Runtime Preconditions

**Evidence: confirmed live.**

- Development backend, frontend, PostgreSQL, and Redis were running and healthy.
- Backend identity was `999:999`, with supplementary groups `987` and `1000`; it was unprivileged and had no added capabilities or devices.
- `/app/sources` remained a read-only `rslave` bind and `/run/photo-organizer-source-access` remained read-only.
- The namespace service was enabled and `active (exited)`; the broker was enabled and `active (running)` with zero restarts.
- The broker socket was visible and usable inside the backend's private mount namespace.
- The namespace row, NAS slot row, canonical CIFS source, filesystem type, uniqueness, major/minor `0:48`, and shared propagation were correct.
- Existing rollback image tags and the repository-pinned frontend validation image remained present.

## 3. Profile 2 Preflight

**Evidence: confirmed application/API.**

- Source Profile `2` was active and used Endpoint `1`.
- Label: `12.65.4 NAS Sample Media`.
- Endpoint type: `nas`.
- Location ID: `linux-nas-photo-organizer`.
- Relative root: `development/sample-media`.
- Host root: `/mnt/photo-organizer-sources/nas/photo-organizer/development/sample-media`.
- Runtime Root: `/app/sources/nas/photo-organizer/development/sample-media`.
- Readiness was ready, verified, identity-matched, and runnable with no blockers.
- Source Selection selected Profile `2` and Endpoint `1`, with provider `linux_stable_mount_v1` and the exact Runtime Root above.
- No Profile, Endpoint, root, or selection mutation was required.

## 4. Bounded Candidate Inventory

**Evidence: confirmed Source filesystem.**

- Inspection was limited to the single approved root; no sibling search or alternate-root discovery was performed.
- Regular files: `16` of the authorized maximum `25`.
- Total bytes: `3,091,122` of the authorized maximum `1 GiB`.
- Symlinks: `0`.
- Special files: `0`.
- Extensions: 16 `.jpg` files, all included in the live allowlist.
- Every file exceeded the live minimum size of `51,200` bytes.
- Predictably rejected regular files: `0`.
- Eligible candidates: `16`.

## 5. Pre-Run Source Immutability Baseline

**Evidence: confirmed Source filesystem.**

- The bounded manifest covered relative-path identity, file type, size, nanosecond mtime, and SHA-256 for every candidate.
- Manifest digest: `577e42e5fdb56a2bf4a57a16747306adf0d6b4f2e80a7e4d63845c5d3bea7163`.
- Root device/inode evidence: device `48`, inode `281`.
- No write, marker file, permission change, ownership change, creation, deletion, or rename was performed against the Source.

## 6. Pre-Run Asset/Vault/Provenance State

**Evidence: confirmed database and confirmed Vault.**

- All 16 candidates classified as `NEW CONTENT`.
- Matching Assets: `0`.
- Matching provenance rows: `0`.
- Profile `2` provenance rows: `0`.
- Candidate canonical Vault conflicts: `0`.
- Profile `2` Source Intake runs: `0`; Ingestion runs: `0`.
- Active Source Intake or Ingestion runs: `0`.
- Pre-existing exact-known candidates: `0`.
- Pre-run event state: 2 Events, 3 Assets, 2 event-assigned Assets, 1 eligible non-user-modified unassigned Asset, and 0 excluded user-modified unassigned Assets.
- Schema inspection found all required tables, columns, indexes, and constraints. No DDL, migration, synchronization mutation, or backfill was needed.
- Schema fingerprint: `39a9af0f1c171bec8837b6e883adae93b378b325019a1834dd8fec38c8eb9ae8`.

## 7. Gate A — Final Pre-Dispatch Identity

**Evidence: confirmed application/API and confirmed live. Result: PASS.**

Immediately before dispatch, readiness and Source Selection again resolved to active Profile `2`, Endpoint `1`, the expected host root, and Runtime Root `/app/sources/nas/photo-organizer/development/sample-media`. The dispatch used the returned selection context, normal batch size `500`, and no special intake limit.

## 8. First Run Ingestion

**Evidence: confirmed application/API.**

- Dispatch endpoint: `/api/admin/run-ingestion/dispatch`.
- Dispatch action: `source_intake_started` using `source_profile_id=2`.
- Source Intake Run: `3`.
- Ingestion Run: `3`.
- Terminal state: `completed`.
- Started: `2026-08-11 00:26:07.828106+00`.
- Finished: `2026-08-11 00:26:08.318441+00`.
- Elapsed: `0.4859241540543735` seconds.
- Counts: scanned `16`, skipped known `0`, selected `16`, staged `16`, processed new unique `16`, failed `0`, remaining `0`.
- Durable report: `/app/storage/logs/source_intake_reports/source_intake_3.json`.

## 9. Gate B — Run/Profile Identity

**Evidence: confirmed database. Result: PASS.**

Source Intake Run `3` and Ingestion Run `3` both referenced Profile `2`; the run root/from-path was exactly `/app/sources/nas/photo-organizer/development/sample-media`. The Profile remained active, attached to Endpoint `1`, and retained the exact host root and relative root.

## 10. Gate C — Provenance

**Evidence: confirmed database. Result: PASS.**

Each candidate had exactly one provenance row after the first run. Every row referenced Profile `2`, Ingestion Run `3`, the exact Runtime Root, and its exact runtime source path and relative path. No host path, raw NAS path, temporary staging path, duplicate row, or mismatched Profile/Run identity appeared.

## 11. Gate D — Asset/Vault

**Evidence: confirmed database and confirmed Vault. Result: PASS.**

- Exactly one Asset existed for each of the 16 candidate hashes.
- Each Asset retained the correct hash, size, and runtime original-source path.
- Each candidate resolved to exactly one canonical Vault object under the standard hash-derived Vault path.
- Vault SHA-256 and size matched the Source candidate for all 16 files.
- The protected post-first-run Vault aggregate was `ddd6041464f851a790593ab141b402d94639be6681a2e5639860ec3055dde4c6`; it included canonical path, SHA-256, size, `mtime_ns`, and `ctime_ns`.
- Normal authorized clustering created one Event and assigned the 16 new Assets. It did not assign the one pre-existing eligible unassigned Asset.

## 12. Gate E — Source Immutability

**Evidence: confirmed Source filesystem. Result: PASS.**

After the first run, the file count, total bytes, file types, relative-path identities, sizes, nanosecond mtimes, and SHA-256 values reproduced the pre-run manifest digest exactly: `577e42e5fdb56a2bf4a57a16747306adf0d6b4f2e80a7e4d63845c5d3bea7163`.

## 13. Repeat Run

**Evidence: confirmed application/API.**

- Exactly one repeat was dispatched after Gates B through E passed.
- Source Intake Run: `4`.
- Ingestion Run: `4`.
- Terminal state: `completed`.
- Started: `2026-08-11 00:28:39.441370+00`.
- Finished: `2026-08-11 00:28:39.487304+00`.
- Elapsed: `0.038284189999103546` seconds.
- Counts: scanned `16`, skipped known `16`, selected `0`, staged `0`, processed new unique `0`, failed `0`, remaining `0`.
- Durable report: `/app/storage/logs/source_intake_reports/source_intake_4.json`.
- No third run was dispatched.

## 14. Gate F — Repeat / Idempotency

**Evidence: confirmed application/API, confirmed database, confirmed Vault, and confirmed Source filesystem. Result: PASS.**

- Run `4` used Profile `2` and the exact Runtime Root.
- All 16 candidates were recognized as known.
- Candidate Asset count remained 16, one per candidate.
- Profile `2` provenance remained exactly one row per candidate, all owned by first Ingestion Run `3`; Run `4` created none.
- Vault objects, including path, content, size, `mtime_ns`, and `ctime_ns`, were unchanged; the aggregate remained `ddd6041464f851a790593ab141b402d94639be6681a2e5639860ec3055dde4c6`.
- Event totals and assignments were unchanged from the post-first-run state.
- The Source manifest remained exact.

## 15. Final Source Immutability

**Evidence: confirmed Source filesystem.**

Final inventory remained 16 regular files, `3,091,122` total bytes, no symlinks, and no special files. The final manifest digest remained `577e42e5fdb56a2bf4a57a16747306adf0d6b4f2e80a7e4d63845c5d3bea7163`, equal to both the pre-run and post-first-run digests.

## 16. Candidate Evidence Matrix

**Evidence: confirmed Source filesystem, confirmed database, and confirmed Vault.** Candidate aliases avoid publishing Source filenames; `path_key` is a truncated hash of the relative path.

| Candidate | path_key | Bytes | SHA-256 | Pre-run | First run | Repeat | Provenance | Vault / Source result |
|---|---:|---:|---|---|---|---|---|---|
| C01 | `e689c4cb99f37d39` | 171714 | `0c37f6407426e300a71072e60e39646031eda0e51ad640b655cdc831a4e8b41b` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C02 | `8ada001d4b3290a5` | 129753 | `d3c34cf4a4c0f13c775caba9f08091b204ba9da37bcc219fe72ee5ec7d4022b2` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C03 | `a7fe8078b2637859` | 211857 | `aafc99733955896bdd656561e2dd4a4edfcd9688bf3230a405ad14ba7903ccfa` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C04 | `078bfdaa279fea5d` | 197007 | `88768be957367b215ccd26800525a5c1fdebaf2e39521ffe0de7ad0c8a7787e4` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C05 | `9de68dfb8ac617e6` | 166582 | `857d72e7615cab708346a864cc387c1e07f9a988f9c19fdad494187285b905e8` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C06 | `d599ebfa18bf279a` | 99639 | `0600a66c765a344c85a7e676f508a6cd96084331df3c90399ccf64883102af73` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C07 | `60714f98fa92e589` | 135104 | `4952dec76b93c818966a7373335742377569683a8670235137f21b207acf4184` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C08 | `62be09701718c5f3` | 143962 | `505adb4dc1058c30f90a6cb821c3d2c44eae3deda5d4aa1f78057f665b48199d` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C09 | `d41c960050db8315` | 183909 | `f256ff4de376557d101b774ae49d5723e0997f6ed054f3f25c47bee09987febb` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C10 | `a4c1383312dd9349` | 357618 | `2842b518d04178768ed7e3f5dd8bbb4c77ac40dc5ccef8126cbad77cde7cc15b` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C11 | `16906727e083f5c8` | 306930 | `0ff28509caaa716d74f2e6ad8ed172eefe0c79022b70fad4d607e1e3e3e40c66` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C12 | `d8f40f9ae7a30539` | 246901 | `0f8f3906fcbb49a263265e03445ba8b7f82cfbebb0f2995d45408e89fce4cb02` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C13 | `33438b304f31211c` | 208719 | `9717132114425b69c6f00f250369c8539f7552592c46a89cc43b7fc48358af75` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C14 | `9f414614a11aaba9` | 249866 | `ab51ac6c81702f9e7bc912a0f0610a1112e49f6ffc56b3258e864dd6d57d0757` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C15 | `65b58e47f07f5f43` | 161005 | `8f16fdd50afbe87fa10120f0aeb6986d053d7595bf578fb12cadab89e662b8f9` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |
| C16 | `d27a2b9f786439b3` | 120556 | `0f6b44354b86d33bd57a2f46ff5fa5a77c821a046c35f0ff736c265c95379547` | new | Asset created | unchanged | 1, Run 3 | canonical exact / unchanged |

## 17. Run Evidence Matrix

**Evidence: confirmed application/API and confirmed database.**

| Source Intake Run | Ingestion Run | Profile | Result | Scanned | Known | Selected | Staged | New unique | Failed | Remaining |
|---:|---:|---:|---|---:|---:|---:|---:|---:|---:|---:|
| 3 | 3 | 2 | completed | 16 | 0 | 16 | 16 | 16 | 0 | 0 |
| 4 | 4 | 2 | completed | 16 | 16 | 0 | 0 | 0 | 0 | 0 |

Both rows used `/app/sources/nas/photo-organizer/development/sample-media`. Evidence was pinned to each dispatch response's exact run identity rather than inferred from a global latest-run value.

## 18. 12.64 Contract Result

**Evidence: confirmed live. Result: PASS for the exercised new-content path.**

The mounted-provider contract held end to end: stable Profile identity selected the exact Runtime Root; normal intake created one Asset, one canonical Vault object, and one Profile/Run-bound provenance row per new candidate; the Source was read-only; and the repeat was idempotent. The existing 12.64 automated suite remains the regression authority for broader exact-known and error-path coverage.

## 19. Exact-Known Live Coverage

**Evidence: not exercised.**

No pre-existing exact-known candidate was present. All 16 controlled candidates were new before Run `3`; therefore live preservation behavior for a pre-existing exact-known canonical Vault object was not exercised in this milestone. The repeat did exercise normal known-content recognition after the first run and showed no Vault rewrite or duplicate provenance.

## 20. Development/Infrastructure Preservation

**Evidence: confirmed live and confirmed repository.**

- Backend, frontend, PostgreSQL, and Redis container IDs, image IDs, start timestamps, health, mounts, and named volumes were unchanged.
- PostgreSQL and Redis were not recreated or restarted.
- Namespace and broker services, scripts, units, enablement, active states, socket health, and selected mount rows remained correct.
- No Docker build, image retag, container recreation, volume deletion, host mount operation, service mutation, reboot, or protected-configuration mutation occurred.
- Source Profiles, Endpoints, Access Node identity, users, groups, GIDs, and roots were not mutated.
- Schema fingerprint remained `39a9af0f1c171bec8837b6e883adae93b378b325019a1834dd8fec38c8eb9ae8`.
- Normal authorized application effects were limited to the two run records/reports, 16 Assets, 16 canonical Vault objects, 16 provenance rows, and normal event clustering/assignment for the 16 new Assets.
- No dispatch beyond the authorized first run and single repeat occurred. Source Intake beyond these runs was not invoked.

## 21. Mounted Local Deferred Status

**Evidence: requires later stabilization.**

Mounted Local remained outside this milestone. No Local root was inventoried, selected, dispatched, or modified.

## 22. Exact Commands and Results

**Evidence: confirmed live, confirmed application/API, confirmed database, confirmed Vault, confirmed Source filesystem, and confirmed repository.**

The execution used the following bounded command forms; inline Python was non-persisted and emitted only scoped evidence:

```bash
git status --short --branch
git rev-parse HEAD
git rev-parse '@{upstream}'
git rev-parse feature/deployment-linux-runtime
git rev-parse origin/feature/deployment-linux-runtime
docker compose -f docker-compose.yml -f docker-compose.dev.yml ps
docker inspect --format '<bounded fields>' photo-organizer-backend photo-organizer-frontend photo-organizer-postgres photo-organizer-redis
systemctl show photo-organizer-source-namespace.service photo-organizer-source-identity-broker.service --property=ActiveState,SubState,Result,UnitFileState,NRestarts,MainPID
findmnt --mountpoint /mnt/photo-organizer-sources --output TARGET,SOURCE,FSTYPE,MAJ:MIN,PROPAGATION --noheadings
findmnt --mountpoint /mnt/photo-organizer-sources/nas/photo-organizer --output TARGET,SOURCE,FSTYPE,MAJ:MIN,PROPAGATION --noheadings
docker compose -f docker-compose.yml -f docker-compose.dev.yml exec -T backend python - <<'PY'
# Bounded API, exact-root inventory, read-only ORM/session, and Vault-evidence harness.
PY
```

The harness used the live Profile/readiness/selection APIs, enumerated only the exact approved root, enforced the 25-file/1-GiB ceiling, and performed read-only candidate-scoped database and Vault checks. Database inspection explicitly used a read-only transaction. Results were the gate values and matrices reported above.

The two authorized dispatches used:

```text
POST /api/admin/run-ingestion/dispatch
source_profile_id: 2
filesystem_options.source_intake_limit: null
filesystem_options.ingest_batch_size: 500
filesystem_options.acknowledge: false
selection context: the immediately preceding exact Profile 2 Source Selection result
```

Each response's exact underlying Source Intake Run (`3`, then `4`) was used for status polling and evidence. Both returned terminal `completed`; all gates passed.

## 23. Deviations

**Evidence: confirmed live.**

- The initial authorized inventory found zero candidates and correctly stopped before dispatch. The Product Owner then populated the already-approved root through the external NAS-management process and explicitly resolved the stop. The rerun inventory was limited to that same exact root.
- The first final Source-manifest comparison harness inserted one extra NUL separator after mtime, producing a false digest mismatch before database or Vault validation. A read-only correction to the comparison algorithm reproduced the baseline digest exactly; no application action or dispatch occurred during the correction.
- The first final Vault aggregate comparison ordered rows by path while the baseline ordered them by SHA-256. Candidate-level evidence already passed. Recomputing the read-only aggregate with the baseline SHA ordering reproduced the baseline digest exactly; no Vault or application mutation occurred.
- No code or configuration correction was needed.

## 24. Known Limitations

**Evidence: not exercised.**

- Pre-existing exact-known content, damaged/conflicting canonical Vault state, rejected-file behavior, symlink rejection, and special-file rejection were not exercised live because the approved root contained only 16 eligible new regular files.
- Mounted Local was not exercised.
- The one pre-existing eligible unassigned Asset remained unassigned; this is recorded behavior, not a failure of the candidate-scoped validation.
- Test and Production were intentionally untouched.

## 25. Recommended 12.65.6 Scope

**Evidence: requires later stabilization.**

Use a separately authorized, equally bounded milestone to reconcile and validate the deferred Mounted Local root. If Product Owner priorities instead require deeper NAS proof, use a controlled pre-existing exact-known candidate to validate canonical Vault non-rewrite semantics live, with the same Source/Vault timestamp and digest protections. Do not combine either objective with broad Source discovery, migration, cleanup, or infrastructure repair.

## 26. Git Status

**Evidence: confirmed repository.**

At final validation, the only worktree change is this untracked closeout:

```text
?? docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.5_controlled_mounted_nas_end_to_end_intake_validation_closeout.md
```

Ordinary `git diff`, `git diff --stat`, and `git diff --check` do not inspect an untracked file. The closeout was not staged merely to make those commands see it. A read-only `git diff --no-index --check /dev/null <closeout>` check was used instead; exit status `1` is expected because content differs from `/dev/null`, with no whitespace-error output. No Git write operation, commit, or push was performed.
