# Milestone 12.65.2 — Linux Host-State Reconciliation Reconnaissance Closeout

```text
STATUS: ESCALATION REQUIRED
```

The Linux host state is bounded and safely classifiable. Two repository/runtime
contract issues require Product Owner disposition before Development activation:

1. `docker/compose.development.yml` tells an operator to use the nonexistent
   fixed group `photo-organizer-source-data`, while the accepted installer and
   protected configuration use the approved existing Source/NAS data-read group
   (`chuck` on this host).
2. The production filesystem dispatch path always invokes
   `start_source_intake`; there is no live no-intake dispatch mode. A real
   dispatch therefore cannot be included in a gate that forbids Source Intake.

Neither issue was changed in this reconnaissance-only milestone.

Evidence labels used below:

- **Live:** confirmed from live read-only host/runtime evidence.
- **Repository:** confirmed from the accepted tracked implementation.
- **Historical:** confirmed only from frozen M012 documentation or bytes.
- **Inferred:** a bounded conclusion from multiple identified evidence sources.
- **Later validation:** requires an approved mutation or validation gate.
- **Unknown:** not established by the authorized evidence.

## 1. Repository State

- Repository: `/home/chuck/projects/photo-organizer-dev`.
- Branch: `integration/linux-runtime-clean`.
- HEAD: `e84388b6201c6e4af07787a2c88e2f421497c171`.
- Upstream: `origin/integration/linux-runtime-clean` at the same commit.
- Accepted 12.65.1 implementation commit: `ce96607cc3212ce697b13b0b744b8dac6d2e2bd1`.
- Frozen local and remote M012 references:
  `453032b3eddaa677094ef0eaadfa3711ff536989`.
- Preflight worktree: clean.
- No branch switch, merge, reset, rebase, stage, commit, or push occurred.

## 2. Reconnaissance Scope

Read-only inspection covered:

- the exact Git preflight and required tracked/frozen references;
- installed Source-access scripts, units, metadata, and hashes;
- systemd unit definitions and active/enabled state;
- the bounded Source-access users and groups;
- sanitized protected Access Node and broker-config evidence;
- broker runtime-directory, socket, process, and bounded journal evidence;
- `/proc`/kernel mount-table topology without causing NAS target access;
- generated NAS mount/automount unit configuration;
- only the two protected Development Source-access GID variables;
- narrow Development container/image identity, health, groups, and mounts;
- clearly named M012 Docker artifacts;
- repository-pinned frontend validation tooling.

No NAS target was dereferenced. The active CIFS row was already present in the
first `/proc/self/mountinfo` inspection. No service, mount, namespace, socket,
user/group, Docker object, protected file, database, API, NAS content, Test, or
Production state was mutated. No broker request was made.

## 3. Stable Access Node

**Live, Product Owner-executed sanitized read:**

```text
regular=True
not_symlink=True
owner_ok=True
mode_ok=True
uuid_syntax=True
masked_hash=sha256:…82fdc4b6c40f
```

The check compared ownership with user `photo-organizer-source-broker`, group
`photo-organizer-source-access`, and mode `0600`. The raw ID was not printed or
copied. Frozen M012 evidence also identifies this as durable state to preserve.

Classification: **KEEP**.

12.65.3 must verify the same masked digest before and after reconciliation and
must not run any path that regenerates this file.

## 4. Protected Broker Configuration

**Live metadata:** `/etc/photo-organizer/source-access.json` is a regular,
non-symlink file owned by `root:photo-organizer-source-access`, mode `0640`.

**Live, Product Owner-executed sanitized structural validation:**

```text
parse_valid=True
clean_validator=True
top_keys_exact=True
fixed_paths_ok=True
approved_data_group=True
local_identity_matches=True
nas_contract_matches=True
owner_mode_ok=True
```

This establishes without emitting protected values that:

- the config passes the accepted clean broker validator;
- no extra top-level or location fields remain;
- the stable-ID file and Source namespace paths are exact;
- the approved data-read group is `chuck`;
- the Local slot's configured filesystem type and opaque UUID/device/inode
  identity match its current host directory;
- the NAS location uses the exact fixed host/runtime slots, CIFS type,
  `//192.168.1.171/PhotoOrganizer`, and `/mnt/nas/photo-organizer`.

Classification: **KEEP**.

No config regeneration or rewrite is required. A root-only pre/post structural
validation is required around later file/service work.

## 5. Installed Broker

Installed path:
`/usr/local/lib/photo-organizer/source-identity-broker.py`.

**Live/repository comparison:**

- regular, non-symlink file; `root:root`, mode `0755`;
- installed SHA-256:
  `ed407f466f234c936bb6a59e546b9168af73d6374c4807a722eba9c49767f74a`;
- frozen `453032b` SHA-256: the same;
- accepted clean SHA-256:
  `137c3098430cc7fbeede6609c1fa9b96373503a2add661382ac8d93e980587eb`.

The installed broker is exactly historical. The clean broker's bounded four-line
delta rejects POSIX backslashes rather than translating them to `/`.

Classification: **REVISE**.

Required action: replace only this installed file atomically with the tracked
clean file while the broker is stopped; retain the historical bytes/hash as the
bounded rollback copy.

## 6. Installed Namespace Script

Installed path:
`/usr/local/lib/photo-organizer/prepare-source-namespace.sh`.

**Live/repository comparison:**

- regular, non-symlink file; `root:root`, mode `0755`;
- installed SHA-256:
  `d254f8c90ff1596f2f8809c8f19da9f8d02efc893f5f3f76adfdd14d435266c4`;
- frozen `453032b` SHA-256: the same;
- accepted clean SHA-256:
  `0a7b363e978521e2d2c1ededa7a01998d2eefe9214bcea4eab35d0318676c626`.

The installed script is exactly the 683-line historical version. The accepted
352-line reconstruction removes intermediate diagnostic ceremony while
retaining exact authority, uniqueness, propagation ordering, and
invocation-owned rollback. The bounded tracked delta is 219 insertions and 550
deletions relative to the installed version.

Classification: **REVISE**.

Required action: replace only this script atomically while both Source services
are stopped, then run it only through the approved namespace-service gate.

## 7. Systemd Units

### Broker unit

`/etc/systemd/system/photo-organizer-source-identity-broker.service` is regular,
`root:root`, mode `0644`, and exactly matches the tracked clean unit at SHA-256
`159d9bdf1560ebe31f22ab74d5c4436fe65d827f2c85e2a169541e4a8927c8de`.

**Live state:** loaded, active/running since 2026-08-04 02:49:43 UTC, but
disabled. It runs as `photo-organizer-source-broker` with primary group
`photo-organizer-source-access`, the expected fixed command, strict filesystem
protection, private networking, and only the required read/write paths.

- Unit definition classification: **KEEP**.
- Activation/persistence state classification: **REVISE**.

### Namespace unit

`/etc/systemd/system/photo-organizer-source-namespace.service` is regular,
`root:root`, mode `0644`, and exactly matches the tracked clean unit at SHA-256
`a5f67e9957010844dc41ae9dc030cf5d470f394f743c1eb2a228be74cb3c66e9`.

**Live state:** loaded, active/exited since 2026-08-04 01:42:21 UTC, successful,
but disabled. Its command is the installed historical namespace script.

- Unit definition classification: **KEEP**.
- Activation/persistence state classification: **REVISE**.

The units should not be replaced. After clean scripts pass live gates, both
units should be enabled for intentional reboot persistence. No daemon reload is
needed when their bytes remain unchanged.

## 8. Users and Groups

**Live:**

- user `photo-organizer-source-broker`: UID `996`, home
  `/var/lib/photo-organizer-source-access`, shell `/usr/sbin/nologin`;
- primary group `photo-organizer-source-access`: GID `987`;
- approved supplemental Source/NAS data-read group `chuck`: GID `1000`;
- `chuck` lists only the broker as an added member in the bounded group record;
- frozen M012 closeout explicitly records `chuck` as the approved preserved
  data-read group.

These identities match the protected config, Local-slot group, CIFS GID, systemd
unit, GID helper, and Compose variables. No unrelated membership was published.

Classifications:

- broker user: **KEEP**;
- socket group: **KEEP**;
- approved data-read group: **KEEP**;
- broker primary/supplemental memberships: **KEEP**.

No user/group mutation is required.

## 9. Broker Runtime Directory and Socket

**Live:**

- `/run/photo-organizer-source-access` exists as a non-symlink directory owned
  by `photo-organizer-source-broker:photo-organizer-source-access`, mode `0750`;
- `/run/photo-organizer-source-access/broker.sock` is absent;
- no named `broker.sock` listener appeared in the bounded Unix-socket listing;
- the broker process remains active with the expected non-root command;
- bounded journal evidence shows a clean service start and no failure after
  2026-08-04 02:49:43 UTC.

The active-process/absent-socket combination is inconsistent and may represent
an unlinked or otherwise inaccessible listener. It is not repaired here.

Classifications:

- runtime directory: **KEEP**;
- broker socket: **ABSENT**;
- current broker process state: **REVISE**.

The clean broker restart in 12.65.3 must recreate the socket and prove its type,
owner/group, `0660` mode, reachability, and denial behavior.

## 10. Source Namespace Topology

All evidence came from the already-populated kernel mount tables. No NAS path
was accessed.

### Namespace root

- exactly one row at `/mnt/photo-organizer-sources`;
- self-bind backed by ext4 source `/dev/…/nvme0n1p2`;
- major/minor `259:2`;
- read/write at the host namespace layer;
- shared propagation;
- directory metadata `root:root`, mode `0755`.

Classification: **KEEP**.

### Local slot

- no separate mount row, as expected;
- regular non-symlink directory on the namespace's Local ext4 backing;
- `root:chuck`, mode `0750`;
- sanitized protected comparison confirms current device/inode and filesystem
  identity match the config.

Classification: **KEEP**.

### NAS slot

- exactly one row at
  `/mnt/photo-organizer-sources/nas/photo-organizer`;
- canonical CIFS source `//192.168.1.171/PhotoOrganizer`;
- major/minor `0:48`, identical to the active authoritative CIFS row;
- read/write at the host namespace layer;
- shared propagation;
- no duplicate stacked slot rows.

Classification: **KEEP**.

The accepted container contract, rather than these host mount flags, supplies
read-only application exposure. The clean namespace script must revalidate this
already-correct topology without recreating or stacking it.

## 11. Authoritative NAS Mount

The very first non-dereferencing `/proc/self/mountinfo` check already showed:

- one generated systemd autofs row at `/mnt/nas/photo-organizer`;
- one active CIFS row at the same target;
- source `//192.168.1.171/PhotoOrganizer`;
- filesystem `cifs`;
- major/minor `0:48`;
- shared propagation;
- active generated `.automount` and `.mount` units sourced from `/etc/fstab`.

The generated config uses `_netdev`, `nofail`, and `x-systemd.automount`; only
the credential-file path was visible, never credential contents. No `stat`,
`realpath`, `mountpoint`, directory listing, or file access was performed against
the target.

Classification: **KEEP**.

The authoritative mount is independent durable infrastructure. 12.65.3 must not
remount, replace, stop, or make it part of namespace rollback.

## 12. Development Protected GIDs

`docker/.env.development` is a regular file owned by UID/GID `1000:1000`, mode
`0600`.

Sanitized local parsing established:

- `SOURCE_ACCESS_SOCKET_GID` occurs exactly once, is numeric, and matches live
  GID `987`;
- `SOURCE_ACCESS_DATA_GID` occurs exactly once, is numeric, and matches approved
  GID `1000`;
- no unrelated variable or value was emitted.

Classification: **KEEP**.

No GID-helper run or protected env edit is required.

## 13. Current Development Container State

The four project containers were running and healthy. No application API or
database was queried.

### Backend

- container `photo-organizer-dev-backend-1`;
- image ID prefix `3aff12de14ab`, created 2026-07-29;
- healthy, non-privileged, user `photo-organizer`;
- `GroupAdd=null`;
- only the existing `application_storage` volume at `/app/storage`;
- no `/app/sources` namespace bind;
- no `/run/photo-organizer-source-access` bind.

The image/container predates accepted reconstruction commit `ce96607` from
2026-08-09. Health proves only the old local-storage runtime is healthy.

Classification: **REVISE**.

### Frontend

- container `photo-organizer-dev-frontend-1`;
- image ID prefix `f240ea7b46c7`, created 2026-07-28;
- healthy, user `node`, Development command;
- also predates `ce96607` and therefore does not establish the reconstructed
  Mounted Source UI.

Classification: **REVISE**.

Both containers were created from the permanent Development Compose file plus
the GPU overlay. PostgreSQL and Redis must remain untouched during later
backend/frontend activation.

## 14. Historical M012 Runtime Artifacts

Bounded exact-name Docker inventory found:

- `photo-organizer-m012-backend-validation:10c37d31b48046fcdb93b0354b256b3b5fe223b5`,
  image ID prefix `8afb9d3de7f1`, size 5.62 GB: **REMOVE** after separate
  cleanup approval because the frozen commit preserves reproducible evidence
  and no active container consumes the image;
- M012 frontend validation image: **ABSENT**;
- clearly named M012 validation containers: **ABSENT**;
- clearly named M012 volumes: **ABSENT**;
- clearly named M012 networks: **ABSENT**;
- separate retained diagnostic files in the bounded milestone directory:
  **ABSENT** beyond the expected prompts/closeouts.

No broad Docker or filesystem search was performed, so this does not claim that
unidentified Product Owner terminal history is absent. Nothing was removed or
archived.

## 15. Frontend Validation Path

**Repository:**

- host `node`, `npm`, and `npx` are absent;
- `frontend/Dockerfile` pins `node:22.23.1-bookworm-slim`;
- its dependency stage runs `npm ci` against `package-lock.json`;
- its builder stage runs `npm run lint` and `npm run build`;
- the accepted Product Owner clarification treats this pinned builder path as
  the required lint/type/build validation path.

Recommended Gate F, under separate Docker build/network approval:

```text
docker build --target builder
  --tag photo-organizer-frontend-validation:<approved-commit>
  frontend
```

Use the exact command in one line during execution and retain its bounded build
result. Do not install host-global Node/npm. Do not execute validation inside
or alter the live frontend container. Complete this builder-only validation
before rebuilding/recreating Development.

## 16. Artifact Classification Matrix

| Artifact | Current state | Tracked expected state | Classification | Reason | Required later action | Rollback |
|---|---|---|---|---|---|---|
| Stable Access Node ID | Healthy protected durable UUID | Preserve | **KEEP** | Sanitized metadata/syntax/digest pass | Pre/post digest check only | Stop if digest changes; never regenerate |
| Protected broker config | Clean-validator and identity checks pass | Host-specific clean schema | **KEEP** | Exact structural comparison passes | Read-only pre/post validation | Restore root-only prior copy only if an authorized change occurs |
| Installed broker script | Exact `453032b` bytes | Clean `ce96607` bytes | **REVISE** | Lacks strict backslash rejection | Atomic replacement while stopped | Restore historical bytes/hash |
| Installed namespace script | Exact 683-line historical bytes | Clean 352-line script | **REVISE** | Historical diagnostic implementation | Atomic replacement while stopped | Restore historical bytes; do not undo pre-existing mounts |
| Broker unit definition | Exact tracked bytes | Same | **KEEP** | Hash match | None | None |
| Namespace unit definition | Exact tracked bytes | Same | **KEEP** | Hash match | None | None |
| Broker service state | Active, disabled, socket absent | Validated, active, persistent | **REVISE** | Runtime is inaccessible/inconsistent | Controlled stop/start, validate, then enable | Stop/disable and restore prior script |
| Namespace service state | Active/exited, disabled, ran historical script | Validated clean script, persistent | **REVISE** | Topology is good but clean implementation has not run | Controlled stop/start, validate, then enable | Stop/disable; clean script may remove only invocation-created mounts |
| Broker user/groups | Exact required identities/membership | Same | **KEEP** | Live contract match | None | None |
| Broker runtime directory | Correct owner/group/mode | Same | **KEEP** | Live contract match | Revalidate after restart | systemd owns recreation |
| Broker socket | Missing | Unix socket, broker:access, `0660` | **ABSENT** | Active process has no accessible named socket | Recreate through broker restart | Stop broker; never manually fabricate socket |
| Namespace root | One shared ext4 self-bind | One exact shared namespace | **KEEP** | Topology matches | Clean idempotent validation | Never remove pre-existing root in rollback |
| Local slot | Correct directory/config identity | Fixed Local slot | **KEEP** | Sanitized identity match | Read-only validation | Leave untouched |
| NAS slot | One canonical shared CIFS bind | One exact slot | **KEEP** | No duplicate; authority identity match | Clean idempotent validation | Never remove pre-existing slot in rollback |
| Authoritative NAS | Active canonical CIFS plus automount | Independent canonical authority | **KEEP** | Exact source/type/target/major-minor | Observe only | Never include in rollback |
| Development GIDs | Exact protected numeric matches | Same | **KEEP** | Both keys valid and current | Revalidate before recreation | Preserve file; no edit |
| Current backend container | Healthy old runtime; no groups/binds | Clean code plus two GIDs/read-only binds | **REVISE** | Predates reconstruction | Later bounded rebuild/recreate | Retain old image and pre-reconstruction Compose snapshot |
| Current frontend container | Healthy old runtime | Reconstructed UI | **REVISE** | Predates reconstruction | Builder gate, then later rebuild/recreate | Retain old image and pre-reconstruction Compose snapshot |
| M012 backend validation image | Exact 5.62 GB historical image | Not active architecture | **REMOVE** | Frozen Git preserves reproducible evidence | Exact-tag removal only after approval | Do not remove until evidence accepted |
| M012 frontend validation image | Not found | Not required | **ABSENT** | Bounded inventory | None | None |
| M012 named containers/volumes/networks | Not found | Not required | **ABSENT** | Bounded inventory | None | None |
| Host-global Node/npm | Not found | Not required | **ABSENT** | Dockerfile supplies pinned toolchain | Do not install | None |
| Compose data-GID error guidance | Names nonexistent fixed group | Describe approved configurable data group | **REVISE** | Tracked guidance contradicts accepted contract | Product Owner-authorized repository correction before activation | Revert only that message if correction fails review |

## 17. Accepted Durable State to Preserve

12.65.3 must preserve:

- the stable Access Node ID and masked digest;
- the protected broker config and its host-specific Local identity;
- approved group `chuck` and live numeric GIDs `987`/`1000`;
- the canonical authoritative NAS automount/mount configuration;
- the already-correct namespace root, Local slot, and single NAS slot;
- current PostgreSQL, Redis, named volumes, and application storage;
- current backend/frontend image IDs until activation rollback is closed;
- frozen M012 Git evidence and the exact validation image until separately
  approved cleanup.

No later installer may be allowed to treat reconstruction as authority to
regenerate identity/config or recreate already-correct mounts.

## 18. Exact 12.65.3 Mutation Plan

### Prerequisite Product Owner decision

Before any host mutation, approve either:

1. the minimal tracked Compose message correction from the nonexistent fixed
   group name to the approved existing Source/NAS data-read group; or
2. an explicit temporary acceptance of that inaccurate failure guidance.

No broader repository change is justified.

### Recommended 12.65.3 — host reconciliation only

Every mutation step below requires explicit Product Owner approval in the
12.65.3 prompt.

1. Re-run Git preflight and the sanitized protected-state checks. Stop if the
   Access Node digest, config result, groups, NAS authority, or topology changed.
2. Record installed/tracked hashes, current unit active/enabled states, and the
   exact pre-existing mount rows. Create root-only rollback copies of only the
   two installed scripts in an approved bounded backup location.
3. Stop the broker service. Confirm the old process is gone. Do not manually
   remove/create the absent socket.
4. Stop the namespace oneshot unit. Because it has no `ExecStop`, this must not
   unmount the pre-existing namespace or NAS slot; verify rows remain unchanged.
5. Atomically install the tracked clean broker and namespace scripts with
   `root:root`/`0755`; verify exact tracked hashes. Do not replace the already
   matching units, config, identity, users, or groups.
6. Skip `daemon-reload` because unit bytes are unchanged. If later evidence
   requires a unit replacement, pause for separate approval before replacing
   it and running `daemon-reload`.
7. Start only the namespace service. The clean script must accept the existing
   one-root/one-slot topology without stacking rows. Run Gate C immediately.
8. Start only the broker service. Confirm non-root identity and newly created
   named socket, then run Gate D's bounded protocol/denial checks.
9. Re-run Gates A and B, including the unchanged Access Node digest and config
   structure.
10. Only after Gates A-D pass, enable the namespace and broker units for reboot
    persistence; re-check enabled and active states.
11. Pause for Product Owner evidence review. Do not rebuild or recreate
    Development in this milestone.

### Recommended subsequent Development activation milestone

After 12.65.3 host evidence is accepted:

12. Obtain separate Product Owner approval for Docker build/network activity.
13. Run the separate frontend builder-target Gate F using the pinned Dockerfile;
    do not use the live frontend container.
14. Record and tag the current backend/frontend image IDs for rollback and
    preserve a read-only pre-reconstruction Compose snapshot from `9086094`
    without switching branches.
15. Build only `backend` and `frontend` with the permanent Development Compose
    file, protected env, and GPU overlay. Do not rebuild PostgreSQL or Redis.
16. Recreate only backend with `--no-deps`; wait for health and run Gate E's
    groups/binds checks before touching frontend.
17. Recreate only frontend with `--no-deps`; wait for health. Leave databases,
    named volumes, and application storage unchanged.
18. With separate approval for application database writes, create or exact-
    reuse one bounded Mounted Local and one bounded Mounted NAS profile, then
    run live readiness and Source Selection only. Do not run production
    filesystem dispatch because it starts Source Intake.
19. Resolve the no-intake dispatch validation decision described in sections 19
    and 21 before claiming Gate G dispatch complete.
20. After all evidence is accepted, separately approve exact-tag removal of the
    historical backend validation image. Do not run broad Docker cleanup.

## 19. 12.65.3 Validation Gates

### Gate A — Installed file/config identity

- Evidence: exact clean hashes/modes for both scripts and units; clean protected
  config validation; unchanged masked Access Node digest.
- Success: all match and durable state is unchanged.
- Failure: stop; restore only replaced script bytes and reassess before restart.

### Gate B — Users/groups/permissions

- Evidence: broker UID/primary group, supplemental `chuck`, GIDs `987`/`1000`,
  state/config/runtime permissions.
- Success: exact current contract remains.
- Failure: do not alter identities ad hoc; stop and obtain a new approved plan.

### Gate C — Namespace topology

- Evidence: one namespace-root row, one exact NAS-slot row, no duplicates,
  canonical source, expected `259:2`/`0:48` relationships, shared propagation,
  Local identity match.
- Success: clean namespace unit exits successfully and topology is unchanged or
  reaches the exact accepted state once.
- Failure: stop broker activation. Clean rollback may remove only mounts created
  by that invocation; never unmount the authoritative NAS or any pre-existing
  row.

### Gate D — Broker/socket

- Evidence: active non-root clean broker, real socket with expected ownership
  and `0660`, bounded tracked protocol checker, malformed/unknown location and
  arbitrary/backslash path denial.
- Success: approved Local/NAS evidence resolves and all denial checks fail
  closed without Source access writes.
- Failure: stop broker, restore prior broker bytes if needed, preserve identity
  and config, and escalate.

### Gate E — Development container visibility

- Evidence: narrow container inspection only.
- Success: backend has supplemental GIDs `987` and `1000`, read-only
  `/mnt/photo-organizer-sources -> /app/sources` with `rslave`, read-only broker
  directory bind, no broad `/mnt`, device, host-root, Docker-socket, or privileged
  access.
- Failure: recreate only backend/frontend from preserved old images and the
  pre-reconstruction Compose snapshot; leave data services/volumes untouched.

### Gate F — Frontend validation

- Evidence: successful repository-pinned builder-target image build showing
  `npm ci`, lint, and build/type validation.
- Success: builder target exits zero before live frontend rebuild.
- Failure: keep the current frontend running; do not activate reconstructed UI.

### Gate G — Mounted Source application flow

- Evidence safely available without intake: approved Create/exact-known reuse,
  readiness, selection, selected Profile ID, endpoint identity, host persisted
  root, and derived Runtime Root for bounded Local/NAS profiles.
- Success for the no-intake portion: Local/NAS are ready and selection returns
  the expected verified Runtime Root without any intake run.
- Current dispatch limitation: production filesystem dispatch calls
  `start_source_intake` unconditionally after revalidation. It cannot be invoked
  live while Source Intake remains forbidden.
- Required Product Owner decision: either authorize a separately designed
  no-intake diagnostic harness/test seam, accept existing isolated dispatch tests
  plus live selection as the pre-intake gate, or later authorize one controlled
  real intake milestone.
- Failure/stop: do not invoke production dispatch, do not create an intake run,
  and do not claim live 12.64 provenance evidence.

## 20. Rollback Plan

| Mutation class | Bounded rollback |
|---|---|
| Compose guidance correction | Revert only the reviewed message if required; no host effect |
| Script replacement | Restore exact root-only historical copy and verify prior hash/mode |
| Namespace start | Stop unit; remove only mounts proven created by the failing clean invocation; preserve all pre-existing and authoritative rows |
| Broker start | Stop broker; allow systemd runtime-directory cleanup; restore prior script if replacement caused failure; preserve config/ID |
| Service enablement | Disable only the two Source units and return to recorded active/inactive state after scripts are restored |
| Frontend validation image | Keep live frontend untouched; remove only the exact failed validation tag after approval |
| Backend/frontend build | Retain/tag old image IDs before build; restore tags if new build fails |
| Backend/frontend recreation | Use preserved old images plus pre-reconstruction Compose snapshot; recreate only those two services with `--no-deps` |
| Application profile validation | Use an explicitly named bounded profile and approved cleanup/reuse decision; never delete unrelated Source identity/history |
| Historical image cleanup | Do not remove until evidence acceptance; once removed, frozen Git can rebuild it but the local layer cache is not recoverable |

Rollback must never use broad Docker cleanup, `compose down`, volume deletion,
NAS unmount, namespace-wide cleanup, identity regeneration, database restore, or
Test/Production mutation.

## 21. Risks and Escalations

### Escalation 1 — tracked Compose data-group guidance

```text
STATUS: ESCALATION REQUIRED

Observed state:
  Approved live/configured data-read group is chuck (GID 1000); no
  photo-organizer-source-data group exists.
Affected artifact:
  docker/compose.development.yml missing-variable message for
  SOURCE_ACCESS_DATA_GID.
Tracked expected state:
  Installer/config contract accepts the Product Owner-approved existing
  Source/NAS data-read group.
Why classification is unsafe:
  The current message can direct a future operator to create/use the wrong fixed
  group even though interpolation works while the protected value is present.
Architecture / 12.64 implications:
  No current provenance effect; inaccurate permission guidance can undermine the
  non-root least-privilege contract during recovery.
Smallest additional evidence needed:
  None.
Recommended Product Owner decision:
  Authorize a minimal message-only repository correction before activation.
```

### Escalation 2 — no live dispatch-without-intake path

```text
STATUS: ESCALATION REQUIRED

Observed state:
  RunIngestionDispatchService._dispatch_filesystem calls start_source_intake
  after selection/readiness/runtime-root revalidation.
Affected artifact:
  Gate G requirement to validate live dispatch while forbidding Source Intake.
Tracked expected state:
  Production dispatch is intentionally the intake launch boundary.
Why classification is unsafe:
  Calling it live would violate the no-intake authorization; claiming dispatch
  without calling it would overstate validation.
Architecture / 12.64 implications:
  A real intake is the point where ingestion/provenance guarantees become live
  evidence, so it cannot be simulated implicitly.
Smallest additional evidence needed:
  Product Owner choice among an explicit diagnostic seam, isolated tested
  dispatch evidence plus live selection, or a later controlled intake.
Recommended Product Owner decision:
  Keep 12.65.3 host-only and decide Gate G separately before activation closeout.
```

### Remaining operational risks

- The active historical broker currently has no accessible named socket.
- The accepted clean namespace script has not yet run against this live, though
  currently correct, topology.
- Current Development is healthy but predates all 12.65.1 provider changes.
- No live container Source visibility, broker protocol, Mounted Create,
  readiness, selection, dispatch, or intake has been validated.
- A future reboot would not restore the currently disabled Source units.

No schema/migration/backfill, Windows coupling, Test/Production mutation, broad
mount authority, or protected-config contradiction was found.

## 22. Recommended 12.65.3 Scope

Do **not** combine host reconciliation and Development activation into one
mutation window.

Recommended split:

1. **12.65.3 — Host reconciliation and non-root provider activation:** dispose
   of the narrow Compose-message escalation, replace only the two historical
   scripts, validate/enable namespace and broker, and stop for evidence review.
2. **Subsequent milestone — Frontend validation and Development activation:**
   run the builder-only frontend gate, build/recreate only backend/frontend,
   validate groups/read-only binds, then perform approved no-intake application
   checks.
3. **Later controlled intake milestone, if approved:** prove the full 12.64
   Profile-ID, Runtime Root, Vault, and provenance chain with one bounded Source.

This split preserves the currently healthy Development stack while the absent
socket and clean host scripts are resolved, and gives Docker recreation its own
rollback boundary.

## 23. Git Status

At closeout creation, the expected only worktree entry is this untracked file:

```text
?? docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.2_linux_host_state_reconciliation_recon_closeout.md
```

Ordinary `git diff --stat` and `git diff --check` produce no output because they
do not inspect an untracked file. The closeout was not staged merely to make
those commands see it. A read-only no-index check is used separately to inspect
the untracked file. No commit or push occurred.
