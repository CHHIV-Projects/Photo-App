# Milestone 12.65.4 — Frontend Validation and Development Activation

## Required Files

Prompt:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.4_frontend_validation_and_development_activation_prompt.md`

Closeout:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.4_frontend_validation_and_development_activation_closeout.md`

Create exactly one closeout.

Do not create separate validation reports, implementation notes, transcripts, or coder-response files.

---

## Reasoning Level

High.

The architecture and host foundation are already established.

This milestone should activate the accepted repository implementation in Development without reopening M012 architecture.

---

## Milestone Mode

Controlled Development activation and no-intake application validation.

Authorized:

- repository-pinned frontend builder validation;

- bounded Docker image builds;

- bounded rollback image tags;

- rebuild/recreation of Development backend and frontend only;

- read-only container/runtime validation;

- browser-safe Mounted location discovery;

- bounded Development Source Profile creation/exact reuse where necessary;

- Mounted readiness;

- Source Selection;

- Runtime Root verification;

- Source Selector validation.

Not authorized:

- PostgreSQL recreation/reset;

- Redis recreation/reset;

- volume deletion;

- application-storage deletion;

- Run Ingestion dispatch;

- Source Intake;

- Vault writes;

- provenance creation;

- NAS file writes;

- Source filesystem writes;

- Test mutation;

- Production mutation;

- broad Docker cleanup.

---

# 1. Goal

Activate the accepted 12.65.1/12.65.3 Mounted Source Provider implementation in the Development application while preserving the healthy data/runtime foundation.

Required target:

```text
clean enabled namespace + broker
→ repository-pinned frontend validation
→ rebuilt Development backend/frontend
→ approved read-only Source namespace/socket visibility
→ browser/API Mounted capability
→ Mounted Source Create or exact reuse
→ readiness
→ Source Selection
→ verified backend-derived Runtime Root
```

Stop there.

Do not invoke production filesystem dispatch because it intentionally launches Source Intake.

Real dispatch/intake is reserved for 12.65.5.

---

# 2. Authoritative State

Repository:

`/home/chuck/projects/photo-organizer-dev`

Required branch:

`integration/linux-runtime-clean`

Accepted repository reconstruction:

12.65.1.

Accepted live host reconciliation:

12.65.3.

Frozen historical branch remains:

`feature/deployment-linux-runtime`

at:

`453032b3eddaa677094ef0eaadfa3711ff536989`

Do not switch to, merge, reset, rebase, or modify the frozen branch.

---

# 3. Required Reading

Read:

1. `docs/context/coding_agent_rules_v7.md`

2. this prompt

3. `12.65.1_mounted_source_provider_clean_reconstruction_closeout.md`

4. `12.65.2_linux_host_state_reconciliation_recon_closeout.md`

5. `12.65.3_host_reconciliation_and_provider_activation_closeout.md`

6. `docs/server_deployment/Photo_Organizer_Mounted_Source_Access_Guide.md`

7. `docs/server_deployment/Photo_Organizer_Development_Operator_Controls.md`

8. `docs/server_deployment/Photo_Organizer_Development_Restart_and_Recovery_Guide.md`

9. `frontend/Dockerfile`

10. the permanent Development Compose file and the already-established GPU overlay used by the current Development runtime.

Do not perform broad repository reconnaissance.

---

# 4. Git Preflight

Run:

```bash
cd /home/chuck/projects/photo-organizer-dev

git branch --show-current
git status --short
git log --oneline --decorate -5
git rev-parse HEAD
git rev-parse '@{upstream}'
git rev-parse feature/deployment-linux-runtime
git rev-parse origin/feature/deployment-linux-runtime
```

Required:

```text
branch = integration/linux-runtime-clean
working tree clean except for committed prompt/addenda
HEAD = upstream

frozen local and remote refs =
453032b3eddaa677094ef0eaadfa3711ff536989
```

Stop if preflight fails.

Do not commit or push.

---

# 5. 12.65.3 Host Preconditions

Before Docker mutation, verify bounded live evidence that:

```text
photo-organizer-source-namespace.service
= enabled and successful/active as appropriate

photo-organizer-source-identity-broker.service
= enabled and active/running

broker named socket exists and is healthy

namespace root = exactly one accepted row

NAS slot = exactly one accepted row

authoritative NAS = unchanged
```

Also verify sanitized evidence that:

```text
SOURCE_ACCESS_SOCKET_GID = current broker socket group GID
SOURCE_ACCESS_DATA_GID = current approved Source/NAS data-read GID
```

Do not print unrelated environment variables.

Do not rerun host reconciliation.

If 12.65.3 state has materially changed, stop and escalate rather than repairing it here.

---

# 6. Locked Architecture

Preserve:

```text
Mounted Source Provider
Windows Source Helper Provider — future
Cloud/iCloud Provider
```

Mounted Source currently supports legitimate Linux-mounted:

```text
Local
NAS
```

Do not route Windows External, Removable, or Optical through the Mounted provider.

Do not translate Windows paths to Linux paths.

Do not modify Windows Helper architecture.

iCloud remains unchanged.

---

# 7. Locked Ingestion Boundary

Preserve:

```text
Mounted identity/access
→ readiness
→ Source Selection
→ verified Runtime Root
→ production dispatch
→ Source Intake
```

This milestone stops before production dispatch.

Do not add a no-intake dispatch mode, diagnostic bypass, dry-run dispatch, alternate ingestion seam, or parallel intake mechanism.

Pre-intake dispatch evidence is already supplied by the passing isolated dispatch tests from 12.65.1.

12.65.4 live evidence is:

```text
Create/exact reuse
+ readiness
+ Source Selection
+ verified Runtime Root
```

12.65.5 will prove real dispatch and Source Intake.

---

# 8. Locked 12.64 Guarantees

Do not modify or weaken:

```text
SourceIntakeRun.ingestion_source_id
= IngestionRun.ingestion_source_id
= Provenance.ingestion_source_id
= selected Source Profile ID
```

and:

```text
IngestionRun.from_path
= current verified container Runtime Root

Provenance.source_root_path
= same Runtime Root

Provenance.source_relative_path
= relative to that Runtime Root
```

Also preserve:

- persisted Profile root as host-side Source root;

- Profile root not rewritten by Runtime Root;

- exact-known Vault reuse;

- immutable canonical Vault object;

- safe failure for damaged/missing/conflicting Vault;

- same-Source idempotency;

- cross-Source provenance;

- no schema/migration/backfill.

No ingestion/Vault/provenance code change is authorized.

---

# 9. Current Development Preservation Baseline

12.65.3 confirmed Development remained healthy and untouched.

Before mutation record:

- backend container ID;

- backend image ID;

- frontend container ID;

- frontend image ID;

- PostgreSQL container ID;

- Redis container ID;

- health/status;

- application storage/named-volume identity required for rollback evidence.

Do not inspect application/database contents unnecessarily.

PostgreSQL and Redis must remain the same running containers throughout this milestone.

---

# 10. Rollback Image Preservation

Before building/recreating backend or frontend, preserve explicit rollback references to the currently running backend/frontend image IDs.

Use narrowly named milestone-specific rollback tags derived from the current images.

Example naming concept:

```text
...:rollback-12.65.4-<starting-commit>
```

Derive the exact existing image repositories/tags from the current Development Compose state rather than inventing unrelated image names.

Requirements:

- tag only the current backend and frontend images;

- record original image IDs and rollback tags;

- do not retag PostgreSQL/Redis;

- no broad image cleanup;

- do not remove rollback tags during this milestone.

If current image identity cannot be mapped safely to bounded rollback tags, stop before building.

---

# 11. Gate A — Frontend Builder Validation

12.65.1 could not validate frontend because Node/npm are absent from the host.

Use the repository-pinned builder path established by 12.65.2/12.65.3:

```text
frontend/Dockerfile
Node 22.23.1
npm ci
lint
build
```

Perform a separate builder-target validation build.

Use a milestone/commit-specific validation tag.

The build may use ordinary Docker build network access required to resolve dependencies pinned by the existing package-lock.

Do not:

- install host-global Node/npm;

- modify package.json;

- modify package-lock.json merely to pass validation;

- run validation inside the existing live frontend container;

- recreate the live frontend before this gate passes.

Success:

```text
npm ci succeeds
lint succeeds
build/type validation succeeds
builder target exits zero
```

Failure:

Keep current frontend untouched and stop.

Do not weaken lint/build rules or alter dependencies merely to obtain green validation.

---

# 12. Gate B — Backend/Frontend Image Build

Only after Gate A passes.

Use the existing permanent Development Compose configuration and its established GPU overlay/environment.

Build only the application services required for the accepted current runtime:

```text
backend
frontend
```

Do not rebuild:

```text
postgres
redis
```

Do not run `compose down`.

Do not delete volumes.

Do not recreate services merely as a side effect of building.

Record resulting backend/frontend image IDs.

Success:

- both builds complete;

- resulting images correspond to the current clean Git working tree;

- no PostgreSQL/Redis mutation occurred.

Failure:

keep current containers running on old images and stop.

---

# 13. Backend Activation First

Recreate only the Development backend, with dependencies left running.

Use the existing Development Compose configuration and GPU overlay.

Use `--no-deps` or equivalent bounded service recreation.

Do not restart PostgreSQL or Redis.

Wait for backend health before proceeding.

If backend fails to become healthy:

- preserve logs/evidence;

- recreate only backend using the preserved rollback image/config;

- verify old backend health;

- stop and escalate.

Do not proceed to frontend.

---

# 14. Gate C — Backend Mounted Runtime Visibility

After the reconstructed backend is healthy, inspect narrowly.

Required backend contract:

```text
non-privileged application container
supplemental socket GID present
supplemental Source/NAS data GID present

/mnt/photo-organizer-sources
→ /app/sources
read-only
rslave as approved

/run/photo-organizer-source-access
→ approved container path
read-only

no broad /mnt bind
no host root bind
no Docker socket
no unexpected device exposure
not privileged
```

Validate that the backend can reach the broker socket through the approved bind and obtain browser-safe location capability information.

Do not read Source file contents.

Do not run intake.

Failure:

rollback backend only to the preserved old image/config and stop.

---

# 15. Frontend Activation

Only after backend health and Gate C pass.

Recreate only the frontend using the newly validated/built image.

Use `--no-deps`.

Do not restart backend, PostgreSQL, or Redis merely to recreate frontend.

Wait for frontend health.

On failure:

- restore/recreate only frontend using the preserved rollback image/config;

- verify old frontend health;

- stop and escalate.

---

# 16. Gate D — Development Runtime Preservation

After both new application containers are healthy, confirm:

```text
backend = new accepted image, healthy
frontend = new accepted image, healthy

PostgreSQL = original container, healthy
Redis = original container, healthy

application storage/named volumes preserved
no broad container privilege added
Source binds remain read-only
broker bind remains read-only
```

Also confirm the 12.65.3 Source services remain healthy and namespace topology remains one root / one NAS slot.

Do not disturb the NAS merely to prove this gate.

---

# 17. Browser-Safe Mounted Capability Validation

Validate the reconstructed application now exposes Mounted Source capability correctly.

Required:

- browser/API can discover approved Mounted Local/NAS locations through opaque location identifiers;

- raw host paths, Runtime Roots, UUIDs, credentials, stable Access Node secrets, and unnecessary hardware evidence are not exposed;

- Mounted capability is not treated as a universal filesystem mode;

- External/Removable/Optical are not routed through Mounted access;

- iCloud remains independently available as designed.

Do not modify code unless a genuine 12.65.1 implementation defect is proven.

If a repository defect is discovered, stop and escalate rather than patching live activation ad hoc.

---

# 18. Bounded Source Profile Validation

Application database writes are authorized only for the minimum Development Source Profile records required for this milestone.

First inspect existing Source Profiles through the normal application/API boundary.

Prefer exact reuse if an existing modern Mounted Profile already represents the intended validation Source and passes durable identity checks.

Do not:

- adopt a legacy path-only Profile;

- relink a legacy Profile;

- merge Profiles;

- rewrite existing roots;

- create duplicate profiles when exact modern reuse is available.

If no suitable modern Profile exists, creation of at most:

```text
one Mounted Local validation Profile
one Mounted NAS validation Profile
```

is authorized.

Use clear Development/stabilization labels.

Do not delete these records during this milestone.

They may remain as explicit Development validation evidence.

---

# 19. Validation Root Safety

Do not invent or create Source filesystem directories merely for this milestone.

Use only an already-existing approved contained root.

For Local:

- use an already-existing safe contained root under the approved Local mounted location;

- do not write or create files.

For NAS:

- preserve the established requirement that a NAS Source uses an explicit contained root rather than an unbounded whole-share scan;

- use only an existing bounded subdirectory that is safe for Development validation;

- do not use an application-storage directory as a photo Source merely because it exists;

- do not create a new NAS directory;

- do not write to NAS.

If no safe existing bounded validation root can be established from current approved evidence, stop that Source's creation step and report the exact missing prerequisite.

Do not broaden filesystem inspection to search the user's NAS for candidate photo folders.

One provider may still be validated if the other lacks a safe existing validation root, but report the limitation precisely.

---

# 20. Mounted Source Creation / Exact Reuse

For each approved validation Source:

validate:

```text
server-issued opaque location
+ contained POSIX endpoint-relative root
+ durable Endpoint identity
+ stable Access Node
+ host-side Observed Path
+ persisted host-side Profile source_root_path
```

Confirm:

- no browser-submitted Runtime Root;

- no arbitrary host path;

- no backslash translation;

- no path-only legacy adoption;

- no unexpected Endpoint/Profile duplication;

- persisted Profile root remains host-side.

Do not run Source Intake.

---

# 21. Gate E — Readiness

For each available validation Profile:

run the normal readiness path.

Required result:

```text
ready
```

or an accurately classified non-ready result based on real Source availability.

For a successful Mounted Source confirm:

- exact selected Profile;

- matching Endpoint;

- strong identity;

- stable Access Node evidence;

- broker re-probe;

- contained readable root;

- backend-derived Runtime Root;

- stored Profile root unchanged.

Do not "fix" readiness failures by rewriting Source identity or paths.

If identity conflicts, stop that Source validation and report.

---

# 22. Gate F — Source Selection

For each ready validation Profile:

run normal Source Selection.

Confirm:

```text
selected Source Profile ID = intended Profile
verified identity = true
Runtime Root = backend-derived container path
persisted Profile source_root_path = unchanged host-side root
```

For NAS also confirm:

- selected Profile is tied to canonical NAS Endpoint identity;

- Runtime Root lies under the fixed container Mounted namespace;

- no Windows path or temporary receiving path is substituted.

Do not launch Run Ingestion.

---

# 23. Source Selector Validation

After creation/exact reuse, validate that Source Selector behavior reflects active saved Profiles as designed.

Expected principle:

```text
Create Source
shows available Source categories/capabilities

Source Selector
shows Source Types represented by active saved Profiles
```

If the new Mounted Local/NAS validation Profiles are active, their Source Types should be selectable accordingly.

Do not redesign selector filtering merely because Create Source and Source Selector contain different lists.

Record actual observed result.

---

# 24. Explicit Dispatch Boundary

Do not call production Run Ingestion dispatch.

Do not attempt to make dispatch "safe" by adding a dry-run.

Accepted pre-intake evidence is:

```text
12.65.1 automated dispatch regression
+
12.65.4 live readiness
+
12.65.4 live Source Selection
+
verified Runtime Root
```

12.65.5 will authorize one controlled real dispatch/intake.

---

# 25. No Source Filesystem Mutation

Throughout 12.65.4:

Do not:

- create/delete/rename Source files;

- chmod/chown Source paths;

- write validation marker files;

- create NAS folders;

- copy photos;

- run cleanup against Source media;

- modify filesystem timestamps intentionally.

Source access remains read-only.

---

# 26. No Data-Service Mutation Beyond Bounded Profiles

Do not:

- reset/recreate PostgreSQL;

- reset/recreate Redis;

- delete application data;

- modify schema;

- run migration/backfill;

- broadly edit DB records;

- delete legacy Sources as cleanup.

Only normal application writes required to create at most the approved validation Source Profiles are authorized.

If additional manual SQL/database surgery appears necessary, stop and escalate.

---

# 27. Rollback Rules

## Frontend builder failure

Do not touch live frontend.

## Application image build failure

Leave existing live containers unchanged.

## Backend activation failure

Restore only backend to preserved rollback image/config.

## Frontend activation failure

Restore only frontend.

## Provider/container visibility failure

Rollback backend/frontend as needed; do not alter Source host services or NAS to compensate.

## Application Source validation failure

Do not delete/rewrite identity records ad hoc.

Preserve evidence and escalate if the failure indicates an architecture defect.

Never use:

```text
docker compose down
volume deletion
broad image prune
NAS unmount
namespace teardown
database restore/reset
```

as routine rollback.

---

# 28. Historical Validation Image

Do not remove the historical M012 backend validation image during this milestone.

Cleanup remains separately approval-gated.

Do not run broad Docker cleanup.

---

# 29. Privileged Command Model

If any privileged host read is required:

- coder prepares the exact bounded command;

- Product Owner runs it manually;

- coder does not request or handle sudo password.

No new privileged host mutation should be necessary in 12.65.4.

If privileged mutation appears necessary, stop and escalate.

---

# 30. Escalation Conditions

Stop and report:

`STATUS: ESCALATION REQUIRED`

if:

- 12.65.3 Source services/topology are no longer healthy;

- frontend builder requires dependency/lockfile changes;

- backend/frontend activation would require PostgreSQL/Redis recreation;

- Source namespace/socket binds are writable or broader than approved;

- backend must become privileged;

- Mounted capability exposes protected host identity/path evidence;

- a legacy Profile must be adopted/relinked;

- no safe existing validation root is available and filesystem mutation would be needed;

- Source readiness requires rewriting Profile identity/root;

- Source Selection derives an unexpected Runtime Root;

- dispatch must be called to prove pre-intake correctness;

- ingestion/Vault/provenance code must change;

- schema/migration/backfill becomes necessary;

- Test or Production must change;

- rollback cannot be bounded to backend/frontend.

Use:

```text
STATUS: ESCALATION REQUIRED

Gate:
Observed state:
Expected state:
Mutation already performed:
Rollback performed:
Development/data state preserved:
Architecture / 12.64 implication:
Smallest next decision required:
Recommended Product Owner action:
```

---

# 31. Required Closeout

Create exactly:

`docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.4_frontend_validation_and_development_activation_closeout.md`

Required structure:

## 1. Repository State

Branch, HEAD/upstream, initial/final Git status.

## 2. 12.65.3 Host Preconditions

Service/socket/namespace readiness.

## 3. Development Pre-Activation Baseline

Old container/image IDs and preservation evidence.

## 4. Rollback Image References

Exact bounded rollback tags/image IDs.

## 5. Gate A — Frontend Builder Validation

Pinned Node/npm/lint/build result.

## 6. Gate B — Backend/Frontend Builds

Images built and exact scope.

## 7. Backend Activation

Exact recreation and health result.

## 8. Gate C — Backend Mounted Runtime Visibility

Groups, binds, privilege, broker reachability.

## 9. Frontend Activation

Exact recreation and health.

## 10. Gate D — Development Preservation

PostgreSQL/Redis/storage/source-service preservation.

## 11. Mounted Capability API/UI

Opaque location/capability behavior.

## 12. Validation Source Profiles

Existing reuse or exact bounded creation.

## 13. Validation Roots

Why each selected root was safe and existing.

Do not expose private Source contents.

## 14. Gate E — Readiness

Local/NAS results.

## 15. Gate F — Source Selection

Profile ID, identity, Runtime Root, persisted host root.

## 16. Source Selector

Observed behavior.

## 17. Explicit No-Dispatch / No-Intake Evidence

Confirm no Run Ingestion or Source Intake occurred.

## 18. 12.64 Protection

Confirm no ingestion/Vault/provenance changes or live claims beyond authorized evidence.

## 19. Docker/Data Preservation

No PostgreSQL/Redis/volume/broad runtime changes.

## 20. Rollback Status

Whether rollback was exercised and preserved image references.

## 21. Exact Commands and Results

Bounded execution record.

## 22. Deviations

Only approved/unavoidable deviations.

## 23. Known Limitations

Especially anything deferred to 12.65.5.

## 24. Recommended 12.65.5 Scope

Controlled real Mounted Source dispatch/intake proof.

## 25. Git Status

Include:

```bash
git status --short
git diff --stat
git diff --check
```

Do not stage the closeout merely for validation.

Clearly label evidence:

```text
confirmed live
confirmed by builder
confirmed container runtime
confirmed application/API
confirmed repository
requires 12.65.5
not tested
```

---

# 32. Definition of Done

12.65.4 is complete when:

- 12.65.3 Source services remain healthy;

- frontend validation passes using the repository-pinned Node 22.23.1 builder path;

- rollback image references exist for the prior backend/frontend;

- only backend/frontend are rebuilt;

- only backend/frontend are recreated;

- PostgreSQL and Redis remain the same healthy containers;

- backend has only the approved supplemental GIDs and read-only Source/broker binds;

- backend is non-privileged;

- frontend is healthy on the reconstructed UI;

- Mounted Local/NAS capability is browser/API-visible through opaque choices;

- no protected raw host identity/path is exposed;

- suitable modern validation Profiles are exactly reused or at most one Local and one NAS validation Profile are created;

- no legacy Profile is adopted/relinked;

- validation uses only safe pre-existing contained roots;

- no Source filesystem writes occur;

- readiness is exercised;

- Source Selection is exercised;

- selected Profile ID and backend-derived Runtime Root are confirmed;

- persisted Profile root remains host-side and unchanged;

- Source Selector behavior is understood/validated;

- production Run Ingestion dispatch is not invoked;

- Source Intake is not invoked;

- Vault/provenance are not mutated;

- no schema/migration/backfill occurs;

- Test and Production remain untouched;

- rollback remains bounded;

- exactly one closeout is created;

- coder does not commit or push.
