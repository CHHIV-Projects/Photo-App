# Milestone 12.65.6 — Stabilization Regression and Documentation Reconciliation Closeout

Date: 2026-08-11

## 1. Repository State

- Branch: `integration/linux-runtime-clean`.
- `HEAD`: `05c5e91ed027704fee4ca3071468692efb5a20d6`.
- Upstream: `05c5e91ed027704fee4ca3071468692efb5a20d6`.
- Frozen historical deployment reference, local and remote:
  `453032b3eddaa677094ef0eaadfa3711ff536989`.
- The frozen branch was not checked out, modified, or used as current authority.
- Initial worktree state contained only the Product Owner-supplied untracked
  controlling handoff.
- No implementation, configuration, migration, dependency, or lockfile changed.
- No commit, push, merge, staging, or other Git write was performed.

Repository preflight: **PASS**.

## 2. Development Runtime Health

The final read-only runtime snapshot matched the pre-documentation baseline:

| Service | Container ID prefix | Image ID prefix | Started | Result |
|---|---:|---:|---|---|
| Backend | `c0c2fd86f73a` | `bea9134246c9` | `2026-08-09T20:44:58.431620893Z` | running/healthy |
| Frontend | `76fd0bc42191` | `ecb02621d48f` | `2026-08-09T20:46:14.034635437Z` | running/healthy |
| PostgreSQL | `42bbb232f702` | `253815cf7579` | `2026-08-03T17:10:19.970961261Z` | running/healthy |
| Redis | `107e680b822a` | `90e7a336d044` | `2026-08-03T17:10:19.938731282Z` | running/healthy |

The backend remained unprivileged as `photo-organizer`, with the accepted
read-only Source bind and broker access. PostgreSQL and Redis were not
recreated, restarted, queried for mutation, or exposed.

Development runtime health: **PASS**.

## 3. Mounted NAS Post-Ingestion Smoke

Read-only live evidence confirmed modern NAS Endpoint `1` / Profile `2` remains
active and available through `linux_stable_mount_v1`:

- persisted host root:
  `/mnt/photo-organizer-sources/nas/photo-organizer/development/sample-media`;
- derived Runtime Root:
  `/app/sources/nas/photo-organizer/development/sample-media`;
- readiness: ready, durable identity verified/matched, zero blockers;
- Source Selection: available and selected exactly once;
- canonical authority: `//192.168.1.171/PhotoOrganizer`;
- host NAS slot: one shared CIFS row, major/minor `0:48`;
- namespace root: one shared ext4 row, major/minor `259:2`.

A final transaction-read-only query found exactly:

| Source Intake Run | Ingestion Run | Scanned | Known | New unique | Rejected | Status |
|---:|---:|---:|---:|---:|---:|---|
| 3 | 3 | 16 | 0 | 16 | 0 | completed |
| 4 | 4 | 16 | 16 | 0 | 0 | completed |

Both runs retain the exact expected Runtime Root. No later Profile-2 intake or
ingestion run exists. The established 16 Assets, 16 canonical Vault objects,
and 16 Profile-bound provenance rows remained the accepted post-12.65.5 state.
No Source path was enumerated or written in this milestone.

Mounted NAS post-ingestion smoke: **PASS**.

## 4. 12.64 Protection Regression

Command:

```bash
PYTHONPATH=backend .venv/bin/python -m unittest -v backend/tests/test_source_intake_provenance_vault_hardening.py
```

Result: **8/8 PASS** in 0.100 seconds.

Coverage preserved runtime identity, cross-Source duplicate provenance,
different-extension behavior, same-Source repeat idempotency, damaged/conflict
fail-closed behavior, and rejected-candidate no-row behavior.

12.64 protection regression: **PASS**.

## 5. Mounted Provider Regression

The focused Mounted/provider/readiness/selection/dispatch regression included:

```text
test_posix_source_paths.py
test_linux_source_access_broker.py
test_linux_stable_mount_provider.py
test_linux_source_access_services.py
test_linux_source_namespace_unit.py
test_prepare_source_namespace.py
test_admin_source_identity_api.py
test_source_identity_probe_service.py
test_source_identity_probe_schema.py
test_source_identity_windows_provider.py
test_linux_development_fixture_probe_provider.py
test_source_creation_service.py
test_admin_source_creation_api.py
test_source_profile_readiness_service.py
test_admin_source_profile_readiness_api.py
test_source_selection_service.py
test_admin_source_selection_api.py
test_run_ingestion_dispatch_service.py
test_admin_run_ingestion_dispatch_api.py
```

Result: **235/235 PASS** in 1.595 seconds. Warnings were limited to existing
Starlette/httpx deprecations and SQLAlchemy identity-map notices.

Mounted provider regression: **PASS**.

## 6. iCloud Preservation Regression

Command:

```bash
PYTHONPATH=backend .venv/bin/python -m unittest discover -v -s backend/tests -p 'test_icloud*.py'
```

Result: 216 tests; 215 passed; one failed in 4.598 seconds.

The sole failure was exactly:

```text
test_icloud_non_repeat_validation_loop.IcloudNonRepeatValidationLoopTests.test_non_repeat_skips_download_when_all_cleaned_resources_are_known
```

Its `Popen.assert_not_called` signature matched the Product Owner-approved
existing baseline. Classification: **PRE-EXISTING ACCEPTED**. No changed or
additional iCloud failure occurred.

iCloud preservation regression: **PASS against accepted baseline**.

## 7. Windows Provider Preservation

Focused automated/static evidence confirmed that Windows Local, External,
Removable Media, NAS, and Optical provider implementation remains present and
was not rerouted through Mounted Linux access. Windows paths remain
Windows-native authority for the future Windows Helper and are not translated
into artificial Linux paths. No Windows ingestion or live Windows operation was
attempted.

Windows provider preservation: **PASS**.

## 8. Source Selector / UI Validation

Read-only live and compiled evidence confirmed:

- frontend root and proxied Mounted-locations endpoint returned HTTP 200;
- Local `linux-local-server-photos` and NAS
  `linux-nas-photo-organizer` were both available through
  `linux_stable_mount_v1` with zero blockers;
- active NAS Profile `2` appeared exactly once;
- endpoint type `NAS` maps to operator Source Type `NAS`;
- selector choices derive from active Profiles;
- Mounted applies only to accepted Local/NAS locations;
- Windows-connected External/Removable/Optical options are not falsely exposed
  as Mounted Linux capability;
- iCloud remains independent.

Literal browser clicking was not required by the approved clarification.

Source Selector/UI validation: **PASS**.

## 9. Full Backend Regression

Command:

```bash
PYTHONPATH=backend .venv/bin/python -m unittest discover -s backend/tests -p 'test_*.py'
```

Result: 643 tests; 642 passed; one failed in 9.295 seconds. The only failure was
the exact iCloud baseline named in Section 6 and was therefore
**PRE-EXISTING ACCEPTED**. There was no new or additional backend regression.

Full backend regression: **PASS against accepted baseline**.

## 10. Frontend Builder Validation

Command:

```bash
docker build --no-cache --progress=plain --target builder \
  --tag photo-organizer-dev-frontend:validation-12.65.6-05c5e91 frontend
```

The repository-pinned `node:22.23.1-bookworm-slim` builder performed fresh
`npm ci`, lint, TypeScript/Next.js compilation, and optimized production build.
All required builder stages passed. Validation image:

```text
sha256:1c0ea9ac942fc10b59313bf483ad6bf177d22933bc9fae05349067e1b3291079
```

Existing lint warnings and the existing npm audit report of eight high-severity
findings did not fail the required build and remain separately deferred. The
authorized validation image/cache was retained; no image or cache cleanup was
performed. No running container was recreated.

Frontend builder validation: **PASS**.

## 11. Development / Test Isolation

The Test containers remained healthy with their original identities:

| Service | Container ID prefix | Image ID prefix |
|---|---:|---:|
| Backend | `d14840ba1cbd` | `1079365367bc` |
| Frontend | `c31a00c9d1fb` | `9a70c2d27ee9` |
| PostgreSQL | `f591b37f231b` | `253815cf7579` |
| Redis | `5c1df0d9be36` | `90e7a336d044` |

Development and Test retained distinct Compose projects, loopback ports,
networks, application-storage volumes, PostgreSQL volumes, and Redis volumes.
No shared writable application-storage coupling was found. Test and Production
were not mutated.

Development/Test isolation: **PASS**.

## 12. Reboot Persistence Status

Current non-reboot evidence is:

```text
photo-organizer-source-namespace.service = enabled, active, exited, success
photo-organizer-source-identity-broker.service = enabled, active, running, success
/mnt/nas/photo-organizer fstab row = present for canonical CIFS authority
/mnt/nas/photo-organizer autofs row = present and active
```

The namespace had zero restarts. The broker retained PID `58307` and zero
restarts. No reboot was authorized or performed. Therefore actual post-reboot
persistence is **NOT PROVEN** by 12.65.6 and is not claimed.

Configuration/present-state evidence: **PASS**. Actual reboot proof: **DEFERRED**.

## 13. Documentation Reconciliation

### Controlling Handoff

The exact Product Owner-supplied controlling handoff was added at its authorized
path. Historical stabilization reasoning was preserved. A bounded Section 44
records the clean reconstruction, accepted Mounted provider, live NAS evidence,
deferred Mounted Local proof, future Windows Helper boundary, independent
iCloud provider, and 12.65.7/12.66 transition.

### Mounted Source Access Guide

The guide now records persisted host-root versus Runtime Root authority,
mount-table-first namespace safety, broker/identity boundaries, readiness and
selection, Source Intake authority, operator-safe troubleshooting, accepted
live NAS state, and deferred Mounted Local live proof.

### Project Workflow / Controlled Reset

`project_workflow_v7.md` now contains the reusable ten-step “Controlled Reset /
Escape From Iterative Repair Loop” workflow. It preserves evidence, stops live
mutation, identifies a trusted baseline, classifies bounded deltas, reconstructs
on a clean branch, and requires independent stabilization acceptance.

### Current Context, Architecture, Rules, and Parking Lot

Bounded current-state corrections align `project_context_v7.md`,
`project_architecture_v7.md`, `coding_agent_rules_v7.md`, and
`canonical_parking_lot_v7.md` with accepted Mounted Local/NAS architecture,
live NAS validation, deferred Local proof, the future Windows Helper, and the
independent Cloud-provider boundary. Historical statements that were clearly
historical were retained.

Documentation reconciliation: **PASS**.

## 14. Documentation Conflict Check

The final bounded scan of all authorized/current controlling documents found no
material contradiction that:

- translates Windows paths into Linux paths;
- assigns Windows External/Removable/Optical access to Mounted Linux;
- retains the old M012 namespace as target architecture;
- replaces persisted Profile root/identity with Runtime Root;
- denies accepted Mounted NAS canonical identity or live validation.

The first scan identified stale current-state passages in
`project_context_v7.md` and `coding_agent_rules_v7.md`. Work stopped, the Product
Owner explicitly expanded the write set, and only those bounded passages were
corrected before the final scan.

Documentation conflict check: **PASS**.

## 15. Regression Acceptance Matrix

| Gate | Evidence | Result | Classification |
|---|---|---|---|
| Development health | live read-only runtime inspection | PASS | accepted |
| Mounted NAS smoke | live API/DB/mount evidence | PASS | accepted |
| 12.64 protection | 8 focused tests | PASS | accepted |
| Mounted provider | 235 focused tests | PASS | accepted |
| iCloud preservation | 216 focused tests | PASS with one exact baseline failure | PRE-EXISTING ACCEPTED |
| Windows preservation | focused/static evidence | PASS | accepted |
| Source Selector/UI | live API + compiled logic | PASS | accepted |
| Full backend | 643 tests | PASS with same exact baseline failure | PRE-EXISTING ACCEPTED |
| Frontend builder | pinned clean builder | PASS | accepted |
| Development/Test isolation | live read-only inspection | PASS | accepted |
| Documentation reconciliation | bounded write set | PASS | accepted |
| Controlled reset workflow | current v7 workflow | PASS | accepted |
| Reboot persistence | enabled/configured evidence only | DEFERRED | actual reboot not proven |

## 16. Known Deferred Limitations

- Mounted Local implementation exists; live Profile/readiness/selection proof
  remains deferred until an approved suitable root is available.
- Actual host-reboot persistence remains unproven.
- Windows Helper implementation begins only after stabilization/merge in 12.66.
- Direct Linux Optical support remains conditional if independently desired.
- Dev-to-Test replacement, rollback, Production, and release-grade backup/
  restore remain future work.
- Existing frontend lint warnings and npm audit findings remain separate work.
- The exact accepted iCloud unit-test baseline failure remains unresolved.
- No claim is made that NAS Source access makes NAS the application, database,
  or Vault storage authority.

## 17. Exact Commands and Results

Principal regression commands and results are recorded in Sections 4, 5, 6, 9,
and 10. Final preservation commands included:

```bash
docker ps --filter label=com.docker.compose.project=photo-organizer-dev \
  --format '{{.Names}}|{{.ID}}|{{.Image}}|{{.Status}}'
docker ps --filter label=com.docker.compose.project=photo-organizer-test \
  --format '{{.Names}}|{{.ID}}|{{.Image}}|{{.Status}}'
systemctl show photo-organizer-source-namespace.service \
  photo-organizer-source-identity-broker.service \
  --property=Id,LoadState,ActiveState,SubState,Result,UnitFileState,NRestarts,MainPID
findmnt --kernel --mountpoint /mnt/photo-organizer-sources \
  --output TARGET,SOURCE,FSTYPE,MAJ:MIN,PROPAGATION --noheadings
findmnt --kernel --mountpoint /mnt/photo-organizer-sources/nas/photo-organizer \
  --output TARGET,SOURCE,FSTYPE,MAJ:MIN,PROPAGATION --noheadings
```

Results: all eight Development/Test containers healthy and identity-stable;
namespace enabled/active/exited; broker enabled/active/running with unchanged
PID and zero restarts; one exact namespace row and one exact shared CIFS slot.

The final ingestion query was executed inside the backend with
`SET TRANSACTION READ ONLY`, returned only Runs 3 and 4, and ended with rollback.
No application mutation command was issued.

## 18. Deviations

- The first Development environment check referenced a nonexistent historical
  path, `/home/chuck/.config/photo-organizer/development.env`. It stopped before
  Docker inspection and was corrected to the accepted
  `docker/.env.development` path.
- Static Test Compose rendering could not complete because the protected static
  Test environment intentionally lacks `TEST_RELEASE_SHA`. No mutation occurred;
  live formatted container inspection established isolation instead.
- The workspace `apply_patch` helper failed before file access because its
  sandbox could not configure loopback networking. Authorized documentation was
  changed through non-staging unified diffs with `git apply`; no index operation
  occurred.
- The Product Owner-supplied handoff used CRLF and contained existing trailing
  spaces. Line endings/trailing whitespace were mechanically normalized to LF
  without changing wording so the required no-index whitespace check could pass.
- An initial systemd automount query used insufficient shell escaping and
  reported a nonexistent unit. `systemd-escape` supplied the exact unit name;
  the corrected query showed the generated automount loaded and active.
- An early mount inspection included a non-secret CIFS username option in local
  output. No password, credential content, Access Node value, protected raw
  identifier, or unrelated environment value was emitted or copied here;
  subsequent evidence used narrow selected fields.
- No deviation changed runtime, Source, data, service, mount, Test, or Production
  state.

## 19. Git Status

Expected tracked modifications are limited to:

```text
docs/context/canonical_parking_lot_v7.md
docs/context/coding_agent_rules_v7.md
docs/context/project_architecture_v7.md
docs/context/project_context_v7.md
docs/context/project_workflow_v7.md
docs/server_deployment/Photo_Organizer_Mounted_Source_Access_Guide.md
```

Expected untracked files are limited to:

```text
docs/context/Photo_Organizer_Ingestion_Completion_Arc_Deep_Context_Handoff_v1.md
docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.6_stabilization_regression_and_documentation_reconciliation_closeout.md
```

`git diff --check` passes for tracked modifications. Ordinary `git diff` and
`git diff --check` do not inspect either untracked file. They were not staged.
For each untracked file, `git diff --no-index --check /dev/null <file>` returns
exit status `1` because the file differs from `/dev/null`, with no whitespace
diagnostic output. That exit status is the expected no-index difference status,
not a whitespace failure.

## 20. 12.65 Stabilization Acceptance Decision

All required 12.65.6 gates pass against the explicitly accepted baseline. The
single reproduced iCloud failure is exactly the Product Owner-approved
pre-existing failure; there is no new regression. Development and Test remain
healthy and isolated, Mounted NAS state and ingestion evidence remain intact,
no new ingestion or runtime mutation occurred, documentation is reconciled,
and the remaining limitations are explicit.

Milestone 12.65.6 is accepted as ready for the separately controlled 12.65.7
stabilization merge milestone. This decision does not itself authorize commit,
push, merge, reboot, cleanup, runtime change, or 12.66 implementation.

READY FOR 12.65.7
