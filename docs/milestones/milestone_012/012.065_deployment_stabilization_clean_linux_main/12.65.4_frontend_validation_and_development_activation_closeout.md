# Milestone 12.65.4 — Frontend Validation and Development Activation Closeout

Status: **COMPLETE with the approved bounded Local validation limitation**

Completed at: `2026-08-09T20:53:58Z`

## 1. Repository State

Evidence: **confirmed live**.

- Branch: `integration/linux-runtime-clean`.
- Initial HEAD: `d4a04058ffa6d5101567b8d932a1db641aebfff9`.
- Initial upstream: `d4a04058ffa6d5101567b8d932a1db641aebfff9`.
- Initial working tree: clean.
- Frozen local and remote `feature/deployment-linux-runtime` refs remained at `453032b3eddaa677094ef0eaadfa3711ff536989`.
- No branch switch, merge, reset, rebase, commit, or push occurred.
- The only repository change is this closeout.

## 2. 12.65.3 Host Preconditions

Evidence: **confirmed live** and **confirmed by Product Owner-executed privileged read**.

- `photo-organizer-source-namespace.service`: enabled, active, exited, result success.
- `photo-organizer-source-identity-broker.service`: enabled, active, running, result success, zero restarts.
- The broker socket was a non-symlink Unix socket with the accepted owner/group and mode `0660`.
- The corrected private-network-namespace socket check found exactly one listening row owned by the broker process.
- Namespace root remained one shared ext4 row at `/mnt/photo-organizer-sources`.
- NAS slot remained one shared CIFS row at `/mnt/photo-organizer-sources/nas/photo-organizer` with major/minor `0:48`.
- The authoritative NAS automount/CIFS infrastructure remained active and unchanged.
- `SOURCE_ACCESS_SOCKET_GID=987` and `SOURCE_ACCESS_DATA_GID=1000` remained exact. No unrelated protected environment values were printed.

The initial unprivileged socket inspection was denied by the protected runtime-directory mode. The Product Owner ran the bounded read-only check. Its first `ss` formatter incorrectly assumed that the socket path was the final field; the corrected all-field matcher passed. This was a validation-harness correction, not a host-state defect.

## 3. Development Pre-Activation Baseline

Evidence: **confirmed live**.

| Service | Original container ID | Original image ID | Initial state |
|---|---|---|---|
| backend | `2ce2bf2cbfe46b8b93e72423947f4ab6c2483e923a018248bce79617938235a0` | `sha256:3aff12de14ab834a9ea644fecb6f224de88fa3d19f5f69e7823ea159a08af072` | running, healthy |
| frontend | `4403b0d146f730581fa725dc62bc330b3fe92241011e8e4ce3f39689756904e9` | `sha256:f240ea7b46c7e43c4c54ccdeec428c8b091cbc83898f634ad39e82f651cf3c06` | running, healthy |
| PostgreSQL | `42bbb232f702da07dd6a302153c0a4aebd910aa148a7d82b7285ed4080112712` | `sha256:253815cf7579ffa05e1673d92e78d37273e61be0e4414e9a1449337d7925be94` | running, healthy |
| Redis | `107e680b822a993871afff260e4e55273e8d30ea405b2474070678f849a08a57` | `sha256:90e7a336d044f1abc9e9dbc05d65566850896d11453bbd1dd0fb7e5059f0e8fb` | running, healthy |

Baseline named volumes:

- `photo-organizer-dev_application_storage` at `/app/storage`;
- `photo-organizer-dev_postgres_data` at `/var/lib/postgresql/data`;
- `photo-organizer-dev_redis_data` at `/data`.

No application or database contents were inspected for the baseline.

## 4. Rollback Image References

Evidence: **confirmed live**.

- `photo-organizer-dev-backend:rollback-12.65.4-d4a0405` resolves to `sha256:3aff12de14ab834a9ea644fecb6f224de88fa3d19f5f69e7823ea159a08af072`.
- `photo-organizer-dev-frontend:rollback-12.65.4-d4a0405` resolves to `sha256:f240ea7b46c7e43c4c54ccdeec428c8b091cbc83898f634ad39e82f651cf3c06`.
- Only the original backend and frontend images were tagged.
- PostgreSQL and Redis were not retagged.
- The rollback references remained present at final verification.

## 5. Gate A — Frontend Builder Validation

Evidence: **confirmed by builder**.

A separate no-cache build used the repository `frontend/Dockerfile` `builder` target and pinned `node:22.23.1-bookworm-slim` image. The following all exited zero:

- fresh `npm ci`;
- `npm run lint`;
- TypeScript validity checking performed by Next.js;
- `npm run build` / optimized production build.

The build compiled successfully and generated all application routes. Existing lint warnings and the npm audit report of eight high-severity dependency findings were observed. They did not fail the locked validation commands, and no dependency or lockfile mutation was made.

Validation image: `photo-organizer-dev-frontend:validation-12.65.4-d4a0405`.

## 6. Gate B — Backend/Frontend Builds

Evidence: **confirmed by builder**.

The permanent Development Compose file, permanent GPU overlay, and protected Development environment file built only `backend` and `frontend`.

- New backend image: `sha256:bea9134246c952d7647320328a630e131cd19c78230e2c2f096acda3cb181609`.
- New frontend image: `sha256:ecb02621d48fe86468bdce8b09083078dd0a6a60ed64e0d5e6e953c20b1c4c19`.
- PostgreSQL and Redis were neither built nor recreated.
- The source tree remained clean after the builds.

## 7. Backend Activation

Evidence: **confirmed container runtime**.

Only the Development backend was recreated, using `--no-deps --force-recreate` after the image build.

- New backend container: `c0c2fd86f73a4d67c22c2fb395bd9e353b17b4e62460cc3da338a783faa9614d`.
- Image: `sha256:bea9134246c952d7647320328a630e131cd19c78230e2c2f096acda3cb181609`.
- Final state: running and healthy.
- Start time: `2026-08-09T20:44:58.431620893Z`.

The backend became healthy on the first bounded poll. Backend rollback was not required.

## 8. Gate C — Backend Mounted Runtime Visibility

Evidence: **confirmed container runtime** and **confirmed application/API**.

- Container user: `photo-organizer`, runtime UID/GID `999:999`.
- Exact groups: primary `999`, supplemental `987` and `1000`.
- `Privileged=false`.
- No added Linux capabilities or broad device binds.
- The established GPU overlay supplied only its expected GPU device request.
- Application storage remained the named volume at `/app/storage`.
- `/mnt/photo-organizer-sources` was bound to `/app/sources` read-only with `rslave` propagation.
- `/run/photo-organizer-source-access` was bound to the same container path read-only.
- The broker socket was visible as a Unix socket inside the backend.
- Container mountinfo reported both approved binds as read-only.
- No Docker socket, broad host root, or unrelated host path was bound.
- The backend reached the broker through the approved socket and returned the bounded Mounted location capability.

No Source bytes were read and no Source write request was made.

## 9. Frontend Activation

Evidence: **confirmed container runtime**.

Only the Development frontend was recreated, using `--no-deps --force-recreate` after backend Gate C passed.

- New frontend container: `76fd0bc42191d10adbf12eb0d3f7480e9ed313ca91a300b04ddab071418e72c5`.
- Image: `sha256:ecb02621d48fe86468bdce8b09083078dd0a6a60ed64e0d5e6e953c20b1c4c19`.
- Final state: running and healthy.
- Start time: `2026-08-09T20:46:14.034635437Z`.
- The frontend root and its proxied Mounted capability API both returned successfully.

Frontend rollback was not required.

## 10. Gate D — Development Preservation

Evidence: **confirmed live** and **confirmed container runtime**.

- PostgreSQL retained original container ID `42bbb232f702da07dd6a302153c0a4aebd910aa148a7d82b7285ed4080112712`, original start time, healthy state, and `photo-organizer-dev_postgres_data`.
- Redis retained original container ID `107e680b822a993871afff260e4e55273e8d30ea405b2474070678f849a08a57`, original start time, healthy state, and `photo-organizer-dev_redis_data`.
- Backend retained `photo-organizer-dev_application_storage`.
- Namespace and broker services remained enabled and healthy.
- Namespace root and NAS-slot rows remained exact and unique.
- No volume was deleted or replaced.

## 11. Mounted Capability API/UI

Evidence: **confirmed application/API**, **confirmed by builder**, and **confirmed repository**.

The rebuilt live frontend/backend exposed exactly two available Mounted choices:

- opaque Local location `linux-local-server-photos`;
- opaque NAS location `linux-nas-photo-organizer`.

The response contained only the accepted top-level and location-summary keys. Location IDs were opaque and contained no slash or backslash. Raw host paths, Runtime Roots, filesystem UUIDs, credentials, Access Node identity, stable hashes, and hardware evidence were not present in the discovery response.

The rebuilt frontend logic routes Mounted access only for Local and NAS. External, Removable, and Optical remain unavailable through this server-mounted capability, and iCloud remains independently represented.

## 12. Validation Source Profiles

Evidence: **confirmed application/API**.

Initial profile inspection found one active legacy path-only profile:

- Profile `1`, `M005 Controlled Fixture Source`.

It was not adopted, relinked, rewritten, or otherwise mutated.

No exact modern Mounted profile existed for either approved validation root.

The approved Local root did not pass the modern read-only plan and no Local profile was created.

The approved NAS root passed. The modern plan/confirm API created exactly:

- Endpoint `1`, alias `12.65.4 Development NAS`, Endpoint type NAS;
- Profile `2`, label `12.65.4 NAS Sample Media`, active;
- endpoint-relative root `development/sample-media`.

Creation returned completed, durable identity verified, `created_endpoint=true`, `created_source=true`, no blockers, and no duplicate record.

## 13. Validation Roots

Evidence: **confirmed application/API**.

Only the two Product Owner-approved exact candidates were submitted to the normal provider plan:

- Local: `linux-local-server-photos` + `family`.
- NAS: `linux-nas-photo-organizer` + `development/sample-media`.

Local failed closed with:

- `linux_location_identity_ambiguous`;
- `probe_not_completed`;
- `invalid_source_root`;
- `linux_mapping_missing`.

No alternative Local root was searched, listed, created, or substituted.

NAS passed exact existence, readability, containment, stable mapping, and durable identity checks. Its persisted host root is `/mnt/photo-organizer-sources/nas/photo-organizer/development/sample-media`; its derived container Runtime Root is `/app/sources/nas/photo-organizer/development/sample-media`.

No directory enumeration, recursive browsing, sibling search, directory creation, marker write, chmod, or chown occurred for either root.

## 14. Gate E — Readiness

Evidence: **confirmed application/API**.

Profile `2` readiness result:

- Profile active;
- Endpoint `1`;
- readiness `ready`;
- durable identity `verified`;
- identity match `matched`;
- broker re-probe safe;
- `can_run_source_intake=true`;
- no operator acknowledgment required;
- no warnings or blockers.

Local readiness was **not tested** because the exact approved Local root failed the creation plan and no valid modern Local profile existed.

## 15. Gate F — Source Selection

Evidence: **confirmed application/API**.

Profile `2` Source Selection returned:

- result `selected`;
- availability `available`;
- workflow `filesystem_source_intake`;
- selected Profile ID `2`;
- selected Endpoint ID `1`;
- durable identity `verified` and matched;
- endpoint-relative root `development/sample-media`;
- backend-derived Runtime Root `/app/sources/nas/photo-organizer/development/sample-media`;
- persisted host root unchanged at `/mnt/photo-organizer-sources/nas/photo-organizer/development/sample-media`;
- provider context limited to location ID, mapping-verification flag, provider name, and provider version.

No Windows path or temporary receiving path was substituted. Local Source Selection was **not tested** for the same accepted Local limitation.

## 16. Source Selector

Evidence: **confirmed application/API**, **confirmed by builder**, and **confirmed repository**.

The live active-profile response returned Profile `2` exactly once with Endpoint `1`, Endpoint type NAS, and relative root `development/sample-media`.

The successfully compiled frontend:

- derives Source Selector type choices only from active saved Profiles;
- maps an active Profile with `endpoint_source_type=nas` to the NAS selector category;
- excludes advanced/legacy types from the normal selector choices;
- keeps Create Source capability choices logically separate from Source Selector saved-profile choices.

Literal browser clicking was not required by the approved execution clarification, and no browser-test framework was introduced.

## 17. Explicit No-Dispatch / No-Intake Evidence

Evidence: **confirmed application/API** and **confirmed execution record**.

- The production Run Ingestion dispatch endpoint was never called.
- Source Intake start/run endpoints were never called.
- No dry-run, diagnostic bypass, or alternate dispatch seam was added.
- Final Profile `2` counters: zero ingestion runs and zero Source Intake runs.
- No Source file read/write operation was requested by the milestone commands.

## 18. 12.64 Protection

Evidence: **confirmed repository** and **confirmed execution record**.

- No ingestion, Vault, provenance, dispatch, or Source Intake implementation changed.
- No schema, migration, or backfill changed or ran.
- No ingestion or provenance claim was made from this pre-intake validation.
- Profile `2` had zero provenance rows at final application/API inspection.
- Persisted host root and backend-derived Runtime Root remained distinct and correctly mapped.

Real dispatch/intake lineage proof **requires 12.65.5**.

## 19. Docker/Data Preservation

Evidence: **confirmed container runtime**.

- Only backend and frontend were built and recreated.
- PostgreSQL and Redis were not restarted, recreated, reset, or retagged.
- No Docker volume was deleted, recreated, or renamed.
- No broad Docker cleanup ran.
- No Development application-storage directory was used as a Source.
- Test and Production were untouched.
- The only application-data mutation was the explicitly authorized Endpoint `1` and Profile `2` creation through the normal Development API.

## 20. Rollback Status

Evidence: **confirmed live**.

Rollback was not exercised because both reconstructed services passed their health and functional gates. The original backend/frontend image references remain preserved under the exact rollback tags recorded in Section 4.

Rollback remains independently bounded to backend or frontend with `--no-deps`; neither data service would need recreation.

## 21. Exact Commands and Results

Evidence: **confirmed execution record**.

Core bounded commands included:

```bash
git branch --show-current
git status --short
git log --oneline --decorate -5
git rev-parse HEAD
git rev-parse '@{upstream}'
git rev-parse feature/deployment-linux-runtime
git rev-parse origin/feature/deployment-linux-runtime

systemctl show photo-organizer-source-namespace.service \
  photo-organizer-source-identity-broker.service \
  -p Id -p ActiveState -p SubState -p Result -p UnitFileState -p MainPID -p NRestarts

findmnt -Rrn /mnt/photo-organizer-sources \
  -o TARGET,SOURCE,FSTYPE,MAJ:MIN,PROPAGATION

docker image tag \
  sha256:3aff12de14ab834a9ea644fecb6f224de88fa3d19f5f69e7823ea159a08af072 \
  photo-organizer-dev-backend:rollback-12.65.4-d4a0405

docker image tag \
  sha256:f240ea7b46c7e43c4c54ccdeec428c8b091cbc83898f634ad39e82f651cf3c06 \
  photo-organizer-dev-frontend:rollback-12.65.4-d4a0405

docker build --no-cache --target builder \
  --tag photo-organizer-dev-frontend:validation-12.65.4-d4a0405 \
  frontend

docker compose --env-file docker/.env.development \
  -f docker/compose.development.yml \
  -f docker/compose.development.gpu.yml \
  build backend frontend

docker compose --env-file docker/.env.development \
  -f docker/compose.development.yml \
  -f docker/compose.development.gpu.yml \
  up -d --no-deps --force-recreate backend

docker compose --env-file docker/.env.development \
  -f docker/compose.development.yml \
  -f docker/compose.development.gpu.yml \
  up -d --no-deps --force-recreate frontend
```

Application/API calls used only:

```text
GET  /api/admin/source-identity/locations
GET  /api/admin/source-profiles?status=all
POST /api/admin/source-creation/plan
POST /api/admin/source-creation/confirm
POST /api/admin/source-profiles/2/check-readiness
POST /api/admin/source-selection/select
GET  /api/admin/source-profiles/2
```

Exact plan inputs were limited to the two approved location/root pairs. The successful NAS confirm used device name `12.65.4 Development NAS`, Source name `12.65.4 NAS Sample Media`, `naming_action=create_new`, and an immediately recomputed ready plan fingerprint.

All required gates exited zero except the approved fail-closed Local creation plan. No command invoked dispatch, Source Intake, mount mutation, service mutation, database tooling, or Source filesystem mutation.

## 22. Deviations

Evidence: **confirmed execution record**.

Approved/unavoidable deviations only:

1. The protected broker socket required the Product Owner's bounded read-only privileged inspection. No password was requested or handled.
2. The first socket `ss` parser used the wrong output-field assumption. It was corrected without host mutation, and the corrected gate passed.
3. Mounted Local live Profile/readiness/selection validation was not possible because the only approved Local candidate failed closed. The Product Owner clarification explicitly prohibited finding or substituting another root.
4. Literal browser clicking was replaced by the explicitly approved builder, live frontend/API, and compiled-logic acceptance path.

No implementation correction or live ad hoc patch was required.

## 23. Known Limitations

Evidence classification: **not tested** unless otherwise stated.

- Mounted Local Profile creation, readiness, and Source Selection remain unvalidated live because `family` failed the provider plan.
- Only the approved NAS `development/sample-media` root was validated live.
- Production dispatch, Source Intake, asset enumeration, copying, provenance, Vault behavior, and real ingestion lineage were intentionally not tested.
- No reboot/restart persistence test was performed in this milestone.
- Existing frontend lint warnings and npm audit findings remain outside this bounded activation milestone.

## 24. Recommended 12.65.5 Scope

Evidence classification: **requires 12.65.5**.

12.65.5 should use Profile `2` and its already-verified NAS Runtime Root for one separately authorized, tightly bounded real Mounted Source dispatch/intake proof. It should verify:

- dispatch revalidation selects Profile `2` exactly;
- Source Intake and IngestionRun carry the selected Profile ID;
- runtime `from_path` and provenance root use the verified container Runtime Root;
- source-relative paths remain relative to that Runtime Root;
- Source files remain read-only;
- Vault/provenance/idempotency guarantees remain exact;
- rollback and stop conditions are defined before dispatch.

Mounted Local should remain deferred until the `family` identity/mapping failure is separately reconciled or another root is explicitly approved. 12.65.5 must not use NAS dispatch to infer Local correctness.

## 25. Git Status

Evidence: **confirmed repository** after closeout creation.

Expected final short status:

```text
?? docs/milestones/milestone_012/012.065_deployment_stabilization_clean_linux_main/12.65.4_frontend_validation_and_development_activation_closeout.md
```

Ordinary `git diff --stat` and `git diff --check` do not inspect this newly created untracked closeout. The closeout was not staged merely for validation. A read-only no-index whitespace check was used separately, with the expected no-index difference status distinguished from whitespace-error output.

No commit or push was performed.
