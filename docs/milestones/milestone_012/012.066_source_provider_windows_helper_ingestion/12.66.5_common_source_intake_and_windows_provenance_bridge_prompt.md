# Milestone 12.66.5 — Common Source Intake and Windows Provenance Bridge

## Required Files

Prompt:

docs/milestones/milestone_012/012.066_source_provider_windows_helper_ingestion/12.66.5_common_source_intake_and_windows_provenance_bridge_prompt.md

Closeout:

docs/milestones/milestone_012/012.066_source_provider_windows_helper_ingestion/12.66.5_common_source_intake_and_windows_provenance_bridge_closeout.md

Create exactly one closeout.

Do not create separate reconnaissance, architecture, transcript, or coder-response documents.

---

# 1. Purpose

Reconnect the verified Windows Helper acquisition boundary to the existing authoritative common Source Intake.

12.66.4 proved:

Windows Source
→ authenticated Helper acquisition
→ durable SourceAcquisitionRun / SourceAcquisitionItem
→ verified Linux receiving objects

12.66.5 must now prove:

verified ready acquisition objects
→ existing common Source Intake
→ existing Vault rules
→ existing Asset behavior
→ existing exact-duplicate behavior
→ existing Provenance behavior

There must remain only one ingestion engine.

The Windows Helper acquisition layer supplies verified input objects and Windows-native lineage.

The existing common Source Intake remains authoritative for:

- Source Intake runs;
- IngestionRun;
- Drop Zone/staging behavior;
- Vault writes;
- exact-known handling;
- Asset creation/reuse;
- Provenance creation;
- duplicate behavior;
- canonicalization and ordinary pipeline behavior.

Do not reproduce those behaviors inside the Windows Helper bridge.

---

# 2. Reasoning / Implementation Style

Use High reasoning.

This milestone crosses the final boundary between provider acquisition and common ingestion.

Implement the smallest safe adapter.

Do not redesign Source Intake, Vault, deduplication, Source identity, or acquisition.

Prefer the existing explicit-candidate/record seam identified during 12.66.0 reconnaissance, including `PipelineContext.explicit_source_records` or the current repository-equivalent seam.

If integration would require bypassing the common pipeline, STOP AND ESCALATE.

---

# 3. Repository / Branch

Repository:

/home/chuck/projects/photo-organizer-dev

Required branch:

feature/windows-helper-ingestion

Preflight:

cd /home/chuck/projects/photo-organizer-dev
git branch --show-current
git status --short
git rev-parse HEAD
git rev-parse '@{upstream}'

Required:

- branch = feature/windows-helper-ingestion
- working tree clean except committed 12.66.5 prompt/addenda
- HEAD = upstream

Do not switch to or modify main.

Do not commit or push.

---

# 4. Required Reading

Read only what is needed:

1. docs/context/coding_agent_rules_v7.md
2. 12.64.1_source_location_provenance_vault_hardening_closeout.md
3. 12.66.0_windows_helper_provider_reconnaissance_closeout.md
4. 12.66.3_remote_source_creation_readiness_selection_and_bounded_inventory_closeout.md
5. 12.66.4_durable_acquisition_and_verified_receiving_closeout.md
6. current Source Intake / pipeline explicit-record seam
7. current Asset/Vault/provenance exact-known handling
8. current SourceAcquisitionRun / SourceAcquisitionItem implementation
9. this prompt

Do not repeat broad repository reconnaissance.

---

# 5. Accepted Controlled Input

Use only the retained completed acquisition run:

13b8e4cd-b6b2-4e5d-a5ec-3df04fa63000

Accepted binding:

Access Node:
95533ddc-ec5c-4678-b3c6-687f2ddbf33e

Endpoint:
2
alias = Chuck_Notebook

Profile:
3
name = 12.66.3 Windows Local Test Media

Windows provider-native root:

C:\Users\chhen\OneDrive\Pictures\Photo Organizer Test\12.66.3 Local Source

Linux receiving root:

/app/storage/acquisition/windows/13b8e4cd-b6b2-4e5d-a5ec-3df04fa63000/ready

The run contains exactly five `ready` items totaling:

10,202,674 bytes

Do not reacquire these files.

Do not re-run Windows inventory merely to perform this bridge.

No Windows Helper upgrade should be necessary for this milestone unless repository evidence proves otherwise.

---

# 6. Controlled Duplicate Set

The five acquisition items contain three unique SHA-256 contents.

Accepted evidence:

IMG_3310.JPG
→ sha256:e581118c4db26662ea86947568a640a658ab205e3c7ff2caed0ae36851bd06ec

IMG_3410 (1).JPG
IMG_3410.JPG
→ sha256:d3b00171dcb6acfced5b700a21b4a7ed84c4dcc3ec505c21bb8aa0e5f13b98b7

IMG_3423 (1).JPG
IMG_3423.JPG
→ sha256:7b71514ee76a2f18a4fb05505b0d403ad9813f733826fc8e4c02cd905bc9d2af

This is an explicit acceptance case.

The bridge must preserve:

5 distinct acquisition items
5 distinct Windows-native Source paths
3 unique file contents

without treating duplicate content as duplicate acquisition identity.

---

# 7. Locked 12.64 Provenance / Vault Rules

Preserve all accepted 12.64.1 behavior.

For this Windows Helper bridge:

SourceIntakeRun.ingestion_source_id
=

IngestionRun.ingestion_source_id
=

Provenance.ingestion_source_id
=

selected Profile ID 3

The persisted Source Profile root must not be rewritten into a Linux receiving path.

Runtime evidence is separate from durable Source identity.

For the common Source Intake execution:

IngestionRun.from_path
=

current Linux receiving Runtime Root

Provenance.source_root_path
=

that same Linux receiving Runtime Root

Provenance.source_relative_path
=

candidate runtime path relative to that Runtime Root

Provenance.source_path
=

repository-consistent runtime receiving path

Do NOT place Windows-native paths into these locked runtime Provenance fields.

---

# 8. Windows-Native Lineage

Windows-native lineage remains durable on `SourceAcquisitionItem`:

provider_native_root
provider_native_relative_path
provider_native_full_path

That lineage must remain linked to the common-ingestion result.

The final durable relationship must allow:

SourceAcquisitionItem
→ Asset
→ Provenance

or the repository-consistent equivalent that provides the same auditability.

For every successfully bridged acquisition item, it must be possible to determine:

- exact acquisition run/item;
- exact Windows-native original path;
- resulting Asset;
- resulting Provenance;
- common Source Intake / Ingestion run that processed it.

Prefer adding minimal provider-neutral bridge/link state to the acquisition layer rather than embedding Windows-specific fields into Asset or Provenance.

Do not duplicate the Windows-native path into multiple unrelated tables merely for convenience.

---

# 9. Asset.original_source_path

This milestone must establish the distinction identified in 12.66.0:

runtime_candidate_path
≠
asset_original_source_path

For a newly created Asset originating from a Windows Helper acquisition:

Asset.original_source_path
=

that acquisition item's provider_native_full_path

NOT:

Linux receiving path
opaque ready filename
Drop Zone path
Vault path

The explicit common-pipeline candidate record may be extended minimally to carry a separate `asset_original_source_path` or exact repository-equivalent field.

Do not make Source Intake infer the Windows path from the Linux receiving filename.

---

# 10. Existing Asset Rule

If the SHA-256 is already represented by an existing canonical Asset before this run:

- reuse the existing Asset under normal exact-known behavior;
- do NOT rewrite `Asset.original_source_path`;
- do NOT replace it with the current Windows path;
- do NOT create a second canonical Asset;
- do NOT create an alternate Vault object merely because the provider-native filename/path differs.

The acquisition item must still link to the reused Asset and its legitimate new Provenance result.

---

# 11. Deterministic First-Creation Path

If a unique SHA in the controlled run is new to the database and multiple acquisition items share that SHA, process acquisition items in their durable acquisition ordinal/order.

The first item for that SHA that creates the Asset establishes `Asset.original_source_path`.

Later exact-duplicate items reuse that Asset and must not rewrite it.

Do not select an original path based on prettier filename, lexical preference, `(1)` removal, or other heuristic.

`Asset.original_source_path` is first-creation evidence, not canonical duplicate ranking.

---

# 12. Pre-Bridge Verification

Before invoking Source Intake, revalidate every selected `ready` acquisition item on Linux.

Required:

- acquisition run state = completed;
- item state = ready;
- exact AccessNode / Endpoint / Profile binding;
- exact provider-native lineage still present;
- ready path derived only from server-side acquisition state;
- ready object exists;
- ready object is a regular non-symlink file;
- ready path remains beneath the exact run ready root;
- file size equals verified acquisition byte count;
- Linux SHA-256 re-hash equals the persisted Linux-verified SHA-256.

If any ready object is missing, changed, unsafe, or hash-mismatched:

STOP before Source Intake.

Do not repair it.

Do not reacquire automatically.

Do not allow the common pipeline to ingest damaged receiving evidence.

---

# 13. Common Source Intake Adapter

Create the smallest provider-neutral bridge from verified acquisition items into existing Source Intake.

The bridge must provide common Source Intake with exact explicit candidates only.

Do not recursively scan the acquisition receiving directory.

Do not ingest arbitrary files merely because they are present beneath the ready root.

Candidate order should follow durable acquisition item order.

Each explicit candidate must distinguish at minimum:

runtime candidate path:
Linux verified ready object

asset original source path:
Windows provider-native full path

selected ingestion Source/Profile:
Profile ID 3

provider/acquisition linkage:
exact SourceAcquisitionItem

Do not create a Windows-specific ingestion engine.

---

# 14. Runtime Root

For this controlled bridge, the common Source Intake Runtime Root is:

/app/storage/acquisition/windows/13b8e4cd-b6b2-4e5d-a5ec-3df04fa63000/ready

or the exact container-visible repository-resolved equivalent.

`IngestionRun.from_path` must record that Runtime Root.

Each Provenance runtime path must resolve beneath that root.

The persisted Profile root remains the Windows provider-native Source root and must remain unchanged.

---

# 15. Opaque Receiving Filenames

The ready receiving objects use application-controlled opaque names.

That is acceptable.

Therefore:

Provenance.source_relative_path
may be an opaque receiving-object relative path

while:

SourceAcquisitionItem.provider_native_relative_path
retains the actual Windows Source relative path.

Do not rewrite provenance runtime evidence to make it look prettier.

The acquisition-item linkage is what preserves original Windows lineage.

---

# 16. Source Intake Authority

After verified candidates enter common Source Intake, use existing behavior unchanged for:

- Drop Zone/staging;
- candidate hashing;
- Vault routing;
- canonical Asset creation;
- exact-known handling;
- duplicate handling;
- canonicalization;
- ordinary successful Provenance creation;
- pipeline reporting.

Do not use the acquisition SHA to bypass common Source Intake hashing/Vault authority unless the existing pipeline already accepts it strictly as non-authoritative precomputed evidence.

Linux Source Intake remains responsible for its own integrity decisions.

Do not trust acquisition hashes as a substitute for existing intake verification.

---

# 17. Exact Duplicate Behavior

For two different acquisition items with the same content hash:

- both remain distinct Source acquisition observations;
- one canonical Asset is used;
- one canonical Vault object is used;
- each legitimate distinct Source path receives its own Provenance relationship;
- the second duplicate must not create an orphan/alternate Vault object;
- the second duplicate must not overwrite first-origin Asset path evidence.

This must be explicitly tested.

---

# 18. Existing Exact-Known Behavior

Before live intake, perform a read-only classification of the three unique controlled SHA-256 values against current Asset/Vault state.

For each unique SHA classify:

- new content; or
- exact-known content.

For exact-known content also safely verify the canonical Vault object under existing 12.64 rules.

Do not mutate during this preflight.

Present the expected Asset/Vault deltas before live Source Intake.

Do not assume all three hashes are new.

---

# 19. Expected Live Delta Model

Let:

N = number of the three unique SHA-256 values that are not already known before intake.

Expected first-run canonical deltas:

Assets:
+N

canonical Vault objects:
+N

Successful Provenance:

expected +5 if no equivalent Profile/runtime Provenance already exists for these five acquisition items.

Source Intake runs:
+1

Ingestion runs:
+1

The exact expected counts must be calculated from read-only preflight evidence before execution.

If actual preflight state contradicts these assumptions, STOP and report the expected corrected delta.

---

# 20. Provenance Per Acquisition Item

Each of the five acquisition items represents a distinct approved Windows Source path.

Even when two items contain identical bytes, they remain distinct lineage observations.

The successful bridge must therefore preserve a one-to-one audit path from each successfully processed acquisition item to its resulting legitimate Provenance record.

Do not collapse:

IMG_3410 (1).JPG
and
IMG_3410.JPG

into one acquisition lineage record merely because their SHA is equal.

Likewise for:

IMG_3423 (1).JPG
and
IMG_3423.JPG

---

# 21. Bridge Linkage / Schema Boundary

Use the smallest provider-neutral durable linkage.

Authorized options include:

- minimal additive bridge/result fields on SourceAcquisitionRun/Item; or
- a narrow provider-neutral acquisition-to-intake linkage table if genuinely required for one-to-many run audit.

Required result:

run → common intake run(s)
item → Asset
item → Provenance

Do not add Windows-specific columns to core Asset or Provenance tables.

Do not alter the locked meaning of existing provenance path fields.

If durable linkage requires broad Source Intake/Provenance schema redesign, STOP AND ESCALATE.

---

# 22. Bridge Idempotency

The same completed acquisition run must be safely bridgeable again for idempotency validation.

A repeated bridge may create a new Source Intake run / IngestionRun under existing run semantics.

It must NOT create:

- new Assets for already known hashes;
- new canonical Vault objects;
- duplicate Provenance for the same acquisition-item/runtime observation;
- duplicate acquisition-to-Asset/Provenance linkage.

The second bridge must resolve to the same Assets/Provenance records or the repository-consistent equivalent.

Do not delete the first run merely to make repeat testing easier.

---

# 23. Acquisition State Preservation

Common Source Intake must not rewrite or destroy the acquisition evidence.

After successful bridge:

SourceAcquisitionRun remains retained.

SourceAcquisitionItems remain retained.

Ready receiving objects remain retained.

Provider-native lineage remains retained.

Linux verified acquisition hashes remain retained.

Do not clean successful receiving objects in 12.66.5.

Receiving retention/cleanup policy belongs to later work.

---

# 24. No Windows Re-Access

12.66.5 should operate entirely from the verified Linux receiving state.

Do not:

- open the original Windows Source;
- issue a new `acquire_item`;
- require Windows Helper `serve`;
- re-pair;
- upgrade the Helper;
- re-inventory the Source;

merely to perform this bridge.

The purpose is to prove that already-verified acquisition output can enter common Source Intake independently.

---

# 25. Selection / can_run_source_intake

Do not prematurely redefine Profile-level Source Selection as directly runnable from a Windows path.

The Source remains:

workflow_kind = windows_helper_intake

and no fabricated Linux resolved Source root is permitted.

If a bridge-specific state is needed, express it as:

completed acquisition run is ready for common Source Intake

rather than claiming the Windows Profile itself is directly mounted/runnable.

Do not weaken the existing `resolved_source_root = null` semantics for pre-acquisition Windows selection.

12.66.6 will own full end-to-end orchestration.

---

# 26. API Boundary

Add only the minimum trusted backend/admin operation needed to invoke or inspect the acquisition-to-Source-Intake bridge.

Conceptually:

plan/validate bridge
execute bridge
inspect bridge result

Use repository conventions.

No new Helper-ingress endpoint should be necessary.

Do not expose this mutation through the Helper credential surface.

The Helper credential must still have no Asset/Vault/Provenance authority.

---

# 27. No Frontend Work

No frontend implementation is required.

Use bounded admin/API/service invocation for controlled validation.

User-facing orchestration remains for later milestones.

If frontend code appears necessary for core correctness, STOP and report why.

---

# 28. Automated Tests

At minimum cover:

### Bridge input integrity

- only completed acquisition run accepted;
- only ready items accepted;
- missing ready object rejected;
- size mismatch rejected;
- SHA mismatch rejected;
- symlink/non-regular ready object rejected;
- receiving-root escape impossible;
- detached arbitrary Linux path impossible.

### Path semantics

- runtime candidate path is Linux ready path;
- runtime root is ready directory;
- Windows-native source path remains separate;
- no POSIX normalization of Windows-native source path;
- persisted Profile root unchanged.

### Asset original path

- newly created Asset receives Windows-native full path;
- receiving path is never written as new Asset original source path;
- exact-known Asset original path remains unchanged;
- duplicate candidate cannot rewrite first-created Asset path.

### Duplicate behavior

Synthetic equivalent of:

five source paths
→ three unique hashes

must produce the expected canonical Asset/Vault behavior while preserving five legitimate lineage observations.

### Exact-known / Vault

- exact-known candidate does not invoke ordinary Vault copy against existing canonical file;
- damaged/missing canonical Vault fails candidate safely;
- no repair/overwrite;
- no successful Provenance on the failed candidate;
- different filename/path does not create alternate orphan Vault object.

Reuse existing 12.64 tests where possible rather than duplicating their internals.

### Linkage

- each successful acquisition item links to correct Asset;
- each successful acquisition item links to correct Provenance;
- run-to-intake linkage is durable;
- repeated bridge is idempotent.

---

# 29. Existing Regression Gates

Run focused regressions for:

12.64.1 provenance/Vault hardening
12.66.1 shared identity/protocol
12.66.2 Helper authentication/ingress
12.66.3 Source workflow/inventory
12.66.4 acquisition/receiving
Source Intake
pipeline orchestration
Vault/exact-known behavior
Source creation/readiness/selection
Mounted provider
affected iCloud/common intake contracts

Because this milestone intentionally touches common Source Intake, run the relevant broader backend ingestion regression set if necessary to prove no provider regression.

Do not weaken existing tests to make the bridge pass.

---

# 30. Development Activation

Only after automated gates pass:

- apply bounded additive bridge linkage schema if required;
- rebuild backend as required;
- preserve rollback image;
- recreate only backend if needed;
- Helper ingress should not require functional change unless shared image recreation is unavoidable;
- preserve acquisition volume and all five ready objects;
- preserve PostgreSQL;
- preserve Redis;
- preserve frontend;
- preserve Test;
- preserve Production;
- preserve Endpoint 2/Profile 3;
- preserve Helper credential.

Do not use:

docker compose down

Do not recreate PostgreSQL or Redis.

---

# 31. Live Pre-Intake Gate

Before the first live common Source Intake mutation, STOP.

Perform only read-only validation.

Present:

- acquisition run ID;
- item count = 5;
- total verified bytes = 10,202,674;
- exact ready receiving root;
- all five ready-object integrity checks;
- selected Profile ID = 3;
- selected Endpoint ID = 2;
- current Source Intake run count;
- current IngestionRun count;
- current Asset count;
- current Provenance count;
- current canonical Vault file count;
- classification of each of the three unique SHA values as new or exact-known;
- expected first-run Asset delta;
- expected first-run Vault delta;
- expected first-run Provenance delta;
- confirmation that `Asset.original_source_path` will use Windows-native lineage for newly created Assets;
- confirmation that runtime Provenance paths will remain Linux receiving paths;
- confirmation that no Windows Source access/reacquisition will occur.

Await explicit Product Owner approval.

Do not start Source Intake before approval.

---

# 32. Controlled Live Bridge

After approval, process only the five retained verified acquisition items through common Source Intake.

Use acquisition item ordinal order.

Do not scan unrelated acquisition storage.

Record:

- Source Intake Run ID;
- Ingestion Run ID;
- five item outcomes;
- resulting Asset IDs;
- resulting Provenance IDs;
- canonical Vault outcomes;
- acquisition-item linkage.

No item may bypass common Source Intake merely because its acquisition SHA is already known.

---

# 33. Live Duplicate Acceptance

Explicitly verify the two exact-duplicate pairs.

For:

IMG_3410 (1).JPG
IMG_3410.JPG

both must resolve to the same canonical Asset and same canonical Vault content while preserving two distinct acquisition-path/provenance observations.

For:

IMG_3423 (1).JPG
IMG_3423.JPG

the same requirement applies.

For every newly created Asset, verify its `original_source_path` is the Windows-native full path of the first acquisition item that created it.

For exact-known pre-existing Assets, verify `original_source_path` remained unchanged.

---

# 34. Live Provenance Acceptance

For all five successful items verify:

Provenance.ingestion_source_id = 3

Provenance.source_root_path
=

Linux ready Runtime Root

Provenance.source_relative_path
=

that item's opaque ready-object path relative to Runtime Root

Provenance runtime path fields must not contain the Windows Source root.

Separately verify the linked SourceAcquisitionItem retains:

provider_native_root
provider_native_relative_path
provider_native_full_path

so the Windows lineage is recoverable from the final Asset/Provenance audit path.

---

# 35. Live Vault Acceptance

For every unique content hash:

If new:

- exactly one canonical Asset/Vault content result under ordinary pipeline behavior.

If exact-known:

- existing canonical Asset/Vault reused;
- canonical Vault object verified under normal exact-known rules;
- no alternate/orphan Vault object;
- Asset original path unchanged.

For intra-run duplicates:

- second occurrence must not create a second canonical Vault object.

Do not deliberately corrupt the live Vault for negative testing.

Use automated 12.64 regression evidence for destructive conflict cases.

---

# 36. Repeat Bridge Validation

After the first bridge is accepted and state is captured, run the same completed acquisition run through the bridge a second time.

Expected:

- new Source Intake / Ingestion run rows may be created under existing run semantics;
- Asset count delta = 0;
- canonical Vault object delta = 0;
- Provenance delta = 0;
- acquisition item → Asset linkage unchanged;
- acquisition item → Provenance linkage unchanged;
- ready receiving files unchanged;
- Windows-native lineage unchanged.

If duplicate Provenance or Asset/Vault state appears, STOP and report.

---

# 37. Receiving Preservation

After both runs:

Do not delete:

/app/storage/acquisition/windows/13b8e4cd-b6b2-4e5d-a5ec-3df04fa63000/ready

Do not delete acquisition run/items.

Do not clean successful receiving state.

12.66.6 may use this evidence while validating full end-to-end Windows Local orchestration.

---

# 38. Runtime / Data Preservation

After live validation confirm:

backend healthy
Helper ingress healthy
frontend unchanged/healthy
PostgreSQL same container/volume
Redis same container/volume
Test unchanged
Production untouched
Mounted provider infrastructure unchanged
Helper credential unchanged
Endpoint 2/Profile 3 unchanged
acquisition run/items retained
ready receiving files retained

Also report exact before/after counts for:

Source Intake runs
Ingestion runs
Assets
Provenance rows
Vault files
SourceAcquisitionRun
SourceAcquisitionItem

Explain every delta.

---

# 39. Escalation Conditions

Stop and report:

STATUS: ESCALATION REQUIRED

if:

- common Source Intake cannot accept explicit verified candidates without a second ingestion engine;
- Windows paths would need to replace locked Provenance runtime-path semantics;
- Linux receiving paths would be written as new Asset original source paths;
- exact-known reuse would rewrite existing Asset original path;
- duplicate content would collapse distinct acquisition lineage;
- one acquisition item cannot be durably linked to its Asset/Provenance result;
- bridge requires Helper Vault/database authority;
- ready receiving data must be moved/destructively consumed;
- acquisition SHA must be trusted instead of common Source Intake verification;
- a fabricated Windows→Linux Source path becomes necessary;
- Source identity/Profile root must be rewritten;
- existing 12.64 Vault protections must be weakened;
- PostgreSQL/Redis/Test/Production require destructive mutation.

Use:

STATUS: ESCALATION REQUIRED

Gate:
Observed state:
Expected state:
Intake implication:
Provenance implication:
Vault/Asset implication:
Acquisition-lineage implication:
Why continuation is unsafe:
Smallest next decision required:
Recommended Product Owner action:

Do not patch around a provenance or Vault integrity boundary.

---

# 40. Required Closeout

Create exactly:

docs/milestones/milestone_012/012.066_source_provider_windows_helper_ingestion/12.66.5_common_source_intake_and_windows_provenance_bridge_closeout.md

Use:

## 1. Repository State

## 2. Implementation Summary

## 3. Acquisition-to-Source-Intake Bridge

## 4. Explicit Candidate Mapping

## 5. Runtime Root / Provenance Semantics

## 6. Windows-Native Lineage

## 7. Asset.original_source_path Behavior

## 8. Bridge Linkage Model

## 9. Ready-Object Reverification

## 10. Common Source Intake Authority

## 11. Exact-Known / Vault Behavior

## 12. Intra-Run Duplicate Behavior

## 13. Bridge Idempotency

## 14. Schema Changes

## 15. Automated Tests

## 16. Existing Regression Results

## 17. Development Activation

## 18. Live Pre-Intake Classification

## 19. Product Owner Approval

## 20. First Live Source Intake

## 21. Live Asset Results

## 22. Live Provenance Results

## 23. Live Vault Results

## 24. Duplicate-Pair Results

## 25. Acquisition-to-Asset/Provenance Audit

## 26. Repeat Bridge Result

## 27. Receiving / Acquisition Preservation

## 28. Runtime / Data Preservation

## 29. Exact Files Changed

## 30. Deviations

## 31. Known Limitations / Deferred Work

## 32. Git Status

## 33. Recommendation for 12.66.6

End with exactly one:

READY FOR 12.66.6

or:

12.66.5 NOT COMPLETE

---

# 41. Definition of Done

12.66.5 is complete when:

- only verified `ready` acquisition objects can enter the bridge;
- ready objects are reverified before common Source Intake;
- explicit candidates are used rather than scanning arbitrary receiving storage;
- common Source Intake remains the single authoritative ingestion engine;
- selected ingestion Source/Profile remains Profile ID 3;
- `IngestionRun.from_path` records the Linux receiving Runtime Root;
- Provenance runtime path fields record Linux receiving evidence under 12.64 rules;
- Windows-native root/relative/full path remains separately preserved on acquisition lineage;
- newly created `Asset.original_source_path` uses Windows-native full path;
- exact-known Assets retain their existing original source path unchanged;
- every successful acquisition item is durably traceable to its Asset and Provenance;
- five distinct Windows Source paths remain five distinct lineage observations;
- the three unique content hashes produce only the canonical Asset/Vault results allowed by existing exact-known behavior;
- both exact-duplicate pairs reuse canonical content correctly;
- no orphan/alternate duplicate Vault objects are created;
- damaged/missing canonical Vault automated regressions remain fail-closed;
- first live bridge produces exactly the preflight-predicted Asset/Vault/Provenance deltas;
- repeat bridge produces zero new Asset/Vault/Provenance state;
- acquisition run/items and ready objects remain preserved;
- no Windows Source access/reacquisition is required;
- no frontend work is required;
- PostgreSQL/Redis/Test/Production remain preserved;
- focused/common ingestion regressions pass;
- exactly one closeout is created;
- coder does not commit or push.

If complete, proceed to:

12.66.6 — Controlled Windows Local End-to-End Validation

12.66.6 will validate the complete operator-visible Local workflow from the retained Windows Helper trust boundary through Source readiness, inventory, acquisition, verified receiving, and common Source Intake as one controlled end-to-end sequence, without changing the provider architecture proven in 12.66.1–12.66.5.
