# Milestone 12.62.28.5 - Backfill Cleanup Readiness + Ambiguous Inventory Investigation Report

## 1. Cleanup Readiness Findings

Source `66` / `Chuck iCloud E2E Test v3` has an existing guarded iCloud staging cleanup mechanism:

- readiness: `GET /api/admin/icloud-staging-cleanup/readiness?source_id=66`
- dry run: `POST /api/admin/icloud-staging-cleanup/run` with `dry_run=true`
- actual execution: `POST /api/admin/icloud-staging-cleanup/execute`, requiring a fresh dry-run id and explicit confirmation phrase

The dry-run planner validates that cleanup is scoped to the registered source root:

```text
storage/exports/icloud/chuck_icloud_e2e_test_v3
```

The dry run completed successfully and found one eligible local staging copy:

```text
run_id = 132
status = completed
dry_run = true
eligible_count = 1
deleted_count = 0
skipped_count = 0
total_files = 1
total_bytes_eligible = 1982778
report_path = storage/logs/icloud_cleanup_reports/icloud_cleanup_20260702_042510_run132.json
```

The eligible file was:

```text
2026/02/04/IMG_5235.HEIC
```

The dry-run report verified:

- staged SHA-256 matched the Asset SHA-256,
- Vault path was present and under the configured Vault root,
- Vault SHA-256 matched the Asset SHA-256,
- no skipped/protected/verification-failed files were reported.

## 2. Cleanup Dry-Run Command

Executed with product-owner approval:

```powershell
$baseUrl = "http://localhost:8001"
$body = @{ source_id = 66; dry_run = $true } | ConvertTo-Json
$start = Invoke-RestMethod -Method Post -Uri "$baseUrl/api/admin/icloud-staging-cleanup/run" -ContentType "application/json" -Body $body
```

The cleanup dry-run writes DB cleanup-run/report evidence only. It does not delete files.

## 3. Cleanup/Delete Performed

No actual cleanup/delete was performed.

The cleanup execute endpoint was not called.

The staged file still existed after the dry run:

```text
storage/exports/icloud/chuck_icloud_e2e_test_v3/2026/02/04/IMG_5235.HEIC
```

## 4. `backfill_execute` Folder Finding

`storage/exports/icloud/backfill_execute` is present but empty.

It is not the registered source root for source `66`, and it was not included in cleanup dry-run `132`. The dry-run report source root was:

```text
storage/exports/icloud/chuck_icloud_e2e_test_v3
```

Recommended disposition: leave `backfill_execute` untouched in this milestone. Treat it as unrelated leftover/dev staging evidence unless a later cleanup milestone explicitly reviews empty staging folders.

## 5. `.partial` Folder Finding

`storage/exports/icloud/chuck_icloud_e2e_test_v3/.partial` contains an old empty directory tree from 2026-06-28.

Read-only inspection found zero files under `.partial`. The cleanup planner scans files and symlinks, not empty directories, so the dry run did not report the empty `.partial` directories.

Current partial-workspace blockers count files under `.partial`; with zero files, this does not appear to be an active partial-download blocker.

Recommended disposition: do not delete manually in this milestone. If cleanup execution is approved, expect it to delete only the eligible staged file, not empty `.partial` directories.

## 6. Ambiguous/Unsupported Row Count Summary

Read-only DB inspection of source `66` found 41 non-eligible inventory rows:

```text
total non-eligible rows = 41
identity_ambiguous = 41
unsupported_reasons_json = ["unsupported_adjusted_resource"] for all 41
primary_content_type = public.heic for all 41
backfill_completed = false for all 41
acquisition_state = null for all 41
```

Summary:

| reason/type | count | recommended disposition |
| --- | ---: | --- |
| adjusted HEIC, single resource | 28 | `unsupported_by_design` for acquisition until adjusted-resource policy is defined |
| adjusted HEIC, Live Photo grouping, two resources | 13 | `needs_live_photo_pairing_fix` / adjusted Live Photo policy before acquisition |

Other groupings checked:

| grouping | count |
| --- | ---: |
| `primary_asset_explicit` | 28 |
| `live_photo_explicit` | 13 |

| resource_count | count |
| --- | ---: |
| 1 | 28 |
| 2 | 13 |

No rows in this set appeared to be AAE-only sidecars, missing-identity rows, orphaned MOV-only rows, or unsupported-extension rows based on stored inventory metadata.

## 7. Ambiguous/Unsupported Row Sample

| inventory_id | primary_relative_path | extension | resource_count | live/photo pair candidate | eligibility_state | known_state | reason/category | recommended disposition |
| ---: | --- | --- | ---: | --- | --- | --- | --- | --- |
| 52 | `2026/06/13/IMG_6247.HEIC` | `.heic` | 1 | false | `ambiguous_metadata_only` | `pending_known_state_check` | `unsupported_adjusted_resource` | `unsupported_by_design` |
| 73 | `2026/06/13/IMG_6226.HEIC` | `.heic` | 1 | false | `ambiguous_metadata_only` | `pending_known_state_check` | `unsupported_adjusted_resource` | `unsupported_by_design` |
| 96 | `2026/06/11/IMG_6200.HEIC` | `.heic` | 1 | false | `ambiguous_metadata_only` | `pending_known_state_check` | `unsupported_adjusted_resource` | `unsupported_by_design` |
| 102 | `2026/06/11/IMG_6194.HEIC` | `.heic` | 1 | false | `ambiguous_metadata_only` | `pending_known_state_check` | `unsupported_adjusted_resource` | `unsupported_by_design` |
| 104 | `2026/06/11/IMG_6192.HEIC` | `.heic` | 1 | false | `ambiguous_metadata_only` | `pending_known_state_check` | `unsupported_adjusted_resource` | `unsupported_by_design` |
| 120 | `2026/06/11/IMG_6176.HEIC` | `.heic` | 1 | false | `ambiguous_metadata_only` | `pending_known_state_check` | `unsupported_adjusted_resource` | `unsupported_by_design` |
| 130 | `2026/06/11/IMG_6166.HEIC` | `.heic` | 1 | false | `ambiguous_metadata_only` | `pending_known_state_check` | `unsupported_adjusted_resource` | `unsupported_by_design` |
| 379 | `2026/06/08/IMG_5913.HEIC` | `.heic` | 2 | true | `ambiguous_metadata_only` | `pending_known_state_check` | `unsupported_adjusted_resource` | `needs_live_photo_pairing_fix` |
| 381 | `2026/06/08/IMG_5911.HEIC` | `.heic` | 2 | true | `ambiguous_metadata_only` | `pending_known_state_check` | `unsupported_adjusted_resource` | `needs_live_photo_pairing_fix` |
| 382 | `2026/06/08/IMG_5910.HEIC` | `.heic` | 2 | true | `ambiguous_metadata_only` | `pending_known_state_check` | `unsupported_adjusted_resource` | `needs_live_photo_pairing_fix` |

## 8. Recommended Disposition by Group

Adjusted HEIC, single resource:

- keep skipped for now,
- do not turn eligible in this milestone,
- decide later whether adjusted derivatives should be imported, linked, ignored, or handled as metadata/edit evidence.

Adjusted HEIC with Live Photo grouping:

- keep skipped for now,
- do not acquire until adjusted-resource and Live Photo pairing semantics are clearer,
- review helper manifest behavior before deciding whether these are safe supported Live Photo assets or adjusted derivatives.

## 9. Classifier/Reporting Fixes Made

Made three narrow reporting fixes:

1. First-scan count snapshot fix:
   - added `db_session.flush()` before `_inventory_counts(...)` in the inventory scan service,
   - updated tests to use `autoflush=False`,
   - asserted first-scan response counts newly created rows.

2. Dry-run acquire item details:
   - `POST /api/admin/icloud-backfill/acquire` with `dry_run=true` and `include_items=true` now returns non-empty safe item details when preview selected rows exist,
   - added safe fields: `logical_resource_count`, `is_live_photo`, and `primary_relative_path`.

3. Status count fields:
   - added additive status fields:
     - `backfill_completed_count`
     - `unresolved_eligible_count`
     - `acquirable_pending_count`
     - `retryable_failed_count`
     - `ambiguous_or_unsupported_count`
   - status now recomputes live inventory counts instead of relying only on stored rollup counts.

No eligibility rules were changed.

## 10. Whether `acquire_limit=5` Is Safe To Run Yet

Not yet recommended.

The `acquire_limit=1` execution succeeded, and the cleanup dry run found exactly one eligible staged file. However, the staged file has not been deleted yet because actual cleanup/delete was not approved or run in this milestone.

Recommended sequence before optional `acquire_limit=5`:

1. Review cleanup dry-run `132`.
2. If approved, run guarded cleanup execution through the existing confirmation-gated endpoint.
3. Verify staging is clean.
4. Then consider optional `acquire_limit=5`.

## 11. Remaining Blockers Before Larger Acquisition

- The completed staged file remains present until guarded cleanup execution is approved and run.
- The 41 adjusted-resource rows remain intentionally skipped pending product policy and/or helper classification refinement.
- The empty `.partial` directory tree remains present but does not appear to block current partial-file checks.
- The running backend must be restarted before the new additive status fields and `dry_run=true` item detail fix are visible through the live API.
