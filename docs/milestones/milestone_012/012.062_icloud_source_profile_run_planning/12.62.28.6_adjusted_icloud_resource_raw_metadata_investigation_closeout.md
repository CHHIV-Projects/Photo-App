# Milestone 12.62.28.6 - Adjusted iCloud Resource Raw Metadata Investigation Closeout

## Summary

Completed the adjusted iCloud resource raw metadata investigation for the 41 source `66` inventory rows carrying:

```text
identity_ambiguous = true
unsupported_reasons_json = ["unsupported_adjusted_resource"]
primary_content_type = public.heic
```

The exact code-proven trigger is:

```text
AssetVersionSize.ADJUSTED in asset.versions
```

Per-row raw helper payloads are not persisted in current inventory rows, so this investigation did not prove each stored row's raw `asset.versions` membership from local persisted payloads.

## Files Changed

Added:

- `docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.6_adjusted_icloud_resource_raw_metadata_investigation_report.md`
- `docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.6_adjusted_icloud_resource_raw_metadata_investigation_closeout.md`

Pre-existing user-authored prompt remains untracked:

- `docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.6_adjusted_icloud_resource_raw_metadata_investigation_prompt.md`

## Whether Code Changed

No product code changed.

No tests were added.

## Whether Live Helper Metadata Call Was Run

No live helper metadata call was run.

## Whether Acquisition Was Run

No acquisition was run.

## Whether Cleanup/Delete Was Run

No cleanup execution/delete was run.

## Tests Run With Exact Results

- `& '..\.venv\Scripts\python.exe' -m unittest discover -s tests -p 'test_icloud_exact_selection_helper.py' -v`
  - Result: passed, 8 tests.
  - Exact summary: `Ran 8 tests in 0.037s` / `OK`.

Additional check:

- `git diff --check`
  - Result: passed.

## Key Findings

- `unsupported_adjusted_resource` is emitted in `backend/app/services/icloud_acquisition/icloud_exact_selection_helper.py`.
- The exact code-proven condition is `if self._asset_adjusted in versions`.
- `self._asset_adjusted` is `AssetVersionSize.ADJUSTED`.
- `adjustmentType` metadata-only is explicitly non-blocking in `backend/tests/test_icloud_exact_selection_helper.py`.
- The 41 stored rows split into 28 single-resource HEIC rows and 13 Live Photo grouped rows.
- All 41 rows are stored as `ambiguous_metadata_only`, `pending_known_state_check`, `backfill_completed=false`, and `acquisition_state=null`.
- Stored inventory does not retain raw `asset.versions` keys or adjusted-version payload details.
- Existing local docs/log searches did not find captured raw helper list payloads for these 41 rows.

## Policy Recommendation

Keep adjusted-resource rows non-eligible for acquisition in v1.0 until product policy decides how adjusted downloadable provider versions should be handled.

Current evidence supports deferring acquisition. It does not support importing adjusted versions, silently ignoring the adjusted marker, changing duplicate behavior, or changing canonicalization.

## Known Limitations

- No live helper metadata listing was run.
- No per-row raw helper payload was available from persisted inventory.
- The investigation proves the code trigger and stored normalized evidence, not the raw provider payload for each of the 41 live rows.
- The one same-stem inventory match for `IMG_5472.HEIC` had a different date folder and capture timestamp, so it was classified as insufficient metadata rather than a proven counterpart.

## Recommended Next Milestone

Add an approved, redacted, metadata-only adjusted-resource diagnostic if product policy requires per-row raw provider evidence before deciding adjusted-resource handling.

The diagnostic should not download, stage, intake, clean, mark completion, expose credentials, expose signed URLs, store broad raw CloudKit payloads, or change eligibility.

## Safety Boundary Confirmation

Confirmed:

- No acquire_limit=5 validation run.
- No dry_run=false acquisition run.
- No cleanup execution/delete run.
- No eligibility rules changed.
- No Admin UI added.
- No scheduler added.
- No remote iCloud deletion added.
- No direct Vault writes added.
- No Source Intake bypass added.
- No commit/push/reset/rebase/merge/tag/checkout/stash run.

## Prompt Deviation / Path Correction

The prompt listed:

```text
12.62.28.3_historical_backfill_execution_and_source_intake_handoff_closeout.md
```

The actual repo file read was:

```text
12.62.28.3_historical_backfill_acquisition_execution_and_source_intake_handoff_closeout.md
```

## git status --short

```text
?? docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.6_adjusted_icloud_resource_raw_metadata_investigation_closeout.md
?? docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.6_adjusted_icloud_resource_raw_metadata_investigation_prompt.md
?? docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.6_adjusted_icloud_resource_raw_metadata_investigation_report.md
```

## git diff --name-only

```text
```

`git diff --name-only` is empty because the 12.62.28.6 files are untracked rather than tracked modifications.
