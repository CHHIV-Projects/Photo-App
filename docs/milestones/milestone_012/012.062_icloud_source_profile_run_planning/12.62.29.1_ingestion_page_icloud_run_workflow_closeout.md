# 12.62.29.1 Ingestion Page iCloud Run Workflow Closeout

Date: 2026-07-03

## Closeout

Implemented the requested Ingestion-page iCloud workflow and kept it focused to UI orchestration plus the two approved backend support changes.

Files changed include:

- `frontend/src/components/IcloudRunWorkflowPanel.tsx`
- `frontend/src/components/icloud-run-workflow-panel.module.css`
- `frontend/src/components/IngestionView.tsx`
- `frontend/src/components/AdminView.tsx`
- `frontend/src/lib/api.ts`
- `frontend/src/types/ui-api.ts`
- `backend/app/api/admin.py`
- `backend/app/schemas/admin.py`
- `backend/app/services/icloud_acquisition/exact_selection_protocol.py`
- `backend/tests/test_icloud_12_62_26_raise_limits.py`
- `backend/tests/test_icloud_backfill_acquisition_preview.py`
- `backend/tests/test_admin_ingestion_guardrails_api.py`

## Acceptance Notes

- Normal iCloud historical backfill workflow is exposed on Ingestion.
- New Import mode is visible but disabled as Coming Soon.
- Historical Backfill mode supports free-form positive integer limits up to backend cap `10000`.
- Candidate search cap remains `1000` for this workflow.
- Preview and Run use typed API wrappers for the existing 12.62.28.x backfill endpoints.
- Deferred/skipped detail table reads only safe deferred-asset rows.
- Admin iCloud controls remain available and are labeled legacy/internal.
- Guarded cleanup execute is attempted only after cleanup dry-run eligible relative paths exactly match just-acquired paths and safety counters are zero.
- If exact cleanup safety is not provable, the UI stops after dry run and requires review.

## Safety

No live iCloud acquisition was run. No cleanup execution was run. No Source Intake was run. No Vault writes were performed. No adjusted-resource eligibility/import behavior was changed. No scheduler, Admin UI live operation, commit, push, reset, rebase, merge, tag, checkout, or stash was performed.

## Validation

- Backend targeted tests passed: 14 + 15 + 7 tests.
- Frontend production build passed.
- Frontend lint completed with existing warnings.

## Follow-Up

The first UI run for source `66` imported `96` logical items / `98` resources, then stopped at cleanup review because the UI compared cleanup candidates against logical primary paths instead of all acquired resource paths. This is fixed by exposing `acquired_resource_paths` and using those paths for cleanup matching.

The current staging cleanup was safely completed through guarded cleanup:

- dry run `139`
- execution run `140`
- deleted local staging files: `98`
- skipped/protected/verification/file-missing/delete-failed: `0`
- source `66` staging files remaining: `0`

The cleanup authorization fingerprint now ignores volatile staged mtime while keeping hash/size/path/Vault verification and final per-file revalidation.

## Recommendation

Recommend commit after review.
