# Milestone 12.62.28.5 — Backfill Cleanup Readiness + Ambiguous Inventory Investigation

## Purpose

Investigate and document the live validation findings from 12.62.28.4 before running any larger historical backfill acquisition.

This milestone should answer two practical questions:

```text
1. Is the completed tiny acquisition ready for guarded cleanup review?
2. What exactly are the ambiguous/unsupported inventory rows that were skipped?
```

Do **not** run the optional `acquire_limit=5` validation yet.

Do **not** perform actual cleanup/delete unless I explicitly approve the exact command after reviewing the dry-run output.

---

## Required context to read first

Before doing anything, read:

```text
docs/context/CODING_AGENT_RULES.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.2_historical_backfill_inventory_acquisition_preview_closeout.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.3_historical_backfill_execution_and_source_intake_handoff_closeout.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.4_historical_backfill_execution_operator_validation_runbook.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.4_historical_backfill_execution_operator_validation_closeout.md
```

Also inspect relevant backend files:

```text
backend/app/api/admin.py
backend/app/schemas/admin.py
backend/app/models/icloud_backfill.py
backend/app/services/icloud_backfill_inventory_service.py
backend/app/services/icloud_backfill_acquisition_preview_service.py
backend/app/services/icloud_backfill_acquisition_execution_service.py
backend/app/services/icloud_backfill_schema.py
backend/app/services/icloud_acquisition/durable_exact_service.py
backend/app/services/icloud_acquisition/batch_source_intake_service.py
```

---

## Git rules

Before starting, report:

```powershell
git branch --show-current
git status --short
git log --oneline --decorate -5
```

Expected branch:

```text
feature/icloud-final-routine
```

Expected condition:

```text
12.62.28.4 committed, or only the user-authored 12.62.28.5 prompt present as untracked/modified.
```

Do **not** run write-affecting Git commands unless I explicitly instruct you.

Do not run:

```text
git commit
git push
git reset
git rebase
git merge
git tag
git checkout another branch
git stash
```

Read-only Git commands are allowed.

---

## Critical live-operation rules

Do **not** run:

```text
POST /api/admin/icloud-backfill/acquire
```

with:

```text
dry_run=false
```

in this milestone.

Do **not** run the optional `acquire_limit=5` validation.

Do **not** delete files, remove staging files, remove `.partial` folders, or run cleanup execution unless I explicitly approve the exact command after reviewing dry-run output.

Allowed:

```text
- read-only inspection
- automated tests with mocks/fakes
- API status calls
- cleanup dry-run only, if it is truly non-deleting
- preparing cleanup execution commands but not running them
- diagnostic queries/reports
```

---

## Live validation context from 12.62.28.4

The tiny live validation used:

```text
source_id = 66
source_label = Chuck iCloud E2E Test v3
managed_staging_path = storage/exports/icloud/chuck_icloud_e2e_test_v3
acquire_limit = 1
max_listing_candidates = 1000
dry_run = false
auto_run_source_intake = true
```

Observed successful execution:

```text
status = acquisition_completed
selected_inventory_count = 1
matched_listing_count = 1
downloaded_logical_count = 1
downloaded_resource_count = 1
source_intake_attempted = true
source_intake_succeeded = true
source_intake_run_id = 84
acquisition_run_id = 60
acquisition_batch_id = 36
backfill_completed_count = 1
stop_reason = source_intake_completed
next_safe_action = Run guarded iCloud cleanup dry run
```

Observed file/staging findings:

```text
storage/exports/icloud/backfill_execute
  present but empty
  not registered as the source profile path for this run

storage/exports/icloud/chuck_icloud_e2e_test_v3
  contains completed staged asset:
  2026/02/04/IMG_5235.HEIC

storage/exports/icloud/chuck_icloud_e2e_test_v3/.partial
  old directory tree observed
  inspected listing had no files
  current partial checks count files, not empty directories
```

Observed inventory/status findings:

```text
inventory_total_count = 1000
eligible_metadata_count = 959
unsupported_or_ambiguous_count = 41
source_exhausted = false
scan_limit_reached = true
stop_reason = scan_limit_reached
```

Known follow-up findings:

```text
1. Cleanup still needs guarded dry-run/review.
2. First-scan count snapshot may be stale until a second scan.
3. Status endpoint lacks separate completed/unresolved/acquirable counts.
4. acquire dry_run=true with include_items=true returned empty items despite matching counts.
5. 41 ambiguous/unsupported rows need explanation before larger runs.
```

---

## Scope

Allowed work:

```text
- create/update a runbook or investigation report
- run automated tests
- inspect cleanup dry-run path
- prepare cleanup dry-run command
- run cleanup dry-run only if it is confirmed non-deleting
- inspect filesystem state read-only
- inspect inventory rows read-only
- group ambiguous/unsupported rows by reason/type/resource pattern
- add a narrow diagnostic helper/report if necessary
- fix small reporting/count bugs if clearly in scope and covered by tests
```

Excluded work:

```text
- no acquire_limit=5 validation
- no new real acquisition run
- no dry_run=false acquire call
- no actual cleanup/delete
- no Admin UI
- no scheduler
- no automation
- no remote iCloud deletion
- no direct Vault writes
- no Source Intake bypass
- no broad durable acquisition refactor
- no broad duplicate/canonicalization redesign
```

---

## Task A — Cleanup readiness investigation

Investigate the cleanup state after the successful `acquire_limit=1` run.

Answer:

```text
1. What cleanup mechanism already exists for iCloud staged files?
2. Is there an existing cleanup dry-run endpoint/command/report?
3. Can it target source_id=66 / acquisition_run_id=60 / acquisition_batch_id=36 safely?
4. What would it delete if executed?
5. Does it recognize the completed staged file:
   storage/exports/icloud/chuck_icloud_e2e_test_v3/2026/02/04/IMG_5235.HEIC
6. Does it report the old empty .partial tree?
7. Does it ignore or report the unrelated empty:
   storage/exports/icloud/backfill_execute
8. Is actual cleanup currently safe to perform after review?
```

Important:

```text
Do not execute cleanup/delete.
Dry-run only, unless I explicitly approve a follow-up cleanup execution command.
```

If no cleanup dry-run path exists, stop and report the gap. Do not invent destructive cleanup behavior in this milestone.

---

## Task B — Ambiguous/unsupported inventory investigation

Investigate the 41 skipped inventory rows for source `66`.

Use read-only DB/API inspection or a narrow diagnostic helper.

Group the 41 rows by:

```text
eligibility_state
known_state
media classification
unsupported/ambiguous reason
primary relative path extension
resource_count
Live Photo flag / grouping flag
presence of sidecar-like resource, such as .AAE, .XMP, .json
presence of video/MOV resource
duplicate helper identity or duplicate remote_identity pattern, if visible safely
missing identity fields
missing/partial resource manifest
```

Produce a count summary such as:

```text
reason/type                         count
AAE/edit sidecar only                N
orphaned MOV                         N
partial Live Photo manifest          N
duplicate helper identity            N
unknown helper resource relationship N
unsupported extension                N
other                                N
```

Also include a small safe sample table, max 10 rows:

```text
inventory_id
primary_relative_path
extension
resource_count
is_live_photo_or_pair_candidate
eligibility_state
known_state
reason/category
recommended disposition
```

Do **not** expose credentials, tokens, raw Apple account identifiers, signed URLs, or sensitive helper internals.

Helper item IDs should not be printed unless already treated as safe local diagnostic IDs in this repo. Prefer local inventory IDs and relative paths.

---

## Ambiguous row decision framework

Classify each group into one of:

```text
safe_to_ignore_sidecar
safe_supported_after_classifier_fix
needs_live_photo_pairing_fix
needs_helper_manifest_fix
unsupported_by_design
needs_manual_review
unknown_needs_more_data
```

Examples:

```text
.AAE:
  likely edit-instruction sidecar
  should not be imported as a standalone photo
  may need to be preserved/related later, but should not block asset import

orphaned MOV:
  may be standalone video or broken Live Photo companion
  needs classification before skipping permanently

partial Live Photo:
  should remain retryable/unsafe until both still and motion resources are safely linked

unknown extension/resource:
  unsupported by design unless product owner approves support
```

Do not change acquisition eligibility rules broadly unless the fix is obvious and tests prove it safe.

Stop and ask before turning ambiguous rows into eligible acquisition rows.

---

## Task C — First-scan count snapshot defect

Investigate the reported first-scan count issue:

```text
First inventory scan response reported last_scan_created_count=1000
but inventory_total_count=0 and eligible_metadata_count=0.
Second scan reported expected totals.
```

Likely cause from closeout:

```text
counting before pending ORM rows are flushed when autoflush=False
```

Allowed fix if confirmed:

```text
add a targeted db_session.flush() before _inventory_counts(...)
add a regression test proving first-scan response counts newly-created rows correctly
```

Do not broaden inventory scan behavior beyond this count/reporting fix.

---

## Task D — dry_run=true include_items gap

Investigate:

```text
POST /api/admin/icloud-backfill/acquire
dry_run=true
include_items=true
```

returned matching counts but an empty `items` array.

Allowed fix if narrow:

```text
carry preview item details through the dry_run=true acquire response
or clearly document that acquire dry_run=true is count-only
```

Preferred behavior:

```text
dry_run=true acquire should mirror acquire-preview item details when include_items=true
```

Add/update tests if changed.

---

## Task E — Status count gap

Investigate whether the status endpoint can cheaply expose separate counts:

```text
inventory_total_count
eligible_metadata_count
backfill_completed_count
unresolved_eligible_count
acquirable_pending_count
retryable_failed_count
ambiguous_or_unsupported_count
```

Allowed implementation only if narrow and low-risk.

Do not redesign the status API.

If adding these counts is larger than expected, document as 12.62.28.6 follow-up instead.

---

## Tests to run

Run relevant tests after any code change:

```text
test_icloud_backfill_inventory.py
test_icloud_backfill_acquisition_preview.py
test_icloud_backfill_acquisition_execute.py
test_icloud_12_62_26_raise_limits.py
test_icloud_new_count_planner.py
test_icloud_exact_selection_adapter.py
test_icloud_durable_exact_service.py
test_icloud_batch_source_intake_handoff.py
test_admin_internal_icloud_run_api.py
```

If adding a diagnostic/test file, name it clearly, for example:

```text
backend/tests/test_icloud_backfill_ambiguous_inventory_diagnostics.py
```

All automated tests should use mocks/fakes or read-only local fixture data.

No live iCloud automated tests.

---

## Required report document

Create:

```text
docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.5_backfill_cleanup_readiness_ambiguous_inventory_investigation_report.md
```

The report must include:

```text
1. Cleanup readiness findings
2. Cleanup dry-run command prepared or run
3. Whether any cleanup/delete was performed
4. backfill_execute folder finding
5. .partial folder finding
6. Ambiguous/unsupported row count summary
7. Ambiguous/unsupported row sample table
8. Recommended disposition by group
9. Whether any classifier/reporting fix was made
10. Whether acquire_limit=5 is safe to run yet
11. Remaining blockers before larger acquisition
```

---

## Closeout document

Create:

```text
docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.5_backfill_cleanup_readiness_ambiguous_inventory_investigation_closeout.md
```

The closeout must include:

```text
summary
files changed
cleanup readiness findings
ambiguous inventory findings
code changes, if any
tests run with exact results
whether cleanup dry-run was run
whether actual cleanup/delete was run
whether any live iCloud acquisition was run
whether acquire_limit=5 was run
safety boundary confirmation
known limitations
recommended next milestone
git status --short
git diff --name-only
```

Explicitly state:

```text
No acquire_limit=5 validation run.
No dry_run=false acquisition run.
No actual cleanup/delete unless product-owner approval was given.
No Admin UI added.
No scheduler added.
No remote iCloud deletion added.
No direct Vault writes added.
No Source Intake bypass added.
No commit/push/reset/rebase/merge/tag/checkout/stash run.
```

---

## Stop conditions

Stop and ask before continuing if:

```text
cleanup dry-run is not clearly non-destructive
actual cleanup/delete appears necessary
ambiguous rows appear to include real supported assets being skipped
Live Photo grouping appears broken
diagnostics require exposing sensitive helper data
status count fixes require broad API redesign
first-scan count fix requires broader transaction changes
include_items fix requires large response schema changes
tests require live iCloud
```

---

## Required final response

After completing the investigation and any allowed small fixes, return:

```text
1. concise summary
2. files changed
3. cleanup findings
4. ambiguous inventory findings
5. tests run and exact pass/fail counts
6. whether cleanup dry-run was run
7. whether actual cleanup/delete was run
8. whether any dry_run=false acquisition was run
9. git status --short
10. git diff --name-only
11. whether you recommend commit
12. whether you recommend running acquire_limit=5 yet
```

Do not commit.

Do not push.

Do not run acquire_limit=5.

Do not run actual cleanup/delete without explicit approval.

Approved: you may run the existing cleanup dry run for `source_id=66`.

Approved command class:

```text
POST /api/admin/icloud-staging-cleanup/run
dry_run=true
source_id=66
```

Do not run the cleanup execute endpoint.

Do not delete files.

Do not run any endpoint requiring a confirmation phrase.

Do not run `acquire_limit=5`.

Do not run any `dry_run=false` acquisition.

## Cleanup dry-run expectations

Please confirm/report:

```text
1. Cleanup scans only the registered source root:
   storage/exports/icloud/chuck_icloud_e2e_test_v3

2. Cleanup does not touch:
   storage/exports/icloud/backfill_execute

3. Dry run reports whether this completed staged asset is eligible for cleanup:
   storage/exports/icloud/chuck_icloud_e2e_test_v3/2026/02/04/IMG_5235.HEIC

4. Dry run reports, ignores, or does not see the old empty .partial directory tree.
   If empty directories are not scanned because the planner only scans files/symlinks, document that clearly.

5. Dry run writes only DB cleanup-run/report evidence and performs no deletion.
```

## First-scan count bug

Approved as a narrow fix if confirmed:

```text
db_session.flush() before _inventory_counts(...)
```

Add a regression test proving first-scan response counts newly-created rows correctly.

Keep this narrow. Do not redesign inventory scan behavior.

## Step 7 include_items=true gap

Preferred behavior:

```text
POST /api/admin/icloud-backfill/acquire
dry_run=true
include_items=true
```

should return non-empty safe item details when preview selected items exist.

However, keep the fix small. If mirroring preview item details requires only a small schema/service addition, do it and test it.

If it becomes broader than expected, document it as count-only behavior for now and leave a follow-up. Do not expand response schemas broadly.

## Status remaining-work counts

Approved if narrow and additive.

Preferred additive fields:

```text
backfill_completed_count
unresolved_eligible_count
acquirable_pending_count
retryable_failed_count
ambiguous_or_unsupported_count
```

Do not redesign the status API. Stop if this becomes broader than additive count fields.

## Ambiguous inventory investigation

Please proceed with grouping the 41 ambiguous/unsupported rows by reason/type/resource pattern.

The goal is to answer whether they are harmless sidecars/unsupported resources, fixable classifier gaps, Live Photo/resource grouping issues, or real supported assets being skipped.

Do not change eligibility rules broadly without asking.

## Non-negotiables remain

```text
No acquire_limit=5 validation.
No dry_run=false acquisition.
No actual cleanup/delete.
No cleanup execute endpoint.
No Admin UI.
No scheduler.
No remote iCloud deletion.
No direct Vault writes.
No Source Intake bypass.
No commit or push.
```
