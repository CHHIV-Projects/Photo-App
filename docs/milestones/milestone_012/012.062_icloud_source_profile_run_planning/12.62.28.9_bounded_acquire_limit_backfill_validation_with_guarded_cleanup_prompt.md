# Milestone 12.62.28.9 — Bounded acquire_limit=5 Backfill Validation with Guarded Cleanup

## Purpose

Run a bounded live validation of the iCloud historical backfill routine using:

```text
source_id = 66
acquire_limit = 5
max_listing_candidates = 1000
auto_run_source_intake = true
```

This milestone should validate the full backend routine:

```text
1. preview acquisition candidates
2. execute acquisition only if preview is safe
3. let the existing Source Intake handoff run
4. verify backfill completion and Vault/Asset/Provenance state
5. run cleanup dry run
6. execute guarded local staging cleanup only if cleanup candidates are exactly expected
7. verify final safety state
```

The product owner does **not** want to run terminal commands manually. The coding agent should run the approved backend/service/API validation steps locally and report back.

This is still a bounded validation, not a broad backfill.

---

## Required context to read first

Before doing anything, read:

```text
docs/context/CODING_AGENT_RULES.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.3_historical_backfill_execution_source_intake_handoff_closeout.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.4_historical_backfill_execution_operator_validation_runbook.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.4_historical_backfill_execution_operator_validation_runbook_closeout.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.5_backfill_cleanup_readiness_ambiguous_inventory_investigation_report.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.7_source_profile_deferred_asset_ledger_live_validation_note.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.8_guarded_cleanup_execution_tiny_backfill_validation_closeout.md
```

Also inspect the current backend code for:

```text
iCloud backfill acquisition preview
iCloud backfill acquisition execution
Source Intake handoff
iCloud staging cleanup dry run
iCloud staging cleanup execute
deferred asset ledger status/counts
```

---

## Git rules

Before starting, report:

```powershell
git branch --show-current
git status --short
git log --oneline --decorate -5
```

Expected branch:

```text
feature/icloud-final-routine
```

Expected condition:

```text
12.62.28.8 committed.
Working tree clean except the user-authored 12.62.28.9 prompt, if present.
```

Do **not** run write-affecting Git commands unless explicitly instructed.

Do not run:

```text
git commit
git push
git reset
git rebase
git merge
git tag
git checkout another branch
git stash
```

Read-only Git commands are allowed.

---

## Important operating principle

This prompt approves coder-run backend validation steps.

The product owner does not want a long manual terminal procedure.

Use the existing backend API/service path, local backend, FastAPI TestClient, or controlled local Python script as appropriate. Report the exact command/API/service calls used.

Do not ask the product owner to perform terminal steps unless a stop condition occurs.

---

## Current known baseline

After 12.62.28.8:

```text
source_id = 66
source_label = Chuck iCloud E2E Test v3
previous tiny validation staged file was cleaned
source root file count after cleanup was 0
backfill_execute was empty/untouched
.partial existed with 0 files
backfill_completed_count = 1
deferred_current_count = 41
deferred_adjusted_resource_count = 41
```

The `.partial` empty directory tree is not a blocker if it contains zero files.

Do not manually delete `.partial` directories in this milestone. If the guarded cleanup endpoint removes empty directories naturally, that is acceptable. If empty `.partial` directories remain with zero files, document it as non-blocking.

---

## Approved live operations

This milestone approves the following live/local operations **only within the gates below**:

```text
- live iCloud metadata/listing needed for preview/acquisition
- acquisition preview for source_id=66, acquire_limit=5, max_listing_candidates=1000
- dry_run=false acquisition only after safe preview confirmation
- Source Intake handoff only through auto_run_source_intake=true
- local staging cleanup dry run after acquisition
- guarded local staging cleanup execute only after exact cleanup-candidate verification
- read-only DB/filesystem/API verification
```

Vault writes are allowed only through the normal Source Intake path.

Direct Vault writes or Source Intake bypasses are not allowed.

---

## Not allowed

Do not run:

```text
- acquire_limit greater than 5
- broad historical backfill
- scheduler/automation
- remote iCloud deletion
- adjusted-resource eligibility change
- adjusted-resource import
- Admin UI changes
- manual filesystem deletion outside the guarded cleanup endpoint
- direct Vault writes
- Source Intake outside the acquisition handoff path
- code changes unless a defect is found and reported first
- commit/push/reset/rebase/merge/tag/checkout/stash
```

---

## Required execution sequence

### Step 1 — Preflight checks

Confirm current state before acquisition.

Report:

```text
source_id/source_label
current backfill_completed_count
current deferred_current_count
current deferred_adjusted_resource_count
current source staging file count
whether .partial exists and its file count
whether backfill_execute is empty
```

Expected:

```text
backfill_completed_count = 1
deferred_adjusted_resource_count = 41
source staging file count = 0, ignoring empty directories
.partial file count = 0 if .partial exists
backfill_execute empty/untouched
```

If unexpected staged files exist before acquisition, stop and report.

---

### Step 2 — Acquisition preview

Run the existing acquisition preview/dry-run path with:

```text
source_id = 66
acquire_limit = 5
max_listing_candidates = 1000
dry_run = true
auto_run_source_intake = true
```

Report:

```text
preview run id, if any
selected logical item count
selected resource count
selected relative paths
whether any selected item is adjusted/deferred/unsupported/ambiguous
whether any selected Live Photo has both still and motion resources selected
whether any selected item is already complete
whether listing limit was reached
whether source appears exhausted
preview report path, if any
```

Proceed to execution only if all are true:

```text
selected logical item count = 5
selected items are eligible/acquirable pending items
no adjusted-resource deferred items are selected
no ambiguous or unsupported rows are selected
no already completed backfill rows are selected
no selection errors are present
Live Photo selections, if present, include required paired resources
source_id is 66
max_listing_candidates is 1000
acquire_limit is 5
```

If preview returns fewer than 5 logical items, stop and report before execution.

If preview selects anything adjusted, ambiguous, unsupported, already complete, or otherwise unsafe, stop and report before execution.

---

### Step 3 — Execute bounded acquisition

Only after Step 2 passes, run the existing acquisition execution path with the same parameters:

```text
source_id = 66
acquire_limit = 5
max_listing_candidates = 1000
dry_run = false
auto_run_source_intake = true
```

This is the only approved `dry_run=false` acquisition in this milestone.

Poll/wait using the repo’s existing pattern until the acquisition and Source Intake handoff are complete.

Report:

```text
acquisition_run_id
acquisition_batch_id or batch ids
source_intake_run_id or run ids
logical item count acquired/resolved
resource/file count downloaded/staged
Source Intake status
created Asset count if available
exact duplicate/provenance-only count if available
failed/skipped count
acquisition report path
Source Intake report path
```

Required success criteria:

```text
acquisition status completed
Source Intake status completed
no acquisition failures
no Source Intake failures
no adjusted resources imported
no ambiguous/unsupported resources imported
each selected logical item is resolved
backfill rows are completed only after Source Intake evidence
backfill_completed_count increases by 5 from the preflight baseline, unless exact repo semantics prove a safe equivalent resolution state
```

If any acquisition or Source Intake failure occurs, stop and report. Do not proceed to cleanup execution. Cleanup dry run may be used for inspection only if needed.

---

### Step 4 — Post-acquisition verification before cleanup

Verify and report:

```text
newly completed backfill inventory rows
primary_relative_path for each completed row
backfill_resolution_state for each completed row
acquisition_state for each completed row
source_intake_run_id for each completed row
asset/provenance presence for each completed row/resource
Vault file exists for each Asset
staged files currently exist before cleanup
deferred_current_count
deferred_adjusted_resource_count
```

Expected:

```text
5 newly completed logical items
all completed rows have Source Intake proof
staged local files exist before cleanup
Vault/Asset/Provenance state exists and is safe
deferred_adjusted_resource_count remains 41
```

---

### Step 5 — Cleanup dry run

Run a fresh guarded cleanup dry run for:

```text
source_id = 66
dry_run = true
```

Report:

```text
cleanup dry_run_id
eligible_count
skipped_count
protected_count
verification_failed_count
file_missing_count
delete_failed_count
eligible relative paths
total_bytes_eligible
cleanup dry-run report path
```

Important:

The eligible cleanup file count may be greater than 5 if any acquired logical item has multiple resources, such as a Live Photo still + motion pair.

Cleanup is safe to proceed only if:

```text
eligible files match exactly the staged resources produced by the successful acquisition/Source Intake handoff in this milestone
eligible_count equals the expected staged resource file count from this milestone
skipped_count = 0
protected_count = 0
verification_failed_count = 0
file_missing_count = 0
delete_failed_count = 0
source root is the registered source root for source_id=66
no file under backfill_execute is included
no .partial file is included
every eligible file has Asset/Vault/Source Intake/provenance verification
```

If the cleanup dry run includes unexpected files, fewer files than expected, more files than expected, any skipped/protected/verification-failed files, or anything outside the registered source root, stop and report before cleanup execution.

---

### Step 6 — Execute guarded cleanup

Only after Step 5 passes, execute cleanup through the existing guarded cleanup execute endpoint/service using the required confirmation phrase:

```text
DELETE LOCAL STAGING COPIES
```

Use the fresh cleanup dry-run id from Step 5.

Do not manually delete files.

Report:

```text
cleanup execution_run_id
status
deleted_count
skipped_count
protected_count
verification_failed_count
file_missing_count
delete_failed_count
deleted relative paths
total_bytes_deleted
execution report path
```

Expected:

```text
status = completed
deleted_count = expected staged resource file count
skipped_count = 0
protected_count = 0
verification_failed_count = 0
file_missing_count = 0
delete_failed_count = 0
```

---

### Step 7 — Final post-cleanup verification

Verify and report:

```text
all cleanup-eligible staged files no longer exist
source root file count after cleanup
.partial exists or not
.partial file count, if it exists
backfill_execute remains empty/untouched
Vault files still exist
Asset rows still exist
Provenance rows still exist
Source Intake run(s) remain completed
completed backfill rows remain completed
deferred_current_count
deferred_adjusted_resource_count
deferred_new_since_last_scan_count
deferred_changed_since_last_scan_count
```

Expected:

```text
staged files from this milestone removed
Vault/Asset/Provenance/Source Intake/backfill state intact
deferred_adjusted_resource_count remains 41
no adjusted-resource import occurred
backfill_execute untouched
```

---

## Stop conditions

Stop and report before continuing if any of these occur:

```text
preview selects fewer or more than 5 logical items
preview selects adjusted/deferred/ambiguous/unsupported/already-complete rows
preview has unresolved Live Photo pair requirements
dry_run=false acquisition parameters would differ from approved values
acquisition fails or partially fails
Source Intake fails or remains incomplete
backfill rows are not completed after Source Intake proof
unexpected staged files exist before acquisition
cleanup dry run includes unexpected files
cleanup dry run has skipped/protected/verification_failed/file_missing/delete_failed counts
cleanup would touch backfill_execute
cleanup would touch .partial files
cleanup would touch anything outside source_id=66 registered source root
code changes appear necessary
```

---

## Tests/checks

If no code changes are made, no full regression suite is required.

Run lightweight operational checks sufficient to prove:

```text
preview selection safety
acquisition completion
Source Intake completion
Asset/Vault/Provenance safety
backfill completion
deferred ledger stability
cleanup dry-run safety
cleanup execution result
post-cleanup safety
```

If code changes are needed, stop and ask before making broad changes.

---

## Required closeout document

Create:

```text
docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.9_acquire_limit_5_backfill_validation_with_guarded_cleanup_closeout.md
```

The closeout must include:

```text
summary
preflight state
acquisition preview request/response summary
preview selected logical items/resources
execution request summary
acquisition execution result
Source Intake result
completed backfill rows
Asset/Vault/Provenance verification
deferred ledger verification
cleanup dry-run id/result
cleanup execution id/result
files staged and deleted
final post-cleanup verification
whether any code changed
tests/checks run
known limitations
recommended next milestone
git status --short
git diff --name-only
```

Explicitly state:

```text
No acquire_limit greater than 5.
No broad backfill.
No scheduler/automation.
No remote iCloud deletion.
No adjusted-resource eligibility change.
No adjusted-resource import.
No Admin UI added.
No manual filesystem deletion outside guarded cleanup.
No direct Vault writes.
No Source Intake bypass.
No commit/push/reset/rebase/merge/tag/checkout/stash run.
```

Also document whether:

```text
.partial empty directories remained or were removed
backfill_execute remained untouched
cleanup deleted exactly the expected staged resources
```

---

## Required final response

After completion, return:

```text
1. concise summary
2. preview result
3. acquisition execution result
4. Source Intake result
5. completed backfill rows
6. staged files/resources
7. cleanup dry-run result
8. cleanup execution result
9. final safety verification
10. files changed
11. tests/checks run
12. git status --short
13. git diff --name-only
14. whether you recommend commit
15. whether moving to Admin UI is now reasonable
```

Do not commit.

Do not push.

Do not start UI work.
