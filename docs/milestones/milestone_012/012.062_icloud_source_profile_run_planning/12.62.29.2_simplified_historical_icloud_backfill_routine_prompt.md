# Milestone 12.62.29.2 — Simplified Historical iCloud Backfill Routine

## Purpose

Refactor the historical iCloud backfill workflow from a low-level acquisition control panel into a simple operator routine:

```text
1. Refresh Inventory
2. Show Available Inventory: Yes / No
3. Backfill Historical / Backfill Next 1000
```

The user-facing goal is:

```text
I do not want to think about scan caps, selected logical count, candidate windows, cleanup dry-runs, or Source Intake handoff.
I want to refresh inventory, know whether there is historical inventory available, and press a button to backfill the next 1000 logical assets.
```

This milestone is focused on **historical iCloud backfill only**.

Do not implement the current/new iCloud photos routine yet.

---

## Required context to read first

Before coding, read:

```text
docs/context/CODING_AGENT_RULES.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.7_source_profile_deferred_asset_ledger_report.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.9_acquire_limit_5_backfill_validation_with_guarded_cleanup_closeout.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.29.1_ingestion_page_icloud_run_workflow_report.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.29.1_ingestion_page_icloud_run_workflow_closeout.md
```

Important 12.62.29.1 facts:

```text
- Ingestion page now has an iCloud workflow panel.
- New Import mode is visible but disabled as Coming Soon.
- Historical Backfill mode is enabled.
- UI run imported 96 logical items / 98 resources.
- Cleanup initially stopped for review because the UI compared cleanup candidates against logical primary paths instead of all acquired resource paths.
- This was fixed by exposing acquired_resource_paths and comparing cleanup against all acquired resource paths.
- Guarded cleanup then safely deleted 98 staged files.
```

The 12.62.29.1 closeout/report confirm this workflow and cleanup recovery. Do not regress it.

---

## Git rules

Before starting, report:

```powershell
git branch --show-current
git status --short
git log --oneline --decorate -5
```

Expected branch:

```text
feature/icloud-final-routine
```

Expected condition:

```text
12.62.29.1 committed.
Working tree clean except the user-authored 12.62.29.2 prompt, if present.
```

Do **not** run write-affecting Git commands unless explicitly instructed.

Do not run:

```text
git commit
git push
git reset
git rebase
git merge
git tag
git checkout another branch
git stash
```

Read-only Git commands are allowed.

---

## Critical safety boundary

This is a backend-routine and UI-simplification milestone.

Do not run live iCloud acquisition or cleanup execution during coding unless separately approved.

Allowed:

```text
- backend routine implementation
- frontend UI simplification
- schema/additive response fields if needed
- mocked/unit tests
- frontend build/lint/tests
- backend unit tests
- read-only status checks
```

Not allowed unless separately approved:

```text
- live dry_run=false acquisition
- live cleanup execute
- broad historical backfill
- Source Intake run
- remote iCloud deletion
- adjusted-resource eligibility change
- adjusted-resource import
- direct Vault writes
- scheduler/automation
```

The product owner will do UI validation later.

---

## Product-owner decisions

### Historical operator workflow

The historical workflow should be:

```text
Step 1: Refresh Inventory
Step 2: Available Inventory: Yes / No
Step 3: Backfill Historical / Backfill Next 1000
```

### Meaning of Available Inventory

`Available Inventory: Yes` means:

```text
There are eligible/acquirable historical iCloud logical assets still available to import.
```

`Available Inventory: No` means:

```text
All currently eligible/acquirable historical inventory has been imported or accounted for.
```

Important:

```text
Deferred/adjusted/ambiguous assets should not keep Available Inventory stuck on Yes.
```

Deferred items should be reported separately as:

```text
Deferred / Needs Policy: 41
```

### Historical batch target

The main historical run button should target:

```text
1000 logical assets per historical backfill routine run
```

This is user-facing.

The system should internally handle scan mechanics, candidate windows, preview, acquisition, Source Intake, and cleanup.

### Internal batch size

Use:

```text
internal batch size = 100 logical assets
```

Meaning:

```text
Backfill Next 1000 should internally process up to ten chunks of 100 logical assets.
```

Each internal chunk should:

```text
1. select eligible historical candidates
2. acquire up to 100 logical assets
3. run Source Intake
4. run guarded cleanup dry run
5. execute guarded cleanup only if safe
6. continue to next chunk
```

Stop when:

```text
- 1000 logical assets have been imported, or
- no eligible/acquirable historical inventory remains, or
- a safety gate fails, or
- a real error occurs
```

If only 250 historical assets remain, the routine should import 250 and then return:

```text
Available Inventory: No
```

### Logical assets versus resources/files

Use this terminology clearly:

```text
Logical assets = iCloud inventory items / photo/video items.
Resources/files = underlying downloaded resources, such as HEIC plus MOV for Live Photo.
```

The top-level historical target is **logical assets**.

The final report should show both:

```text
Imported logical assets: 1000
Imported files/resources: 1036
Cleaned local staging files: 1036
```

---

## Current problem to solve

The current 12.62.29.1 UI exposes low-level endpoint behavior.

Observed confusion:

```text
- Refresh Inventory repeatedly shows the same inventory total, e.g. 1011.
- Candidate scan cap behavior is unclear.
- Eligible pending may be high, but preview may select fewer than requested.
- Batch size is not actually implemented as an internal routine loop.
- A run may appear to do nothing without a clear operator-level reason.
```

This milestone should make the historical backfill routine return clear operator-level meanings.

---

## Required backend routine semantics

Add a higher-level historical routine layer.

Preferred endpoint names, adjust to repo conventions:

```text
GET  /api/admin/icloud-routine/historical/status?source_id=<id>
POST /api/admin/icloud-routine/historical/refresh-inventory
POST /api/admin/icloud-routine/historical/run-next-batch
```

If there is a better existing admin route convention, use it, but keep the semantics clear.

### Historical status response

Return operator-level fields such as:

```text
source_id
source_label
inventory_total_logical
backfill_completed_logical
eligible_pending_logical
available_inventory
deferred_current_logical
deferred_adjusted_resource_logical
deferred_ambiguous_logical
deferred_unsupported_logical
retryable_failed_logical
last_inventory_scan_at
last_historical_run_at
last_historical_run_id
last_cleanup_run_id
local_staging_file_count
partial_file_count
backfill_execute_file_count
operator_message
```

Where:

```text
available_inventory = true only if eligible/acquirable historical logical assets remain.
```

Do not count adjusted/deferred-only rows as available inventory.

### Refresh inventory semantics

The refresh inventory routine should do metadata-only inventory refresh for the selected iCloud source.

The response should clearly state:

```text
inventory_total_logical
created_logical
updated_logical
eligible_pending_logical
available_inventory
deferred_current_logical
deferred_adjusted_resource_logical
source_exhausted if known
scan_limit_reached if known
operator_message
```

Important investigation/fix:

```text
Determine whether the current inventory scan is only refreshing the same newest/current listing window or can actually extend historical inventory deeper.
```

If current backend cannot truly scan deeper historical inventory beyond the current listing window, stop and report before inventing weak behavior.

If deeper scanning requires a provider cursor/page token/date boundary not currently available, document the limitation clearly and propose the smallest next backend step.

The desired long-term behavior is:

```text
Refresh Inventory should expand or update the known historical inventory until either the approved scan bound is reached or the source is exhausted.
```

But do not fake source exhaustion.

### Backfill Next 1000 semantics

Add a routine operation:

```text
Backfill Next 1000
```

Suggested request:

```json
{
  "source_id": 66,
  "target_logical_assets": 1000,
  "internal_batch_size": 100
}
```

The UI may not expose these values in the first version. Use defaults:

```text
target_logical_assets = 1000
internal_batch_size = 100
```

The routine should internally loop over existing validated lower-level steps:

```text
preview/select eligible historical candidates
acquire with dry_run=false
auto_run_source_intake=true
cleanup dry run
guarded cleanup execute if safe
repeat
```

Do not require the UI/user to click through each chunk.

### Per-chunk safety gates

Each internal chunk must preserve existing gates:

```text
- no adjusted/deferred/ambiguous/unsupported rows selected
- Live Photo resources remain paired
- Source Intake completes successfully
- backfill rows complete only after Source Intake evidence
- cleanup eligible paths exactly match acquired_resource_paths for that chunk
- cleanup skipped/protected/verification_failed/file_missing/delete_failed counts are zero
- no .partial files included
- no backfill_execute files included
- source root matches selected source profile
```

If any chunk fails a safety gate:

```text
- stop the routine
- do not continue to next chunk
- do not execute unsafe cleanup
- return operator_message explaining the reason
- preserve enough detail for debugging
```

### End states

Routine result states should include:

```text
completed_target
completed_exhausted
completed_partial_scan_bound
stopped_needs_review
failed
```

Definitions:

```text
completed_target:
  Imported the requested 1000 logical assets.

completed_exhausted:
  Imported fewer than 1000 because no eligible/acquirable historical inventory remains.
  Available Inventory should become No.

completed_partial_scan_bound:
  Imported fewer than 1000 because scan/listing bounds prevented finding more in this run.
  Available Inventory may remain Yes or Unknown depending on evidence.

stopped_needs_review:
  Acquisition/Source Intake succeeded or partially succeeded, but cleanup or verification needs operator/developer review.

failed:
  Hard error.
```

### No-op handling

Do not let the run silently do nothing.

If no assets are imported, return a clear reason:

```text
No historical assets imported.
Reason: no eligible/acquirable inventory remains.
```

or:

```text
No historical assets imported.
Reason: inventory refresh required before backfill.
```

or:

```text
No historical assets imported.
Reason: current scan window found no safely re-resolvable eligible rows.
```

---

## Required UI simplification

Update the iCloud panel on the Ingestion page to hide the low-level machinery by default.

Historical section should look conceptually like:

```text
Historical Backfill

Inventory refreshed: <timestamp or Not yet>
Available Inventory: Yes / No / Unknown

Known inventory: 1011 logical assets
Imported: 598
Eligible remaining: 372
Deferred / Needs Policy: 41

[Refresh Inventory]
[Backfill Next 1000]
```

### Remove or hide as top-level controls

Do not make these prominent top-level controls:

```text
candidate search cap
selected logical
selected resources
stop reason
unsafe manifest count
cleanup dry-run internals
Source Intake internals
```

These may appear in an expandable Details panel.

### Backfill button behavior

The button should be:

```text
Backfill Next 1000
```

It should call the historical routine endpoint, not make the user manually preview/acquire/cleanup.

Disable or warn if:

```text
source profile is missing/inactive
inventory has never been refreshed
available_inventory = No
another iCloud routine is running
local staging has unexpected files from prior failed/incomplete run
```

### Operator result card

After routine completes, show a simple result card.

Examples:

```text
Historical Backfill Complete

Requested: 1000 logical assets
Imported logical assets: 1000
Imported files/resources: 1036
Cleaned local staging files: 1036
Eligible remaining: 4250
Deferred / Needs Policy: 41
Available Inventory: Yes
```

or:

```text
Historical Backfill Complete

Requested: 1000 logical assets
Imported logical assets: 250
Imported files/resources: 263
Cleaned local staging files: 263
Eligible remaining: 0
Deferred / Needs Policy: 41
Available Inventory: No
```

or:

```text
Backfill stopped for review

Imported logical assets before stop: 100
Imported files/resources: 104
Cleaned local staging files: 100
Cleanup needs review for 4 files
Reason: cleanup candidates did not match acquired resources
```

### Details panel

Provide expandable details for debugging:

```text
Chunks run
Per-chunk imported logical/resources
Per-chunk cleanup deleted counts
Run ids
Batch ids
Source Intake run ids
Cleanup run ids
Report paths
Stop reason
Safety gate failure reason
```

Keep it collapsed by default.

---

## Skipped/deferred detail

Keep the skipped/deferred detail view added in 12.62.29.1.

It should remain bounded and safe:

```text
state = active_deferred
limit = 100
```

Show:

```text
path/filename
deferred category
reason code
current state
resource count
is live photo
first seen
last seen
observation count
```

Do not expose raw helper payloads, credentials, tokens, signed URLs, or Apple account identifiers.

---

## Current/new photos are a non-goal

Do not implement the current/new iCloud import routine in this milestone.

Keep the UI as:

```text
Import New iCloud Photos — Coming Soon
```

But document the intended future definition:

```text
Current/new iCloud import scans newest-first and imports eligible assets whose stable remote identity is not already known to the selected source profile. Date/listing order determines where to look first; remote identity determines whether an asset is actually new.
```

Do not implement this yet.

---

## Admin page behavior

Keep the Admin iCloud controls marked as:

```text
Legacy/internal iCloud controls — use Ingestion page for normal iCloud runs.
```

Do not remove them in this milestone.

Do not do broad Admin page cleanup.

---

## Tests required

Add focused backend tests for the routine layer.

Suggested backend tests:

```text
- historical status returns available_inventory=true when eligible pending/acquirable rows exist
- historical status returns available_inventory=false when only completed and deferred rows remain
- deferred adjusted rows do not make available_inventory true
- run-next-batch imports in internal chunks up to target 1000
- internal chunk size is 100
- routine stops at target 1000
- routine stops exhausted when fewer than 1000 remain
- routine returns completed_partial_scan_bound when scan/listing bound prevents target
- routine stops_needs_review when cleanup exact-match verification fails
- no-op returns explicit reason
- logical/resource counts are reported separately
```

Add focused frontend tests if the project has a pattern:

```text
- Historical panel shows Available Inventory Yes/No
- Backfill Next 1000 button calls routine endpoint
- low-level scan/preview fields hidden by default
- result card displays requested/imported/resources/cleaned/deferred/remaining
- Details panel exposes chunk/run ids when expanded
- Import New iCloud Photos remains Coming Soon
```

Run relevant validations:

```text
backend focused tests
frontend build
frontend lint/test if available
```

Do not run live acquisition or live cleanup during implementation.

---

## Explicit non-goals

Do not:

```text
- implement current/new iCloud import
- implement scheduler/background jobs
- run live acquisition during coding
- run live cleanup execution during coding
- change adjusted-resource eligibility
- import adjusted resources
- remote delete from iCloud
- redesign the whole Ingestion page
- redesign the whole Admin page
- remove legacy Admin controls
- add broad source workflows for OneDrive/Google/local folders
```

---

## Required report document

Create:

```text
docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.29.2_simplified_historical_icloud_backfill_routine_report.md
```

Include:

```text
1. Routine design summary
2. Available Inventory semantics
3. Logical assets versus resources/files terminology
4. Refresh Inventory behavior
5. Backfill Next 1000 behavior
6. Internal batch size behavior
7. Cleanup safety behavior
8. UI simplification
9. Details/debug panel behavior
10. What was learned about scan limit / inventory refresh depth
11. Tests/checks run
12. Known limitations
13. Follow-up recommendations
14. Future current/new iCloud import definition
```

---

## Required closeout document

Create:

```text
docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.29.2_simplified_historical_icloud_backfill_routine_closeout.md
```

Include:

```text
summary
files changed
backend routine endpoints/behavior
available inventory behavior
refresh inventory behavior
backfill next 1000 behavior
internal batching behavior
cleanup behavior
UI changes
details/debug behavior
tests/checks run with exact results
whether live iCloud was called
whether live acquisition was run
whether cleanup execution was run
whether Source Intake was run
whether adjusted-resource behavior changed
known limitations
recommended next milestone
git status --short
git diff --name-only
```

Explicitly state:

```text
No live dry_run=false acquisition was run during coding.
No live cleanup execute was run during coding.
No Source Intake run was started during coding.
No remote iCloud deletion was run.
No adjusted-resource eligibility change was made.
No adjusted-resource import was run.
No scheduler/automation was added.
No broad Admin/Ingestion cleanup was done.
No current/new iCloud import routine was implemented.
No commit/push/reset/rebase/merge/tag/checkout/stash run.
```

---

## Required final response

After implementation, return:

```text
1. concise summary
2. files changed
3. backend routine endpoint behavior
4. Available Inventory behavior
5. Refresh Inventory behavior
6. Backfill Next 1000 behavior
7. internal batch behavior
8. cleanup safety behavior
9. UI simplification
10. tests/checks run and exact results
11. whether live iCloud/acquisition/cleanup/Source Intake was run
12. known limitations
13. git status --short
14. git diff --name-only
15. whether you recommend commit
```

Do not commit.

Do not push.

Do not start live acquisition.


Good catch. Do not proceed on top of the dirty tree.

I have settled the 12.62.29.1 follow-up cleanup/resource-path fixes first, then committed the 12.62.29.2 prompt separately.

Please re-check:

git branch --show-current
git status --short
git log --oneline --decorate -5

Expected:
- branch: feature/icloud-final-routine
- working tree clean
- 12.62.29.1 cleanup resource-path/fingerprint fixes committed
- 12.62.29.2 prompt committed

Then proceed with 12.62.29.2 under the prompt rules.

I agree with your implementation direction:
- build a small historical routine layer over the validated 12.62.28.x backfill services
- do not reuse internal_loop_orchestrator if it carries ordinary-still/new-count assumptions
- derive Available Inventory from acquirable_pending_count / eligible acquirable historical rows, not from deferred rows
- keep adjusted/deferred rows visible as Deferred / Needs Policy, but do not let them keep Available Inventory = Yes
- document honestly that current refresh with the same cap likely re-scans the same newest window unless a true cursor/page/deeper continuation exists
- keep low-level details in a collapsed Details panel

Do not run live acquisition, cleanup execution, Source Intake, or Git write commands.



Do not commit the current 12.62.29.2 implementation as final. It is useful groundwork, but it does not yet match the product-owner’s intended iCloud intake behavior.

We need to revise 12.62.29.2 around a different mental model.

# Correct Product Model

The operator does **not** want:

```text
Known Inventory
Eligible Remaining
Imported / Accounted For
Total Deferred / Needs Policy
candidate scan caps
preview counts
cleanup internals
```

as the main workflow.

Those are historical/accounting/debug details.

The operator wants an intake station:

```text
iCloud Intake

Source Profile: <selected profile>       Total Imported from Source: <count>

Last Inventory Refresh: <timestamp>
Available Inventory: Yes / No
Logical Candidates Ready: <count>

[Refresh / Prepare Next 1000]
[Import Next 1000]
```

The main action is not “backfill currently known eligible rows.”

The main action is:

```text
Prepare and import the next 1000 good logical iCloud assets.
```

# Key Semantic Correction

Current behavior appears to mean:

```text
Backfill Next 1000 = import up to 1000 from currently known eligible inventory.
```

That is not the desired behavior.

Desired behavior:

```text
Refresh / Prepare Next 1000 =
  scan iCloud as deeply as needed to prepare up to 1000 good eligible logical candidates,
  ignoring already-known/imported assets,
  recording newly discovered deferred/needs-policy assets,
  and continuing deeper until 1000 eligible logical candidates are ready or the source is truly exhausted.

Import Next 1000 =
  import the prepared candidate set through acquisition, Source Intake, and guarded cleanup.
```

So if the current UI says:

```text
Known inventory: 1016
Imported/accounted for: 966
Eligible remaining: 9
Deferred / Needs Policy: 41
```

that does **not** mean Import Next 1000 should only import 9 unless the system has proven the whole iCloud source is exhausted.

It means only 9 are currently known eligible rows. The routine should then scan deeper to prepare more candidates.

# Unified iCloud Intake Architecture

Do not split historical and new/current photos into two separate user-facing workflows right now.

Use one unified iCloud Intake routine:

```text
Scan newest-first.
Skip remote identities already known/imported/accounted for.
Record unknown adjusted/ambiguous/unsupported assets as deferred/needs-policy.
Collect unknown eligible logical candidates.
Continue scanning deeper until 1000 good logical candidates are prepared or source exhaustion is proven.
Import prepared candidates.
```

This same routine behaves like historical backfill during first library intake. After the source is fully accounted for, the same routine naturally behaves like current/new import because only newly added unknown iCloud assets will be imported.

Date/listing order determines where to look first. Remote identity determines whether an asset is actually new/unknown.

# Required UI Redesign

Replace the current historical/accounting-heavy card with this top-level layout:

```text
iCloud Intake

Source Profile: [Chuck iCloud E2E Test v3]      Total Imported from Source: 966

Last Inventory Refresh: July 3, 9:12 AM
Available Inventory: Yes
Logical Candidates Ready: 1000

[Refresh / Prepare Next 1000]
[Import Next 1000]

Last Run Summary
Logical candidates: 1000
Logical imported: 1000
Files/resources imported: 1012
Local staging files cleaned: 1012
New deferred / needs policy this run: 0

[Details]
```

If the source is near completion:

```text
Last Inventory Refresh: July 3, 9:12 AM
Available Inventory: Yes
Logical Candidates Ready: 250

[Import Next 1000]
```

After importing those 250, if source exhaustion is proven:

```text
Available Inventory: No
Logical Candidates Ready: 0
```

# Main UI Fields

Top-level fields should be limited to:

```text
Last Inventory Refresh
Available Inventory
Logical Candidates Ready
Total Imported from Source
Last Run Summary
```

Do **not** show these as top-level workflow fields:

```text
Known Inventory
Eligible Remaining
Imported / Accounted For
Total Deferred / Needs Policy
Candidate scan cap
Selected logical
Selected resources
Stop reason
Cleanup dry-run internals
```

They may appear only in a collapsed Details panel.

# Refresh / Prepare Next 1000 Behavior

The refresh button should be renamed or described as:

```text
Refresh / Prepare Next 1000
```

It should produce a prepared candidate set.

Desired response fields:

```text
source_id
prepare_run_id
last_inventory_refresh_at
target_logical_candidates = 1000
logical_candidates_ready
available_inventory
source_exhausted
new_deferred_this_prepare
operator_message
```

Meaning:

```text
logical_candidates_ready = count of good eligible logical candidates prepared for the next import
```

If there is plenty of inventory, this should normally be:

```text
Logical Candidates Ready: 1000
```

even if the system had to scan more than 1000 provider records to get those 1000.

If only 250 eligible unknown assets remain and source exhaustion is proven:

```text
Logical Candidates Ready: 250
Available Inventory: Yes
```

Then after importing them:

```text
Available Inventory: No
Logical Candidates Ready: 0
```

If source exhaustion is not proven, do not say Available Inventory = No.

# Prepared Candidate Set

Implement a prepared candidate set/snapshot if needed.

Minimum concept:

```text
prepare_run_id
source_id
target_logical_candidates
logical_candidates_ready
candidate inventory ids / remote identities
prepared_at
source_exhausted true/false/unknown
new_deferred_count
```

The Import button should execute the prepared candidate set, not recalculate a different hidden selection without explanation.

If the prepared set becomes stale, the UI should say:

```text
Refresh inventory again before importing.
```

# Deferred / Needs Policy Reporting

Do not pollute new runs with old deferred rows.

The durable deferred ledger is still important, but top-level run reporting should show only new deferred items from this prepare/run.

Use:

```text
New deferred / needs policy this run: 0
```

or:

```text
New deferred / needs policy this run: 7
```

Total historical deferred rows can appear only in Details:

```text
Total deferred in source profile: 41
```

The old 41 should not appear as the main run result every time.

# Import Next 1000 Behavior

The button should be:

```text
Import Next 1000
```

It should use the latest prepared candidate set.

Routine behavior:

```text
1. Take prepared candidate set.
2. Process in internal chunks of 100 logical candidates.
3. Acquire.
4. Source Intake.
5. Guarded cleanup dry run.
6. Execute guarded cleanup only if cleanup candidates exactly match acquired resource paths and safety counters are zero.
7. Continue until prepared set is imported, safety gate stops, or hard error occurs.
```

Final top-level report:

```text
Import Complete

Logical candidates: 1000
Logical imported: 1000
Files/resources imported: 1012
Local staging files cleaned: 1012
New deferred / needs policy this run: 0
```

Partial example:

```text
Import Partially Complete

Logical candidates: 1000
Logical imported: 996
Files/resources imported: 1008
Local staging files cleaned: 1008
New deferred / needs policy this run: 4
Reason: 4 candidates became ambiguous during final acquisition re-resolution.
```

Source nearly exhausted example:

```text
Import Complete

Logical candidates: 250
Logical imported: 250
Files/resources imported: 263
Local staging files cleaned: 263
New deferred / needs policy this run: 0
Available Inventory: No
```

# Scan Depth / Deeper Discovery Requirement

This is the main backend gap to solve.

The current bounded newest-window refresh is not enough if it repeatedly scans the same newest assets.

The routine must be able to scan deeper as needed to prepare 1000 good logical candidates.

Acceptable approaches may include:

```text
- provider cursor/page-token continuation, if available
- deterministic expanding scan depth, such as 1000, then 2000, then 3000, until enough eligible candidates are found
- date/position boundary if the provider exposes a reliable one
```

Do not fake source exhaustion.

If deeper scanning cannot be implemented safely with current helper/provider capabilities, stop and report options before coding further.

# Available Inventory Semantics

Top-level Available Inventory means:

```text
Yes = there are prepared logical candidates ready to import, or the system believes more can be prepared by deeper scanning.
No = source has been fully accounted for except deferred/needs-policy assets.
Unknown = system cannot determine because scan/deeper continuation is not proven.
```

Do not set Available Inventory = No just because current known eligible rows are zero.

# Total Imported from Source

A small context box next to Source Profile is useful:

```text
Total Imported from Source: 966
```

That is the only historical accounting number that should be top-level.

Everything else historical/accounting belongs in Details.

# Details Panel

Collapsed by default.

May show:

```text
known inventory
imported/accounted for
currently known eligible
total deferred
new deferred this prepare/run
provider records scanned
scan depth used
prepare_run_id
routine run id
chunk ids
acquisition ids
Source Intake ids
cleanup run ids
report paths
stop reasons
safety gate details
```

# Current/New iCloud Photos

Do not build a separate New Import workflow in this milestone.

Replace or relabel the disabled Coming Soon area if needed. The long-term product concept is now unified intake:

```text
Import Next 1000 unknown iCloud assets.
```

Once the whole source is accounted for, the same routine will pick up newly added iCloud photos.

# Current 12.62.29.2 Status

The existing 12.62.29.2 code/report/closeout should be treated as in-progress, not final.

Revise the implementation and update the report/closeout accordingly.

Do not commit until this corrected behavior is implemented or until you stop with a clear technical blocker.

# Safety Boundaries

Do not run live acquisition, Source Intake, or cleanup execution during coding unless separately approved.

Do not run remote iCloud deletion.

Do not change adjusted-resource eligibility.

Do not import adjusted resources.

Do not run commit/push/reset/rebase/merge/tag/checkout/stash.

# Stop Conditions

Stop and ask before continuing if:

```text
- preparing 1000 candidates requires a broad provider redesign
- deeper scan cannot be implemented without unsafe assumptions
- provider ordering cannot be relied on even for scan direction
- prepared candidate snapshots require schema changes broader than this milestone
- import cannot reliably execute the prepared set
```

The product-owner goal is simple:

```text
Refresh prepares the next 1000 good logical candidates.
Import imports those candidates.
Reports focus on this run, not old historical accounting.
```




Yes, your read is correct. Proceed with the revised 12.62.29.2 direction, but treat the current implementation/report/closeout as in-progress until this addendum is implemented.

## 1. Prepared candidate snapshot is approved

Yes, use a small additive durable schema.

Approved concept:

```text id="jz0b2j"
icloud_intake_prepare_runs
icloud_intake_prepared_candidates
```

The prepared set is necessary because:

```text id="i7oep5"
Refresh / Prepare Next 1000 should create the exact candidate set.
Import Next 1000 should execute that exact prepared set.
```

Do not let Import silently recalculate a different hidden selection.

Suggested fields for `icloud_intake_prepare_runs`:

```text id="2aefsk"
id
source_profile_id
status
target_logical_candidates
logical_candidates_ready
new_deferred_count
provider_records_scanned
scan_depth_used
source_exhaustion_state  -- exhausted / not_exhausted / unknown
prepared_at
expires_at
operator_message
created_at
updated_at
```

Suggested fields for `icloud_intake_prepared_candidates`:

```text id="7q4lwd"
id
prepare_run_id
source_profile_id
inventory_id
remote_identity
primary_relative_path
candidate_index
candidate_state  -- prepared / imported / skipped_stale / deferred_at_execution / failed
resource_count
is_live_photo
created_at
updated_at
```

Keep schema additive/idempotent.

## 2. Staleness rule

Use a prepared-set validity window.

Recommended:

```text id="v2y6c0"
prepared candidate set expires after 60 minutes
```

Import should require a fresh prepare if:

```text id="zczmpz"
prepare_run is expired
prepare_run source_id does not match selected source
candidate rows are already imported/completed
candidate rows are no longer eligible/acquirable
prepare_run was already consumed
```

If stale, UI should show:

```text id="ftjhsm"
Refresh / Prepare Next 1000 again before importing.
```

## 3. Deterministic expanding scan is approved

Yes, implement deterministic expanding scan depth if provider cursor/page-token continuation is not available.

Approved behavior:

```text id="muff2m"
Start with a bounded newest-first scan.
If fewer than 1000 good unknown eligible logical candidates are prepared,
expand scan depth and continue.
```

Example strategy:

```text id="tkpe90"
1000 → 2000 → 3000 → 5000 → 10000
```

or another conservative repo-appropriate expansion sequence.

The product requirement is:

```text id="p7xy9z"
Prepare up to 1000 good unknown eligible logical candidates,
not merely whatever is already known eligible.
```

Do not claim source exhaustion unless the helper/provider actually proves exhaustion.

If the routine reaches a local scan ceiling without proving exhaustion, return:

```text id="q0w40t"
available_inventory = Unknown or Yes
logical_candidates_ready = <count found>
operator_message = Prepared fewer than 1000 because scan depth limit was reached; deeper inventory may remain.
```

## 4. Available Inventory semantics

Use this meaning:

```text id="3k3xqr"
Available Inventory = Yes
```

when:

```text id="udazgh"
logical_candidates_ready > 0
or deeper unknown inventory may remain
```

Use:

```text id="alymti"
Available Inventory = No
```

only when:

```text id="g16bd8"
logical_candidates_ready = 0
and source exhaustion is proven
```

Use:

```text id="wr8w0g"
Available Inventory = Unknown
```

when the system cannot prove either way because scan depth/cursor limitations were reached.

Do not base Available Inventory only on current `acquirable_pending_count`.

## 5. Deferred / needs-policy reporting

Top-level report should show only **newly discovered** deferred items for this prepare/run.

Use:

```text id="i8vb90"
New deferred / needs policy this run: 0
```

or:

```text id="ii1w4v"
New deferred / needs policy this run: 7
```

Existing historical deferred totals may remain in Details only.

Do not keep showing the old 41 as a headline result every run.

## 6. UI top-level fields

Revise the top-level UI to:

```text id="mz0r08"
iCloud Intake

Source Profile: <selected source>      Total Imported from Source: <count>

Last Inventory Refresh: <timestamp>
Available Inventory: Yes / No / Unknown
Logical Candidates Ready: <count>

[Refresh / Prepare Next 1000]
[Import Next 1000]

Last Run Summary
Logical candidates: <count>
Logical imported: <count>
Files/resources imported: <count>
Local staging files cleaned: <count>
New deferred / needs policy this run: <count>
```

Move these to collapsed Details only:

```text id="ql5n58"
Known inventory
Eligible remaining
Imported/accounted for
Total deferred
scan caps
scan depth
stop reason
run ids
chunk ids
cleanup internals
```

## 7. Import behavior

`Import Next 1000` should execute the latest valid prepared set.

Process in internal chunks of:

```text id="lqlmf2"
100 logical candidates
```

Each chunk must preserve existing safety gates:

```text id="fx7m3k"
acquisition
Source Intake
guarded cleanup dry run
exact acquired_resource_paths match
guarded cleanup execute
```

If a candidate becomes ambiguous/deferred during final re-resolution, record it as newly deferred for this run and report it. If this causes fewer than 1000 logical imports, that is acceptable and should be explained.

## 8. Source exhaustion

Do not fake source exhaustion.

If there is no provider cursor/page-token/date-boundary and expansion hits the configured maximum scan depth, say so clearly.

Acceptable operator message:

```text id="q3txdp"
Prepared 750 candidates. Scan depth limit was reached before 1000 candidates were found. More iCloud inventory may remain.
```

Do not show Available Inventory = No in that state.

## 9. Current/new photos

Do not create a separate current/new workflow.

The unified intake routine is the product direction.

Once the source is fully accounted for, this same routine will naturally pick up newly added unknown iCloud assets.

## 10. Safety

Continue not running live acquisition, Source Intake, cleanup execution, or remote iCloud operations during coding.

Use mocked/unit tests.

## 11. Tests to add/update

Add tests for:

```text id="er37j8"
- prepare creates a durable candidate snapshot
- import executes the prepared candidate set, not a recalculated hidden set
- prepare expands scan depth until 1000 candidates or exhaustion/scan ceiling
- already-known/imported identities are skipped during prepare
- newly discovered deferred items are counted per prepare/run
- old deferred totals do not appear as top-level run result
- Available Inventory = No only when source exhaustion is proven
- Available Inventory = Unknown when scan ceiling is reached without exhaustion
- stale/expired prepared set blocks import
- UI shows Logical Candidates Ready as top-level field
```

Proceed with the small durable prepared-candidate snapshot and deterministic expanding scan implementation.

Do not commit.
