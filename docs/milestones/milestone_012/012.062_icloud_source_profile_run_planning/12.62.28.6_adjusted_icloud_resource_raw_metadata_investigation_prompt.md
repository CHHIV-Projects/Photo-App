# Milestone 12.62.28.6 — Adjusted iCloud Resource Raw Metadata Investigation

## Purpose

Investigate the exact raw iCloud/helper metadata behind the 41 inventory rows classified as:

```text
unsupported_reasons_json = ["unsupported_adjusted_resource"]
identity_ambiguous = true
primary_content_type = public.heic
```

The purpose is to replace assumptions with evidence before making product policy decisions about adjusted iCloud resources.

This milestone should answer:

```text
What exact helper/listing fields caused the app to classify these rows as unsupported_adjusted_resource?
What do those fields mean in the context of the current helper output?
Are these rows edited/rendered images, adjustment resources, duplicate alternate resources, Live Photo variants, or something else?
Do corresponding original/non-adjusted resources exist in the same listing or local inventory?
```

Do **not** change acquisition eligibility policy in this milestone.

Do **not** run additional acquisition.

Do **not** run cleanup execution.

---

## Required context to read first

Before doing anything, read:

```text
docs/context/CODING_AGENT_RULES.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.5_backfill_cleanup_readiness_ambiguous_inventory_investigation_report.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.5_backfill_cleanup_readiness_ambiguous_inventory_investigation_closeout.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.4_historical_backfill_execution_operator_validation_runbook.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.3_historical_backfill_execution_and_source_intake_handoff_closeout.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.2_historical_backfill_inventory_acquisition_preview_closeout.md
```

Also inspect the relevant classifier/helper code:

```text
backend/app/services/icloud_backfill_inventory_service.py
backend/app/services/icloud_backfill_acquisition_preview_service.py
backend/app/services/icloud_backfill_acquisition_execution_service.py
backend/app/services/icloud_acquisition/exact_selection_adapter.py
backend/app/services/icloud_acquisition/exact_selection_protocol.py
backend/app/services/icloud_acquisition/icloud_exact_selection_helper.py
backend/app/models/icloud_backfill.py
backend/tests/test_icloud_backfill_inventory.py
backend/tests/test_icloud_backfill_acquisition_preview.py
```

If the exact helper/classifier code lives elsewhere, inspect that too.

---

## Git rules

Before starting, report:

```powershell
git branch --show-current
git status --short
git log --oneline --decorate -5
```

Expected branch:

```text
feature/icloud-final-routine
```

Expected condition:

```text
12.62.28.5 committed, or only this user-authored 12.62.28.6 prompt present as untracked/modified.
```

Do **not** run write-affecting Git commands unless I explicitly instruct you.

Do not run:

```text
git commit
git push
git reset
git rebase
git merge
git tag
git checkout another branch
git stash
```

Read-only Git commands are allowed.

---

## Critical safety rules

Do **not** run:

```text
POST /api/admin/icloud-backfill/acquire
```

with:

```text
dry_run=false
```

Do **not** run `acquire_limit=5`.

Do **not** execute cleanup/delete.

Do **not** call any cleanup execute endpoint.

Do **not** make adjusted resources eligible for acquisition.

Do **not** change duplicate/canonicalization behavior.

Do **not** write directly to Vault, Asset, provenance, or Source Intake state.

Allowed:

```text
- read-only DB/API inspection
- code tracing
- mocked/fake automated tests
- metadata-only helper/listing diagnostics if they do not download, stage, intake, clean, or update completion state
- adding a narrow redacted diagnostic report/tool if necessary
- adding tests around classifier/reporting behavior
```

If a live metadata-only helper listing is needed, stop first and state the exact command/API call, source_id, candidate limit, and why it is non-downloading. Do not run live helper diagnostics unless the product owner approves the exact command.

---

## Known live context

The 12.62.28.5 investigation found 41 non-eligible inventory rows for source `66`.

All 41 had:

```text
identity_ambiguous = true
unsupported_reasons_json = ["unsupported_adjusted_resource"]
primary_content_type = public.heic
extension = .heic
backfill_completed = false
acquisition_state = null
```

Grouped summary:

```text
28 adjusted HEIC single-resource rows
13 adjusted HEIC Live Photo grouped rows with resource_count=2
```

The report found no AAE-only sidecars, orphaned MOV-only rows, missing-identity rows, or unsupported-extension rows in this 41-row set.

Do not assume what “adjusted” means. Trace it to raw helper/listing evidence.

---

## Main task

Produce a clear evidence report that traces this chain:

```text
raw helper/listing fields
    ↓
adapter/parser normalized fields
    ↓
inventory classifier decision
    ↓
stored inventory fields:
       identity_ambiguous=true
       eligibility_state=ambiguous_metadata_only
       unsupported_reasons_json=["unsupported_adjusted_resource"]
```

The report should answer:

```text
1. Which code path sets unsupported_adjusted_resource?
2. Which exact helper/listing field or value triggers it?
3. Is the trigger resource-level, item-level, or grouping-level?
4. Is the adjusted signal present on the primary HEIC, a companion resource, or another resource?
5. Is there an original/non-adjusted counterpart in the same helper item, same listing, or existing inventory?
6. Is there a Live Photo pairing relationship for the 13 resource_count=2 rows?
7. Are the 28 single-resource rows complete standalone adjusted assets, or adjusted variants related to originals elsewhere?
8. Are the 41 rows skipped because they are truly unsupported by design, because classifier policy is not decided, or because current metadata is insufficient?
```

---

## Required investigation phases

### Phase A — Code trace

Find and document the exact code path that emits:

```text
unsupported_adjusted_resource
```

Report:

```text
file path
function/class name
condition
input field(s)
normalized field(s)
stored output field(s)
```

Example desired format:

```text
unsupported_adjusted_resource is added in:
  <file>:<function>

Condition:
  if <field> == <value> ...

Raw/helper source:
  <helper field name/value if known>

Normalized output:
  unsupported_reasons_json includes unsupported_adjusted_resource
  eligibility_state becomes ambiguous_metadata_only
  identity_ambiguous becomes true
```

If the code currently loses the raw source field before storing/reporting, state that clearly.

---

### Phase B — Stored inventory evidence

Using read-only DB/API inspection, summarize the 41 rows by stored fields.

Include grouped counts by:

```text
resource_count
grouping_kind or grouping field
primary_relative_path extension
primary_content_type
eligibility_state
known_state
unsupported_reasons_json
identity_ambiguous
backfill_completed
acquisition_state
```

Include a safe sample table, max 15 rows:

```text
inventory_id
primary_relative_path
resource_count
grouping_kind
eligibility_state
known_state
unsupported_reasons_json
identity_ambiguous
classification_category
```

Do not expose credentials, tokens, signed URLs, Apple account identifiers, or sensitive helper IDs.

---

### Phase C — Raw helper/listing metadata evidence

Determine whether raw helper/listing metadata is already available from existing logs, reports, stored JSON, test fixtures, or helper output captured during inventory scan.

If available, produce a redacted summary for representative rows.

If not available, determine the minimum safe metadata-only diagnostic needed.

The diagnostic should capture only redacted, non-sensitive fields such as:

```text
relative path
filename extension
content type
resource count
resource type/role/name/class
resource relationship/grouping
is_original / is_adjusted / adjusted marker, if present
is_live_photo / live photo grouping marker, if present
resource size
resource checksum presence, not checksum value unless already considered safe in repo reports
```

Do not include:

```text
Apple credentials
tokens
cookies
signed URLs
raw account identifiers
private helper auth data
unredacted raw CloudKit payloads
```

If live metadata helper access is required, stop and ask for approval before running it.

---

### Phase D — Original/counterpart analysis

For the 41 adjusted rows, determine whether there appears to be a corresponding original/non-adjusted asset.

Check safely using inventory/listing metadata:

```text
same base filename
same capture timestamp
same date folder
same helper grouping
same logical identifier family, if safely available
same Live Photo group
nearby inventory rows with same IMG number but eligible_metadata_only
```

Report grouped results:

```text
adjusted row has apparent eligible original counterpart      N
adjusted row has apparent non-eligible original counterpart  N
adjusted row has no apparent counterpart in current inventory N
unknown / insufficient metadata                              N
```

Do not use fuzzy image similarity or new duplicate logic.

This is metadata analysis only.

---

### Phase E — Product interpretation options

Based on evidence, classify each group into one of:

```text
adjusted_rendered_variant_of_original
adjustment_metadata_or_internal_resource
alternate_resource_for_same_logical_asset
adjusted_live_photo_variant
classifier_policy_gap
helper_metadata_insufficient
unknown_needs_more_data
```

For each group, recommend one of:

```text
defer_for_policy
safe_to_ignore_as_internal_resource
safe_to_import_as_standalone_original
safe_to_link_as_derivative_version
needs_live_photo_adjusted_pair_policy
needs_helper/classifier_fix_before acquisition
```

Do not implement the policy yet.

---

## Explicit non-goals

Do not:

```text
- change eligibility rules for adjusted resources
- import adjusted resources
- run acquisition
- run cleanup execution
- delete staged files
- create Admin UI
- add scheduler/automation
- redesign duplicate handling
- redesign canonicalization
- add broad versioning support
```

---

## Optional narrow code changes

Allowed only if needed and small:

```text
- add a redacted diagnostic function/report for adjusted inventory rows
- add tests around unsupported_adjusted_resource classification
- preserve/report the raw adjusted-resource trigger field in diagnostics
- improve documentation of the classifier decision
```

Not allowed without asking:

```text
- schema changes to support full versioning
- eligibility rule changes
- acquisition behavior changes
- Source Intake changes
- helper protocol changes that affect downloads
- broad raw payload storage
```

---

## Tests to run

If no code changes are made, run only relevant existing tests needed to confirm no drift, or state that this was docs/diagnostics only.

If code changes are made, run focused tests plus relevant regressions:

```text
test_icloud_backfill_inventory.py
test_icloud_backfill_acquisition_preview.py
test_icloud_backfill_acquisition_execute.py
test_icloud_exact_selection_adapter.py
test_icloud_12_62_26_raise_limits.py
test_icloud_new_count_planner.py
```

If adding a diagnostic test file, use a clear name such as:

```text
backend/tests/test_icloud_adjusted_resource_diagnostics.py
```

No live iCloud automated tests.

---

## Required report document

Create:

```text
docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.6_adjusted_icloud_resource_raw_metadata_investigation_report.md
```

The report must include:

```text
1. Executive summary
2. Code path that emits unsupported_adjusted_resource
3. Exact raw/helper/listing field(s) that trigger the classification
4. Stored inventory evidence summary
5. Raw metadata evidence summary
6. Original/counterpart analysis
7. Grouped interpretation of the 28 single-resource adjusted HEIC rows
8. Grouped interpretation of the 13 adjusted Live Photo grouped rows
9. What is known with evidence
10. What remains unknown
11. Policy options based on evidence
12. Recommendation for adjusted-resource handling in v1.0
13. Whether any code changed
14. Whether any live metadata helper call was run
15. Whether any acquisition/cleanup/delete was run
```

Use careful language:

```text
Evidence shows...
The helper field indicates...
The classifier currently interprets this as...
Unknown / not proven...
```

Avoid:

```text
probably
likely
assuming
appears to mean
```

unless the sentence clearly labels it as an inference.

---

## Closeout document

Create:

```text
docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.6_adjusted_icloud_resource_raw_metadata_investigation_closeout.md
```

The closeout must include:

```text
summary
files changed
whether code changed
whether live helper metadata call was run
whether acquisition was run
whether cleanup/delete was run
tests run with exact results
key findings
policy recommendation
known limitations
recommended next milestone
git status --short
git diff --name-only
```

Explicitly state:

```text
No acquire_limit=5 validation run.
No dry_run=false acquisition run.
No cleanup execution/delete run.
No eligibility rules changed.
No Admin UI added.
No scheduler added.
No remote iCloud deletion added.
No direct Vault writes added.
No Source Intake bypass added.
No commit/push/reset/rebase/merge/tag/checkout/stash run.
```

---

## Stop conditions

Stop and ask before continuing if:

```text
live helper metadata access is required
raw helper data contains sensitive fields that cannot be safely redacted
the exact adjusted-resource trigger cannot be found in current code
classification requires broad helper/parser refactor
policy implementation seems necessary
eligibility changes seem necessary
acquisition is required to inspect metadata
cleanup execution seems necessary
tests require live iCloud
```

---

## Required final response

After completing the investigation, return:

```text
1. concise summary
2. files changed
3. exact raw/helper fields identified
4. what the 28 single-resource rows represent
5. what the 13 Live Photo grouped rows represent
6. whether code changed
7. tests run and exact pass/fail counts
8. whether any live helper metadata call was run
9. whether any acquisition/cleanup/delete was run
10. git status --short
11. git diff --name-only
12. whether you recommend commit
13. what policy decision is now supported by evidence
```

Do not commit.

Do not push.

Do not start cleanup execution.

Do not start `acquire_limit=5`.





Approved. Good review. Please proceed with 12.62.28.6 using the following refinements.

## 1. Git expectation update

12.62.28.5 has now been committed.

Expected condition is now:

```text
12.62.28.5 committed.
Working tree clean except the user-authored 12.62.28.6 prompt, if present.
```

Please confirm with:

```powershell
git branch --show-current
git status --short
git log --oneline --decorate -5
```

Do not run write-affecting Git commands.

## 2. Additional files/tests to inspect

Please also inspect:

```text
backend/tests/test_icloud_exact_selection_helper.py
backend/app/services/icloud_acquisition/phase0_validation.py
```

`test_icloud_exact_selection_helper.py` is important because it directly covers adjusted-resource helper behavior.

`phase0_validation.py` may provide historical/adjacent context for the same unsupported reason.

## 3. Raw metadata feasibility boundary

Please distinguish clearly between:

```text
code-proven trigger
```

and:

```text
per-row raw metadata evidence for the 41 stored inventory rows
```

If the stored inventory rows do not persist raw `asset.versions` keys or the raw adjusted marker, say that directly.

Do not infer per-row raw evidence from normalized stored fields.

If proving the exact raw marker for the 41 live rows requires a new live metadata-only helper listing, stop and ask for approval before running it.

## 4. Known likely code trigger to confirm

Please confirm through code, not assumption, whether the adjusted-resource trigger is:

```text
AssetVersionSize.ADJUSTED in asset.versions
```

If that is correct, report:

```text
- exact file/function
- exact condition
- exact normalized output
- exact stored inventory result
```

If that is not correct, report the actual trigger.

## 5. Model field naming

Use the actual model field:

```text
grouping
```

not `grouping_kind`, unless code has both.

## 6. Original counterpart distinction

Please distinguish these two cases:

```text
A. The same helper item contains a primary/original resource while an adjusted marker is also present.

B. A separate original/non-adjusted logical item exists elsewhere in the listing or stored inventory.
```

Those are different policy implications.

## 7. “No raw evidence available” path

The report may conclude:

```text
Exact adjusted-resource trigger is proven by code path,
but per-row raw helper payload is not persisted for the 41 inventory rows.
```

That is acceptable.

Do not run live helper metadata access just to satisfy the report unless you stop and ask first with the exact command, source_id, candidate limit, and why it is metadata-only/non-downloading.

## 8. Safety boundaries remain

Do not run:

```text
- acquire_limit=5
- dry_run=false acquisition
- cleanup execution/delete
- eligibility rule changes
- adjusted-resource acquisition
- Admin UI
- scheduler
- direct Vault writes
- Source Intake bypass
```

Proceed with 12.62.28.6 investigation under these refinements.
