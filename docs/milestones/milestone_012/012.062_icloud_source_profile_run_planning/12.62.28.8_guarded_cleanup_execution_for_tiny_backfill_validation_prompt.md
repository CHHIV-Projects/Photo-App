# Milestone 12.62.28.8 — Guarded Cleanup Execution for Tiny Backfill Validation

## Purpose

Execute and verify the guarded iCloud staging cleanup for the single staged file produced by the successful tiny historical backfill validation.

This milestone completes the safe post-intake cleanup loop for the already-approved `acquire_limit=1` validation before considering any larger acquisition validation.

This is **not** a new acquisition milestone.

---

## Required context to read first

Before doing anything, read:

```text
docs/context/CODING_AGENT_RULES.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.4_historical_backfill_execution_operator_validation_runbook.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.4_historical_backfill_execution_operator_validation_closeout.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.5_backfill_cleanup_readiness_ambiguous_inventory_investigation_report.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.5_backfill_cleanup_readiness_ambiguous_inventory_investigation_closeout.md

docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.7_source_profile_deferred_asset_ledger_live_validation_note.md
```

Also inspect the existing cleanup API/service code:

```text
backend/app/api/admin.py
backend/app/schemas/admin.py
backend/app/services/icloud_acquisition/
```

Find the exact staging-cleanup service files if they live under a more specific path.

---

## Git rules

Before starting, report:

```powershell
git branch --show-current
git status --short
git log --oneline --decorate -5
```

Expected branch:

```text
feature/icloud-final-routine
```

Expected condition:

```text
12.62.28.7 live validation note committed.
Working tree clean except the user-authored 12.62.28.8 prompt, if present.
```

Do **not** run write-affecting Git commands unless I explicitly instruct you.

Do not run:

```text
git commit
git push
git reset
git rebase
git merge
git tag
git checkout another branch
git stash
```

Read-only Git commands are allowed.

---

## Known cleanup dry-run evidence

12.62.28.5 approved cleanup dry-run `132` for source `66`.

Dry-run result:

```text
run_id = 132
status = completed
dry_run = true
source_root_path = storage/exports/icloud/chuck_icloud_e2e_test_v3
eligible_count = 1
deleted_count = 0
skipped_count = 0
total_files = 1
total_bytes_eligible = 1982778
report_path = storage/logs/icloud_cleanup_reports/icloud_cleanup_20260702_042510_run132.json
```

The eligible file was:

```text
storage/exports/icloud/chuck_icloud_e2e_test_v3/2026/02/04/IMG_5235.HEIC
```

The dry run verified:

```text
staged SHA-256 matched Asset SHA-256
Vault path was present and under configured Vault root
Vault SHA-256 matched Asset SHA-256
no skipped/protected/verification-failed files were reported
```

It did not scan or touch:

```text
storage/exports/icloud/backfill_execute
```

The old `.partial` tree under the source staging path contained zero files and was not reported because cleanup scans files/symlinks, not empty directories.

---

## Critical live-operation approval

For this milestone, the product owner approves running the guarded cleanup execution **only if all of the following are true immediately before execution**:

```text
source_id = 66
dry_run_id = 132, or a newly refreshed dry-run id if run 132 is no longer accepted
eligible_count = 1
skipped_count = 0
verification_failed_count = 0, if such a field exists
the only eligible file is 2026/02/04/IMG_5235.HEIC
source root is storage/exports/icloud/chuck_icloud_e2e_test_v3
cleanup execute endpoint requires the repo’s existing confirmation phrase
```

If any condition differs, stop and ask before executing cleanup.

---

## Allowed work

You may:

```text
- inspect cleanup readiness/status
- inspect cleanup dry-run 132/report if available
- rerun cleanup dry-run for source_id=66 if run 132 is stale or execute requires a fresh dry-run id
- execute cleanup only after confirming the eligible set is exactly one expected file
- verify the file was removed from staging
- verify Vault/Asset state was not damaged
- verify backfill/deferred ledger state was not changed unexpectedly
- create docs-only closeout
```

---

## Not allowed

Do not run:

```text
- any new acquisition
- acquire_limit=5
- POST /api/admin/icloud-backfill/acquire with dry_run=false
- Source Intake
- remote iCloud delete
- adjusted-resource eligibility changes
- adjusted-resource import
- Admin UI
- scheduler/automation
- direct Vault writes
- manual filesystem deletion outside the existing guarded cleanup execute endpoint
```

Do not manually delete files in File Explorer or PowerShell.

Cleanup must go through the existing guarded cleanup API/service.

---

## Required execution sequence

### Step 1 — Confirm backend/schema state

Confirm backend is running and current API/service code is loaded.

If backend restart is required, restart it using the repo’s normal local development process.

### Step 2 — Inspect cleanup dry-run evidence

Inspect dry-run `132` if available.

If the cleanup execute endpoint requires a fresh dry-run id, rerun cleanup dry-run for source `66`:

```text
POST /api/admin/icloud-staging-cleanup/run
dry_run = true
source_id = 66
```

Report the new dry-run id and eligible/skipped/failed counts.

### Step 3 — Stop if eligible set differs

Stop and ask if:

```text
eligible_count is not exactly 1
the eligible file is not 2026/02/04/IMG_5235.HEIC
skipped/protected/verification-failed files exist
source root differs from storage/exports/icloud/chuck_icloud_e2e_test_v3
backfill_execute appears in the cleanup plan
the .partial tree has files, not just empty directories
```

### Step 4 — Execute guarded cleanup

If and only if the eligible set matches expectations, run the existing cleanup execute endpoint using the required confirmation phrase.

Use the cleanup service/API’s normal expected request shape.

Do not invent a bypass.

### Step 5 — Verify after cleanup

Verify:

```text
cleanup execution status completed
deleted_count = 1
deleted file path = 2026/02/04/IMG_5235.HEIC
staged file no longer exists
Vault file still exists
Asset record still exists
Source Intake/provenance record still exists
no remote iCloud action occurred
backfill_completed row remains complete
deferred ledger still has 41 adjusted-resource active deferred rows
```

Also confirm:

```text
backfill_execute remains untouched
.partial empty-directory tree status is documented
```

---

## Tests

If no code changes are made, no full regression suite is required.

Run only lightweight/read-only checks needed to validate cleanup result.

If a code defect is discovered and fixed, stop and ask before broad changes. Then run focused cleanup tests plus relevant regressions.

---

## Required closeout document

Create:

```text
docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.28.8_guarded_cleanup_execution_tiny_backfill_validation_closeout.md
```

The closeout must include:

```text
summary
cleanup dry-run id used
whether dry-run was refreshed
cleanup execute request summary
cleanup execution result
files deleted
verification after cleanup
Vault/Asset/Source Intake safety confirmation
backfill/deferred ledger safety confirmation
whether any code changed
whether any tests were run
known limitations
recommended next milestone
git status --short
git diff --name-only
```

Explicitly state:

```text
No acquisition run.
No acquire_limit=5 validation run.
No Source Intake run.
No remote iCloud deletion.
No direct Vault writes.
No adjusted-resource eligibility change.
No adjusted-resource import.
No Admin UI added.
No scheduler added.
No manual filesystem deletion outside guarded cleanup.
No commit/push/reset/rebase/merge/tag/checkout/stash run.
```

---

## Required final response

After completion, return:

```text
1. concise summary
2. cleanup dry-run id used
3. whether dry-run was refreshed
4. cleanup execution result
5. file deleted
6. post-cleanup verification
7. files changed
8. tests/checks run
9. git status --short
10. git diff --name-only
11. whether you recommend commit
12. whether optional acquire_limit=5 validation is now reasonable
```

Do not commit.

Do not push.

Do not start acquire_limit=5.


Approved. Please proceed with 12.62.28.8 guarded cleanup execution under the conditions below.

## 1. Context file correction

Yes, treat this file as the intended required context:

```text
12.62.28.4_historical_backfill_execution_operator_validation_runbook_closeout.md
```

The earlier prompt filename without `runbook` was a naming mismatch.

## 2. Fresh dry run required

Correct: dry-run `132` is expired for execution because of the cleanup service freshness window.

Proceed with:

```text
1. Run a fresh cleanup dry run for source_id=66.
2. Confirm the eligible set is still exactly one file.
3. Confirm skipped/protected/verification-failed counts are zero.
4. Confirm the eligible file is:
   2026/02/04/IMG_5235.HEIC
5. Confirm source root is:
   storage/exports/icloud/chuck_icloud_e2e_test_v3
6. Only then execute cleanup using the required confirmation phrase:
   DELETE LOCAL STAGING COPIES
```

If any condition differs, stop and report before executing.

## 3. Empty directory cleanup is approved

Yes, it is acceptable and preferred for the guarded cleanup endpoint to remove empty directories under the registered source staging root after deleting eligible files.

This includes empty dated folders and the empty `.partial` directory tree, as long as:

```text
- they are empty directories only
- no unverified files are deleted
- cleanup remains scoped to the registered source root
- backfill_execute is not touched because it is outside the registered source root for source_id=66
```

Do not preserve empty `.partial` directories. If cleanup removes the empty `.partial` tree, document that as expected cleanup behavior.

## 4. Proceed with guarded cleanup execution

Approved to proceed with the live guarded cleanup path:

```text
- fresh dry run
- verify exact one-file eligible set
- execute through /api/admin/icloud-staging-cleanup/execute
- use the existing confirmation phrase
- verify post-cleanup state
```

## 5. Still not allowed

```text
- no acquisition
- no acquire_limit=5
- no Source Intake run
- no remote iCloud deletion
- no adjusted-resource eligibility change
- no adjusted-resource import
- no Admin UI
- no scheduler
- no manual filesystem deletion outside the guarded cleanup endpoint
- no commit or push
```

Please report the fresh dry-run id, cleanup execution result, deleted file count/path, whether empty directories were removed, and post-cleanup verification.
