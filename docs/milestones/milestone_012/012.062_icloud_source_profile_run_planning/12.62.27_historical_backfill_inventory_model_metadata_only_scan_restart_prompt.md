# New Chat Ground Rules — Photo Organizer Milestone 12.62.27 Restart

We are restarting Milestone 12.62.27 from a clean branch state.

## Current Git State

Current branch:

```text
feature/icloud-final-routine
```

Current HEAD:

```text
2d9dff8 Milestone 12.62.26: raise iCloud recent sync limits
```

Working tree should be clean.

A prior 12.62.27 attempt was abandoned due to scope/test/closeout drift. Do not reuse it automatically.

## Strict Git Rules

Do **not** run any of the following unless I explicitly ask:

```text
git commit
git push
git reset
git rebase
git merge
git tag
git checkout another branch
git stash
```

You may show proposed git commands, but do not execute write-affecting git commands without explicit approval.

At closeout, provide:

```text
git status --short
git diff --name-only
tests run
files changed
```

Do not commit.

## Task

We are implementing Milestone 12.62.27:

```text
Historical Backfill: Inventory Model and Metadata-Only Scan
```

This milestone must be metadata-only.

Do not download files.  
Do not stage files.  
Do not invoke Source Intake.  
Do not write to Vault.  
Do not add acquisition from inventory.  
Do not add Admin UI.  
Do not add scheduler/automation.

## Required Scope

Implement only:

```text
1. IcloudRemoteAssetInventory model/table
2. IcloudBackfillState model/table
3. Idempotent migration/schema creation using existing repo convention
4. Metadata-only inventory scan service
5. Admin API endpoints:
   POST /api/admin/icloud-backfill/inventory-scan
   GET  /api/admin/icloud-backfill/status
6. Focused model/service/API tests
7. Closeout document
```

## Non-Negotiable Test Requirements

Add explicit tests for:

```text
Model tests:
- inventory row creation
- uniqueness per source + remote identity
- same remote identity allowed across different sources
- backfill state creation/update

Scanner/service tests:
- first scan creates state
- scan writes inventory rows
- repeated scan upserts instead of duplicates
- max_candidates validation
- Live Photo metadata classification if helper metadata exposes it
- unsupported/ambiguous candidates recorded, not globally blocking safe items
- no downloads/staging/Source Intake/Vault calls

API tests:
- POST inventory-scan accepts valid request
- POST inventory-scan rejects max_candidates > 100000
- POST inventory-scan rejects invalid source_profile_id
- GET status returns expected state/counts when state exists
- GET status returns expected 404/not-found when no state exists

Regression:
- rerun 12.62.26 recent-sync limit tests
```

Use mocks/fakes only. No live iCloud. No downloads.

## Key Design Requirements

Use inventory-first historical backfill.

Do not pretend there is a stable iCloud cursor.

Use `observed_remote_position` only as a scan-local ordinal, not as a durable cursor.

Remote identity:

```text
Use the best available helper/listing identity field.
Call it remote_identity_basis = "helper_item_id_observed_stable" or similar.
Do not claim CloudKit/iCloud guarantees long-term stability unless the code/docs prove it.
If no helper identity is available, use a deterministic composite key and document the basis.
```

Known-state semantics:

```text
eligibility_state = whether candidate appears eligible for future acquisition
known_state = whether candidate is already known to local Photo Organizer library
```

For 12.62.27, do not check the local library unless there is already a safe metadata-only service for that.

Eligible inventory rows should usually be:

```text
eligibility_state = "eligible_metadata_only"
known_state = "pending_known_state_check"
```

Do not use `known_state` to mean “media type recognized.”

## Required Closeout

Create/update:

```text
docs/milestones/milestone_012/012.062_icloud_source_profile_run_planning/12.62.27_historical_backfill_inventory_model_metadata_only_scan_closeout.md
```

Closeout must include:

```text
1. Summary
2. Files changed
3. DB models/tables added
4. Migration/schema details
5. Inventory identity strategy
6. Inventory scan service behavior
7. API endpoints added
8. Tests run and exact results
9. Safety boundary confirmation
10. Confirmation that no downloads/staging/intake/Vault/acquisition/UI/scheduler work was added
11. Known limitations
12. Recommended next milestone
13. git status --short
14. git diff --name-only
```

The test-count section must be internally consistent. Do not claim 14 or 19 tests unless that exact number was run and passed.

Proceed carefully and stop if the migration pattern or helper identity behavior is unclear.


Answers to your 12.62.27 design questions
1. Proceed from current HEAD?

Yes. Proceed from f9b4920 after the 12.62.26.1 planner-limit check/fix.

That commit only restores docs and should remain.

2. Migration/schema pattern

Your proposed pattern sounds correct:

- SQLAlchemy models under backend/app/models
- idempotent schema helper following existing schema.py / orchestration_schema.py patterns
- table.create(checkfirst=True)
- inspector-based additive drift tolerance if existing repo pattern uses it

Follow the existing repo convention. Do not introduce Alembic if the repo is not using it for these local schema helpers.

3. Helper identity behavior

Use the helper identity conservatively.

Use:

remote_identity = item.item_id
remote_identity_basis = "helper_item_id_observed_stable"

Do not claim CloudKit/iCloud guarantees long-term stability unless the code/library documentation proves it.

If item_id is required by ExactSelectionLogicalItem and invalid items never reach the scanner, you do not need a composite fallback in 12.62.27. Document composite identity as a future fallback only.

4. API request field name

Use the existing admin/iCloud API style if there is one.

If current internal iCloud admin endpoints use:

source_id

then use source_id externally for the new API too:

POST /api/admin/icloud-backfill/inventory-scan
body: { "source_id": int, "max_candidates": int }

Internally, the model field may be source_profile_id if that matches existing DB/source-profile naming.

The rule is:

API should feel consistent with existing admin routes.
DB/model naming should follow existing source/profile FK conventions.

Do not support both source_id and source_profile_id unless the repo already commonly aliases them.

5. GET status missing state

Yes. Missing state should be a hard 404.

Use the local admin API style. If existing code uses FastAPI HTTPException(status_code=404, detail=...), follow that. If it uses structured JSON with error_code, follow that.

6. Inventory scan limits

Use separate inventory constants from recent sync.

For 12.62.27:

DEFAULT_INVENTORY_SCAN_LIMIT = 50000
MAX_INVENTORY_SCAN_LIMIT = 100000

Tests can use tiny fake values, but production defaults should remain separate from recent sync.

Do not use recent sync’s MAX_CANDIDATE_SCAN_LIMIT = 1000 as the historical inventory default.

7. Eligibility / known-state semantics

Keep these separate:

eligibility_state = whether the item appears eligible for future acquisition
known_state = whether the item is already known to the local Photo Organizer library

For 12.62.27, if no local-library match is performed:

eligible item:
  eligibility_state = "eligible_metadata_only"
  known_state = "pending_known_state_check"

Do not use known_state to mean “media type recognized.”

8. 12.62.27 implementation after 12.62.26.1

Once the planner-limit prerequisite is resolved, proceed with 12.62.27 exactly as scoped:

- models
- idempotent schema helper
- metadata-only scanner
- admin API scan/status endpoints
- model/service/API tests
- closeout

Still excluded:

- no acquisition/download
- no staging
- no Source Intake
- no Vault writes
- no Admin UI
- no scheduler
- no commit/push