# Milestone 12.62.23 - Enable All Supported iCloud Assets in Single-Flow Ingestion Closeout

## 1. Scope Completed

Implemented approved 12.62.23 lock-ins for internal/admin single-flow iCloud ingestion:

- removed blanket execution block for `all_supported_media` while preserving safe-stop behavior for unsupported scopes
- mapped request scope to execution mode without silent downgrade
- changed dry-run execution-safe fill semantics to logical asset count (not resource count)
- added explicit mixed-asset reporting fields (asset scope, still/video/live-photo logical/resource counters)
- updated Admin card wording from media-oriented to asset-oriented
- expanded backend tests for mixed-asset and no-downgrade expectations

## 2. Backend Changes

### 2.1 Internal Wrapper Scope Semantics

Updated internal wrapper service to allow executable scopes and preserve safety:

- file: `backend/app/services/admin/internal_icloud_run_service.py`
- behavior:
  - `ordinary_stills` -> `ordinary_still_only=True`
  - `all_supported_media` -> `ordinary_still_only=False`
  - other exposed scopes (`videos_only`, `stills_with_live_photo_pairs`) still return explicit safe stop (`MEDIA_SCOPE_NOT_SUPPORTED_FOR_EXECUTION`)
- status projection now reports:
  - `requested_asset_scope`
  - `effective_asset_scope`
  - explicit media/resource count fields when payload is available

### 2.2 Orchestrator Reporting and Fill Logic

Updated internal loop orchestrator payload/report details:

- file: `backend/app/services/icloud_acquisition/internal_loop_orchestrator.py`
- changes:
  - execution-safe fill uses logical count target: `selected_count == batch_target`
  - added mixed-asset counting helper over selected acquisition items/resources
  - report/status now include:
    - `ordinary_still_logical_count`
    - `ordinary_still_resource_count`
    - `video_logical_count`
    - `video_resource_count`
    - `live_photo_logical_count`
    - `live_photo_still_resource_count`
    - `live_photo_motion_resource_count`
    - `requested_asset_scope`
    - `effective_asset_scope`
    - `ambiguous_count`

### 2.3 Schema Surface

Extended admin status schema:

- file: `backend/app/schemas/admin.py`
- added fields aligned with above payload/report counters and asset-scope names.

## 3. Frontend Changes

### 3.1 Types

Updated internal run status types:

- file: `frontend/src/types/ui-api.ts`
- added:
  - `InternalIcloudAssetScope`
  - `requested_asset_scope`
  - `effective_asset_scope`
  - explicit still/video logical/resource counters
  - `ambiguous_count`

### 3.2 Admin Card

Updated internal iCloud card language and displayed counters:

- file: `frontend/src/components/IcloudInternalRunCard.tsx`
- changes:
  - label changed to Asset Scope
  - default selection changed to All supported assets (`all_supported_media`)
  - displays requested/effective asset scope and explicit mixed-asset counters
  - retains existing safe-stop handling and cloud deletion visibility

## 4. Tests Added/Updated

### 4.1 API Tests

- file: `backend/tests/test_admin_internal_icloud_run_api.py`
- added assertion that `all_supported_media` request can be accepted (HTTP 200 path) when service accepts start.

### 4.2 Orchestrator Tests

- file: `backend/tests/test_icloud_internal_loop_orchestrator.py`
- added:
  - logical-vs-resource batch fill coverage for live-photo pair selection
  - mixed-asset payload counter coverage (still/video/live-photo breakdown)

## 5. Validation Evidence

## 5.1 Command Results

1. Admin API tests

```powershell
& "c:/Users/chhen/My Drive/AI Photo Organizer/Photo Organizer_v1/.venv/Scripts/python.exe" -m unittest discover -s tests -p "test_admin_internal_icloud_run_api.py"
```

Result: `Ran 6 tests ... OK`

2. Orchestrator tests

```powershell
& "c:/Users/chhen/My Drive/AI Photo Organizer/Photo Organizer_v1/.venv/Scripts/python.exe" -m unittest discover -s tests -p "test_icloud_internal_loop_orchestrator.py"
```

Result: `Ran 17 tests ... OK`

3. iCloud-focused backend bundle

```powershell
& "c:/Users/chhen/My Drive/AI Photo Organizer/Photo Organizer_v1/.venv/Scripts/python.exe" -m unittest discover -s tests -p "test_icloud*.py"
```

Result: `Ran 121 tests ... OK`

4. Full backend suite

```powershell
& "c:/Users/chhen/My Drive/AI Photo Organizer/Photo Organizer_v1/.venv/Scripts/python.exe" -m unittest discover -s tests
```

Result: `Ran 238 tests ... OK`

5. Frontend lint

```powershell
npm run lint
```

Result: pass (`exit code 0`) with existing non-blocking warnings only.

6. Diff hygiene

```powershell
git diff --check
```

Result: no diff-check errors; only line-ending normalization warnings.

## 5.2 Bounded Live Validation

### Validation A - ordinary_stills regression

Observed live run:

- start: HTTP accepted (`status=started`)
- run_id: 8
- final: `completed`, `stop_reason=total_limit_reached`
- requested/effective media scope: `ordinary_stills` / `ordinary_stills`
- requested/effective asset scope: `ordinary_stills_only` / `ordinary_stills_only`
- counts:
  - `logical_assets_selected=1`
  - `resources_selected=1`
  - `ordinary_still_logical_count=1`
  - `ordinary_still_resource_count=1`
  - `video_logical_count=0`
  - `video_resource_count=0`
  - `live_photo_logical_count=0`
- cleanup safety outcomes:
  - `cloud_deletion_occurred=false`
  - `final_staging_clean=true`
  - `drop_zone_clean=true`
  - `partial_workspace_clean=true`

### Validation B - all_supported_media bounded behavior

Observed live response for same bounded source and limits:

- HTTP 409 safe stop payload returned (expected domain response)
- payload status: `stopped`
- stop reason: `DRY_RUN_NOT_SAFE`
- requested/effective media scope: `all_supported_media` / `null`
- requested/effective asset scope: `all_supported_assets` / `null`
- next safe action: `Add new supported iCloud assets or increase candidate_search_cap after review.`
- cloud deletion remained `false`

Interpretation:

- execution path is enabled for `all_supported_media` (no blanket scope hard block), but bounded inventory in this live slice did not satisfy dry-run execution safety at time of validation.
- this is an environment/data availability blocker, not a scope downgrade regression.

## 5.3 Final Clarifications (Pre-Commit)

1. For the `all_supported_media` live run, was selector mode `ordinary_still_only=False`?

- Yes.
- Verified by code path in `start_internal_single_flow_run(...)` mapping `all_supported_media -> ordinary_still_only=False` and passing that value into `plan_internal_loop(...)`.

2. Why did the bounded `all_supported_media` dry-run fail?

- Primary reason: insufficient safe supported logical inventory within the bounded `candidate_search_cap`/limit window for this run.
- Supporting signal in returned payload: `logical_assets_selected=0`, `resources_selected=0`, `unsupported_or_blocked_count=2`, `stop_reason=DRY_RUN_NOT_SAFE`.
- It was not because the run was forced to ordinary-stills-only.

3. Was next-safe-action wording updated for all-supported assets?

- Yes.
- Updated to scope-aware wording and re-verified live response:
  - `Add new supported iCloud assets or increase candidate_search_cap after review.`
- Ordinary-stills wording is now emitted only when `ordinary_still_only=True`.

## 6. Assumptions and Non-Breaking Decisions

- retained explicit safe-stop behavior for still non-executable explicit scopes (`videos_only`, `stills_with_live_photo_pairs`) to avoid unsafe partial semantics.
- preserved compatibility fields (`ordinary_still_count`, `video_count`) while adding explicit new counters.
- accepted pre-existing frontend lint warnings as non-blocking because they were not introduced by this milestone.

## 7. Milestone Checklist

- what changed:
  - backend scope handling, orchestrator counting/reporting, schema extensions, frontend types/card wording, and test coverage updates
- how to run:
  - use commands listed in Section 5.1
- what passed:
  - backend targeted tests, iCloud bundle, full backend suite, frontend lint (warnings only), diff-check hygiene

## 8. Explicit Final Outcome

- all_supported_media blanket block removed: yes
- all_supported_media live execution completed: no
- all_supported_media live run stopped safely: yes
- reason: DRY_RUN_NOT_SAFE / insufficient safe supported inventory within bounds
- ordinary_stills regression passed: yes
- standalone video live validation: not performed / not selected
- Live Photo live validation: not performed / not selected
- videos_only explicit scope remains blocked: yes
- stills_with_live_photo_pairs explicit scope remains blocked: yes
- silent downgrade from all_supported_media to ordinary_stills: no
- cloud deletion occurred: no
- normal user-facing exposure added: no
- normal admin API exposure added beyond internal/admin route: no
